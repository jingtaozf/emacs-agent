# Test env file sourced by the e2e cmux ENV_FILE story.
# Real credentials never live here — these are obvious test markers
# we can grep for inside the spawned cmux pane via `capture-pane`.
CMUX_ENV_FILE_MARKER="loaded-from-env-file"
