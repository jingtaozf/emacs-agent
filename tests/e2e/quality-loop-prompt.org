#+TITLE: Architecture & Code Quality Improvement Loop
#+DATE: 2026-03-29
#+DESCRIPTION: Prompt for /loop recurring code quality iterations

* Role

You are a senior engineering team: architect, code quality lead, and test
engineer. Each iteration you improve the claude-agent codebase incrementally
— one focused change at a time, verified by tests, committed cleanly.

Never ask questions or enter plan mode. Decide autonomously. Fix issues
directly, verify with tests, commit, and move on.

* Principles

** Boy Scout Rule
Leave every file cleaner than you found it. One improvement per touched file.

** Mikado Method
When a refactor has cascading dependencies, start at the leaves (no other
refactors depend on them), fix those first, then work inward.

** Strangler Fig
Wrap old code with a thin shim, build new behind it, migrate callers one
by one, delete the shim last.

** Ratchet Pattern
Add structural tests that prevent new debt. If the count of known issues
increases, CI should fail.

** Small is Beautiful
Each iteration = one focused change. Never combine refactoring with feature
work. Prep commits (add abstraction) separate from move commits (migrate callers).

* Reference Documents

| File | Purpose |
|------+---------+
| docs/design-docs/2026-codebase-review-improvements.org | Authoritative tech debt map (P0-P3) |
| ARCHITECTURE.org | Module boundaries, invariants, absence constraints |
| docs/ELISP_IDIOMS.org | Emacs Lisp footguns for AI agents |
| docs/literate-programming-principles.org | Literate elisp block discipline |
| tests/test-structural.el | Mechanical invariant enforcement |
| tests/test-static-analysis.el | Dynamic static analysis |

* Priority Queue (work through in order)

Each iteration, pick the HIGHEST priority item that hasn't been done yet.
Check the design doc for current status before starting.

** P0 — Prevents Silent Failure

*** DONE Q1: Guard process filter with condition-case :done:
- Already implemented: claude-agent--process-filter and claude-agent--process-sentinel
  both wrap body in (condition-case err ... (error (message ...)))
- Structural test: test-structural-process-filters-guarded (F34) prevents regression
- Verified [2026-04-06]

*** DONE T7: Sentinel tests (zero coverage) :done:
- Implemented: tests/test-claude-agent-sentinel.el (719 lines, 7+ tests)
- Covers: normal exit, abnormal exit, buffer killed during stream,
  signal kill, double sentinel fire, non-object JSON, Docker path-mapping cleanup
- Tag: :unit :sentinel
- Verified [2026-04-06]

*** DONE A1: Consolidate parallel state tracking (active-states) :done:
- Implemented [2026-03-29]: Removed standalone claude-agent--active-states defvar
  and all setq sync points from claude-agent-backend.org
- All reads go through claude-agent-registry-active-states accessor
- All writes use setf on the registry struct field
- Test code updated to use registry accessors
- Structural test F38: test-structural-no-active-states-alias prevents reintroduction
- Remaining: hash-table aliases (pending-background-tasks, pending-control-requests)
  are safe (same object) but can be consolidated in future iteration

** P1 — Wrong Behavior Users See

*** DONE Q11: Remove duplicate extract-error-message :done:
- Verified [2026-03-29]: duplicate defun in claude-org.org already removed
- Single canonical definition in claude-agent-backend.org:2478 covers all cases
  (symbol error, (error "msg") list, plist, string, fallback)

*** DONE Q12: Clear path-mappings on abnormal exit :done:
- Implemented [2026-03-29]
- Both sentinel-cleanup and sentinel-handle-abnormal-exit clear path-mappings
- Tests: test-sentinel-abnormal-exit-clears-docker-path-mappings,
  test-sentinel-abnormal-exit-preserves-mappings-for-non-docker

*** DONE Stale iTerm2 references in docstrings :done:
- Implemented [2026-03-29]
- Removed all iTerm2 mentions from claude-org-terminal-base.org (5),
  claude-org.org (2), claude-org-cmux.org (3)
- Structural test F37: test-structural-no-iterm2-references prevents reintroduction

** P2 — Missing Tests for Dangerous Paths

*** DONE T3: Protocol conformance fuzzer :done:
- Implemented [2026-03-29]: 14 property-based fuzzer tests in test-json-parser-property.el
- Covers: random splits, interleaved ordering, malformed recovery, large messages,
  deeply nested JSON, buffer overflow, control_request dispatch, mixed valid/invalid
- Property-based test for JSON stream parser
- Generate semi-valid JSON streams, feed to process filter
- Verify no unhandled errors

*** DONE Sentinel edge cases + non-object JSON bug fix :done:
- Implemented [2026-03-29]: 7 new tests in test-claude-agent-sentinel.el
- Buffer killed during stream: sentinel handles dead output buffer
- Process exits with signal: hangup and killed:9 events handled via fallback
- Rapid successive exit: double sentinel fire is idempotent
- Non-object JSON: bare values (42, "string") no longer crash plist-get/intern
- Bug fix in claude-agent-backend.org: both process-json-buffer and sentinel
  remaining-JSON handler guard with (listp parsed) and (stringp msg-type)

*** DONE Permission round-trip :done:
- Implemented [2026-03-29]: 10 unit tests in test-permission-round-trip.el
- Allow/deny/error round-trips, tracking lifecycle, hook_callback, unsupported
  subtype, dead process, context propagation, updated input, concurrent requests
- Integration test already existed in test-harness-phase2.el

** P3 — Polish and Cleanup

*** Remove dead claude-cli/eat backend code
- claude-agent-backend.org lines 3775-4470 (~700 lines)
- Retained after commit 1038144 removed iTerm2/native
- Remove if no active users (check with user first for this one)

*** DONE Stale verbose output dispatcher :done:
- Implemented [2026-03-29]: Updated claude-org-show-verbose docstring to
  accurately describe the 3-level fallback (cmux capture, backend-specific,
  shared session). The fallback chain itself is correct; only the docstring
  was stale.

*** DONE Extract ensure-session (154 lines) :done:
- Implemented [2026-03-29]: Extracted 4 helpers from claude-org-cmux--ensure-session
  (workspace-alive-p, restore-workspace, resolve-workspace-uuid, launch-workspace)
- Orchestrator reduced from 154 to 30 lines
- All 963 unit tests + 116 Python tests pass

*** DONE Hardcoded /tmp paths in tests :done:
- Implemented [2026-03-29]: Replaced all 14 occurrences in test-cmux-e2e-simulated.el
  with `claude-org-terminal-status-dir' constant
- Structural test F40: test-structural-no-hardcoded-tmp-in-tests prevents reintroduction
- 573 unit tests + 116 Python tests pass

*** DONE Commented-out test file :done:
- Implemented [2026-03-29]: Validation functions already exist in claude-agent-backend.org
  (claude-agent--validate-answer, validate-file-path, validate-command) and
  claude-org.org (claude-org--parse-env-file-safe). All 20 tests pass.
- Uncommented and added to test-org-unit target (needs LOAD_ALL for env parsing tests)
- Removed stale "TDD stubs" comment from Makefile
- Updated test file header to reflect implemented status
- Structural test F41: test-structural-no-commented-out-test-files prevents reintroduction
- 259 agent unit + 593 org unit tests pass (20 newly enabled)

** P4 — Discovered in Fresh Scan (Iteration 12+)

*** DONE Extract verbose-format-message (69 lines) :done:
- Implemented [2026-03-29]: Extracted 5 helpers from claude-agent--verbose-format-message
  (--verbose-format-content-blocks, --verbose-format-assistant-msg,
   --verbose-format-user-msg, --verbose-format-system-msg, --verbose-format-result-msg)
- Dispatcher reduced from 69 to 9 lines
- 16 new unit tests in test-verbose-formatter.el
- Structural test F43: test-structural-verbose-format-dispatcher-thin prevents regrowth
- 979 unit tests + 116 Python tests pass

*** DONE Extract activity-string helpers (70 lines) :done:
- Implemented [2026-04-06]: Extracted 2 helpers from claude-agent--update-activity-string
  (--activity-alert-string, --activity-queries-string)
- Dispatcher reduced from 70 to 15 lines
- Structural test F44: test-structural-activity-string-dispatcher-thin prevents regrowth
- All checks pass (lint + unit + python)

*** Long functions remaining (>60 lines)
- claude-agent--start-process (100 lines, claude-agent-backend.org:2111) [sequential setup — low ROI]
- claude-org-execute (91 lines, claude-org.org:4489)
- claude-agent-list-queries (76 lines, claude-agent.org:449)
- claude-org-cmux--launch-workspace (73 lines, claude-org-cmux.org:512)
- claude-agent--update-activity-string (15 lines, claude-agent.org — DONE, extracted helpers)
- claude-org--workspace-create-docs-files (70 lines, claude-org.org:3336)
- claude-agent--process-sentinel (69 lines, claude-agent-backend.org:1645)
- claude-org--recover-session (68 lines, claude-org.org:1082)
- claude-org--send-request (65 lines, claude-org.org:4678)
- claude-agent-generate-title (65 lines, claude-agent.org:1445)
- claude-agent--verbose-insert-query-header (64 lines, claude-agent-backend.org:3133)
- claude-org-cmux--execute-ai-block (63 lines, claude-org-cmux.org:732)
- claude-org--handle-complete (63 lines, claude-org.org:4296)
- claude-org-cmux--build-launch-command (62 lines, claude-org-cmux.org:355)
- claude-org--execute-core (62 lines, claude-org.org:4426)
- claude-agent--handle-control-request (61 lines, claude-agent-backend.org:2806)
- Not all need extraction — some are intrinsically sequential.
  Focus on those with clear phase boundaries.

* Quality Dimensions

Each iteration should address ONE item from the priority queue above,
following this workflow.

** Architecture Quality

Check against ARCHITECTURE.org:
- Dependencies flow downward (higher → lower abstraction)
- No circular dependencies between modules
- Each module has one reason to change
- Absence constraints table is respected and growing
- Module boundary diagram matches actual code

** Code Quality

Apply these checks to touched files:
- Functions under 50 lines (extract helpers for longer ones)
- Max 3 levels of let nesting
- Max 4 cond branches (extract predicate functions)
- Every defun has a docstring
- Every defvar/defconst has a docstring
- Process filters and sentinels wrapped in condition-case
- Hash tables have matching puthash/remhash (no leaks)
- Closures use lexical-let in dynamic-binding context
- No hardcoded paths (/tmp, /Users, etc.)

** Test Quality

Apply to touched test files:
- File-backed buffers when code calls save-buffer
- unwind-protect for all cleanup (not inline at end)
- Tags on every test (:unit, :regression, :integration)
- No hardcoded paths — use constants
- Assertions test behavior (org properties, cmux commands sent), not internals
- Every bug fix has a regression test

** Documentation Quality

- Prose above code blocks matches what code does
- ARCHITECTURE.org updated if module boundaries change
- Absence constraints table grows with new invariants
- Design doc status headers updated (proposed → implemented)
- Stale references to removed features cleaned up

* Each Iteration Workflow

** 1. Assess Current State
- Read 2026-codebase-review-improvements.org for the priority table
- Check which items are already done (look for strikethrough or status)
- Pick the highest-priority undone item
- Run make check to get a clean baseline

** 2. Brief Web Research
- Search for 1 specific best practice related to the item you're fixing
- Example: "Emacs process filter error handling best practices"
- Apply findings to improve the fix

** 3. Write Failing Test (if applicable)
- Before fixing, write a test that exposes the issue
- Verify the test fails for the right reason
- Tag with :regression if it's a bug fix

** 4. Make the Fix
- Smallest possible change
- One concern per commit
- After editing .org files, reload via literate-elisp-load in Emacs
- Run make test-smoke immediately after any edit

** 5. Verify
- Run make check (full suite)
- Verify the new test passes
- Verify no regressions
- If architecture changed, verify ARCHITECTURE.org is updated

** 6. Update Tracking
- Update 2026-codebase-review-improvements.org: mark item done with date
- Add structural test if a new invariant was discovered
- Update absence constraints in ARCHITECTURE.org if applicable
- Commit with descriptive message: fix: / refactor: / test: prefix

** 7. Quality Sweep of Touched Files
- While in the file, fix any adjacent quality issues (Boy Scout Rule)
- Stale docstrings, missing error handling, hardcoded paths
- But keep these as separate commits from the main fix

* Rules

- Never ask questions — decide autonomously
- Never enter plan mode — act directly
- One focused change per iteration (not multiple unrelated fixes)
- Always run make test-smoke after editing .org files
- Always run make check before committing
- After editing .org source, reload via literate-elisp-load in Emacs
- Commit with conventional prefix: fix:, refactor:, test:, docs:
- Never combine refactoring with feature work in one commit
- If a fix breaks > 3 tests, stop and reconsider the approach
- Keep ARCHITECTURE.org in sync with reality
- Grow the absence constraints table — rules are multipliers
- Update the design doc priority table after each completed item
- evalElisp for Emacs operations (< 2s only)
- Bash for make, git, grep
- Navigate org by CUSTOM_ID, never heading text
- For Live E2E items: execute via evalElisp + cmux CLI, then add simulated test
- If cmux is not running, fall back to simulated-only testing
- Skip Phoenix trace assertions if localhost:6006 unreachable
- Record live test results in tests/e2e/living-workspace-test.org
- Append test workspaces to tests/e2e/org/workspace-test.org (never reset)

* Live E2E Coverage (work through after P4)

Each item produces TWO artifacts: a live E2E test (evalElisp + cmux CLI,
recorded in living-workspace-test.org) and a simulated test (mock cmux,
added to test-cmux-e2e-simulated.el for CI via make test-cmux).

Live test discovers real-world issues. Simulated test locks in the fix.

** P0-E2E — User-Facing Flows with Zero Test Coverage

*** DONE E01: Launch fresh workspace :done:
- Execute: create workspace via ensure-session on new story heading
- Verify: new-workspace called, CMUX_SURFACE_ID + CMUX_WORKSPACE_ID set
- Verify: color applied, verbose started, workspace visible in cmux list-workspaces
- Simulated: assert mock calls for new-workspace, list-pane-surfaces, sidebar-state

*** DONE E02: Restore existing workspace (Emacs restart) :done:
- Execute: clear hash tables, call ensure-session on story with saved props
- Verify: hash tables rebuilt, no new-workspace called, verbose restarted
- Simulated: T53 exists — extend to verify verbose timer started

*** DONE E03: Recover stale UUID after cmux restart :done:
- Execute: store stale UUID in org, call ensure-session
- Verify: list-workspaces queried, props refreshed, workspace reused
- Simulated: T61 exists — extend to cover restart flow end-to-end

*** E04: Restart Claude Code in existing tab
- Execute: claude-org-cmux-restart on running workspace
- Verify: /exit sent, shell prompt detected, fresh Claude Code launched
- Verify: verbose restarted, IDE /ide sent
- Simulated: mock capture-pane transitions (busy→shell→INSERT)

*** E05: Execute simple query end-to-end
- Execute: real AI block in workspace-test.org
- Verify: prompt sent, response appears in org, busy→ready transition
- Verify: cmux capture-pane shows Claude Code output
- Simulated: extend existing execute tests with response verification

*** E06: Cancel active query
- Execute: start query, send escape via cmux-cancel
- Verify: busy cleared, [Cancelled] text in org, no orphan state
- Simulated: mock busy→cancel transition

*** E07: Execute with --resume (session continuity)
- Execute: two queries in same story
- Verify: second launch uses --resume with CLAUDE_CLI_SESSION
- Verify: org property persisted between queries
- Simulated: assert --resume flag in build-launch-command output

*** E08: Queue Block B while Block A runs
- Execute: start Block A, immediately execute Block B
- Verify: B queued, A completes, B auto-executes, responses in correct positions
- Simulated: extend existing queue tests

** P1-E2E — Verbose, Streaming, and Session Management

*** E09: Verbose buffer created on session start
- Execute: ensure-session on new workspace
- Verify: buffer *cmux: <session-id>* exists, header-line set
- Verify: verbose timer running (claude-org--session-get :verbose-timer)
- Simulated: assert create-verbose-buffer called with correct args

*** E10: Verbose tick captures screen changes
- Execute: send text to surface, wait for verbose tick
- Verify: verbose buffer contains the sent text (diff-based dedup)
- Simulated: mock capture-pane returns changing content, verify buffer update

*** E11: Verbose restart on surface change
- Execute: relaunch workspace (new surface), check verbose
- Verify: old timer cancelled, new timer started with fresh surface
- Simulated: assert stop-verbose then start-verbose sequence

*** E12: Stop verbose on session cleanup
- Execute: close workspace or kill buffer
- Verify: timer cancelled, no orphan timers in timer-list
- Simulated: assert cancel-timer called

*** E13: Session recovery from org buffer scan
- Execute: clear hash tables, call recover-session with known session ID
- Verify: scans buffers, finds matching CLAUDE_SESSION_ID, rebuilds mappings
- Simulated: T50 exists — extend to verify surface + workspace-id restored

*** E14: Story switch updates ACTIVE_STORY
- Execute: switch from story A to story B via on-story-changed
- Verify: ACTIVE_STORY property updated, tab renamed
- Simulated: mock rename-tab call with correct title

** P2-E2E — Color, Icon, Launch Command

*** E15: Apply named color (Red, Blue, Teal, Purple)
- Execute: set WORKSPACE_COLOR on heading, call apply-color
- Verify: cmux sidebar-state shows color pill
- Simulated: assert set-status called with correct color hex

*** E16: Apply hex color (#C0392B)
- Execute: WORKSPACE_COLOR: #C0392B, call apply-color
- Verify: sidebar-state shows hex color
- Simulated: assert hex passed through to set-status

*** E17: Apply color + icon
- Execute: WORKSPACE_COLOR + WORKSPACE_ICON set
- Verify: both color and icon appear in sidebar-state
- Simulated: assert set-status called with icon= parameter

*** E18: Color applied early (before wait-for-ready)
- Execute: launch workspace, check color before TUI ready
- Verify: color visible while Claude Code still loading
- Simulated: assert apply-color called before wait-for-ready in call order

*** E19: Claude legacy launch command (no profile)
- Execute: build-launch-command with default agent type
- Verify: flags include --print, --output-format stream-json
- Simulated: assert command string structure

*** E20: Claude profile launch command
- Execute: build-launch-command with CLAUDE_PROFILE set
- Verify: CLAUDE_PROFILE env var in command, --resume if session exists
- Simulated: assert profile flag and resume flag

*** E21: Copilot launch command
- Execute: build-launch-command with AGENT_TYPE=copilot
- Verify: copilot CLI path used, COPILOT_CLI_SESSION read
- Simulated: assert copilot-specific flags, no claude-only flags

*** E22: System prompt injected in launch
- Execute: build-launch-command with :system_prompt: section
- Verify: --system-prompt flag present with bridge content
- Simulated: assert workspace-bridge-system-prompt called

*** E23: Permission mode bypass
- Execute: CLAUDE_PERMISSION_MODE: bypass on heading
- Verify: --dangerously-skip-permissions in launch command
- Simulated: assert flag present in built command

** P3-E2E — Loop, IDE, Wait, and Edge Cases

*** E24: Loop send dispatches prompt
- Execute: trigger loop-send with prompt text
- Verify: from-emacs flag written, text sent to surface, enter pressed
- Simulated: assert write-from-emacs-flag + send + send-key sequence

*** E25: Loop cancel guard skips iteration
- Execute: cancel session, trigger loop iteration
- Verify: iteration skipped with "cancelled" message
- Simulated: existing test — extend with property verification

*** E26: IDE server starts for Claude Code
- Execute: ensure-ide-server for workspace
- Verify: lockfile exists in ~/.claude/ide/
- Simulated: assert ide--session-alive-p or lockfile creation

*** E27: IDE server skipped for Copilot
- Execute: ensure-session with AGENT_TYPE=copilot
- Verify: no IDE lockfile created, no /ide sent
- Simulated: assert ensure-ide-server NOT called

*** E28: Wait detects INSERT mode
- Execute: wait-for-ready with capture-pane showing INSERT
- Verify: returns promptly, no timeout
- Simulated: mock capture-pane → ready pattern, assert quick return

*** E29: Wait timeout on slow launch
- Execute: wait-for-ready with capture-pane never showing ready
- Verify: times out after configured seconds, returns anyway
- Simulated: mock capture-pane → always "loading...", assert timeout

*** E30: Unicode in story name
- Execute: create workspace with unicode heading (e.g. "API 开发")
- Verify: workspace created, slug generated, no crash
- Simulated: existing test — extend with CJK characters

*** E31: Org metacharacters in heading
- Execute: heading with * and # characters
- Verify: properties set correctly, no org corruption
- Simulated: existing test covers this

*** E32: File-level CLAUDE_BACKEND dispatch
- Execute: #+PROPERTY: CLAUDE_BACKEND cmux at file level
- Verify: ensure-session dispatches to cmux backend
- Simulated: T52 exists — extend with property inheritance check

*** E33: TabManager not available recovery
- Execute: close all cmux tabs, then ensure-session
- Verify: new-window created first, then new-workspace succeeds
- Simulated: mock first new-workspace → TabManager error, retry succeeds

*** E34: Concurrent permission + query
- Execute: trigger permission-needed while query active
- Verify: both handled, no crash, workspace focused
- Simulated: call permission-needed mid-query, assert state consistency

*** E35: Stale verbose timer after workspace relaunch
- Execute: relaunch workspace (surface changes), check timers
- Verify: old timer replaced, no double-tick, buffer updated
- Simulated: assert stop-verbose then start-verbose, single timer

*** E36: set-status / clear-status round-trip
- Execute: set-status "test_key" "test_value", then clear-status "test_key"
- Verify: sidebar-state shows key after set, key gone after clear
- Simulated: assert set-status and clear-status mock calls

*** E37: set-progress bar
- Execute: set-progress "0.5" --label "Building..."
- Verify: sidebar-state shows progress
- Simulated: assert set-progress mock call with value and label
