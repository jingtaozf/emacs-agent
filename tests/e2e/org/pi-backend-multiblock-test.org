# -*- mode: org; eval: (code-agent-org-mode 1) -*-
#+TITLE: Pi Backend Multi-Block Same-Workspace E2E Fixture

* Pi Multi-Block Workspace
:PROPERTIES:
:CUSTOM_ID: pi-mb-workspace
:CLAUDE_BACKEND: pi
:CLAUDE_SESSION_ID: pi-mb-e2e
:END:

Two AI blocks under the SAME workspace heading.  Together they prove
two invariants the user reported broken on 2026-05-25:

1. *Same Pi subprocess across blocks* — block 2's =C-c C-c= must
   reuse the still-alive =pi --mode rpc= subprocess from block 1, not
   spawn a fresh one.  PID equality is the assertion.

2. *Session does NOT get stuck =:busy t= after block 1* — historically
   block 1's =agent_end= fired its =:on-complete= callback but the
   chain that clears =:busy= never reached its destination, so block
   2 got queued instead of dispatched.

A third block exercises *restart resilience*: between block 2 and
block 3 the runner explicitly kills Pi's process; block 3 must
trigger a respawn through the same session-key and still deliver a
response.

** Workflow :sdd:

*** Instruction 1 :claude_chat:pi-mb-e2e:
:PROPERTIES:
:CUSTOM_ID: pi-mb-1
:EXPECTED: ACK_ONE
:TIMEOUT: 60
:END:

#+begin_src ai
Reply with exactly the text ACK_ONE and nothing else. No punctuation, no quotes.
#+end_src

*** Instruction 2 :claude_chat:pi-mb-e2e:
:PROPERTIES:
:CUSTOM_ID: pi-mb-2
:EXPECTED: ACK_TWO
:TIMEOUT: 60
:END:

#+begin_src ai
Reply with exactly the text ACK_TWO and nothing else. No punctuation, no quotes.
#+end_src

*** Instruction 3 (after Pi kill) :claude_chat:pi-mb-e2e:
:PROPERTIES:
:CUSTOM_ID: pi-mb-3
:EXPECTED: ACK_THREE
:TIMEOUT: 60
:END:

#+begin_src ai
Reply with exactly the text ACK_THREE and nothing else. No punctuation, no quotes.
#+end_src
