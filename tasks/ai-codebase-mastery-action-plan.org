#+TITLE: AI Codebase Mastery → code-agent repo 行动计划
#+AUTHOR: Jingtao
#+DATE: 2026-05-01
#+SOURCE_OF_LENS: ~/projects/dummy/notes/ai-codebase-mastery.org(19 lens)
#+TARGET_REPO: ~/projects/emacs-agent/
#+OPTIONS: toc:2

* 概览

对照 19 个 lens 的 *Best practices*,逐项判定 code-agent repo 现状,产出可执行 checklist。
标记:[X] 已具备;[-] 部分具备;[ ] 缺失。

** 现状速览(全 lens 横扫)

| 类别 | 已具备 | 部分 / 待加强 | 缺失 |
|------+--------+---------------+------|
| 文档 | ARCHITECTURE.org / CLAUDE.md(12K) | AGENTS.md(736 行,**超 cap 12×**) | lessons.md / NL Outline |
| 测试 | ert + tag 分层 + mock CLI + test-structural.el | unit/integration | mutation testing / characterization / property-based |
| 安全 | (基本无) | — | gitleaks / SBOM / OWASP LLM Top 10 review |
| 观测 | Phoenix(:6006)+ OTel bridge | gen_ai.usage 部分 | 团队级 ROI dashboard |
| 协作 | git-worktree skill / pre-commit hook | review at scale 协议 | PR size cap / approval gates 集中 |
| Agent | tasks/ 目录 | subagent 用法 | tribal knowledge mining / litprog reverse-sync |

** 头号风险(立即处理)

*AGENTS.md 736 行* — Lens #5 / #11 共识 *≤60 行*,**超 12 倍**。
ETH 研究证实超长 agent doc 让任务成功率反降 0.5-2%、推理成本 +20%。
这是单条最高 ROI 的修法,优先于其他所有项。

** 怎么读这份计划

1. 每个 lens 一节;每节内含 *现状* + *对照表* + *Action Items*。
2. 文末 [[#priority-summary][* 优先级总结]] 给你 *第 1 周 / 第 1 月 / 第 1 季度* 三档 actionable 清单。
3. 如果只看一节,看 [[#priority-summary][优先级总结]]。

* Lens #1 — Onboarding to a vibe-coded codebase
:PROPERTIES:
:CUSTOM_ID: a-lens-1
:END:

** 现状

你不是接手别人的 vibe code,是自己长期维护;但模块演化已快到 "风格分界 commit"
现象出现(如 tri-protocol 重构前后)。

** Best practices 对照

- [X] git log/blame 路径清楚(commit message 已严格)
- [X] AI 当 junior 对待(每条 PR 经 pre-commit + 自审)
- [ ] 第一周 security pass(从未跑过完整 secret scan history)
- [X] ARCHITECTURE.org / 不变量已写
- [-] AI 来源注释:大块改动有 commit footer,但 module top-level 没有 "(AI v / date / prompt 摘要)" 这种 metadata

** Action Items

- [ ] 跑一次 `gitleaks detect --no-banner --redact` 扫完整 git history,记录处理结果到 tasks/lessons.md
- [ ] 给主要 .org 模块顶部加 :AI_CONTRIBUTION: drawer(列出大改动的 model + 日期 + prompt 摘要),便于后人 trace

* Lens #2 — Detecting AI-hallucinated APIs / packages
:PROPERTIES:
:CUSTOM_ID: a-lens-2
:END:

** 现状

Python deps 用 uv.lock + pyproject;Elisp 用 straight.el。lockfile 在,但**没有 supply-chain scanner**。

** Best practices 对照

- [X] lockfile 双层闸(uv.lock + Elisp 用 straight)
- [ ] CI 集成 supply-chain scanner(Socket.dev / OWASP dep-scan / Safety CLI)
- [ ] AI 提议新依赖时 metadata 检查清单(npm view / PyPI API)
- [-] devcontainer 沙盒安装(.devcontainer/ 已有,但是否禁了 install hook 未审过)
- [-] pre-generation 白名单(CLAUDE.md 没显式 "只允许 stdlib + lockfile 已有依赖")

** Action Items

- [ ] 在 `.github/workflows/ci.yml` 加 `safety check` + `pip-audit`(Python)+ `npm audit`(若有 JS)
- [ ] CLAUDE.md 加一段 *Dependency rule*:"AI 提议新依赖必须先报 metadata,**人审才入 lockfile**"
- [ ] 增加 pre-commit hook:lockfile 改动时打 ⚠️ 标签让 reviewer 警觉

* Lens #3 — Reviewing AI commits at scale
:PROPERTIES:
:CUSTOM_ID: a-lens-3
:END:

** 现状

pre-commit `make check`(lint + unit + python)在跑;CI 也跑。但**没有 PR size cap**。

** Best practices 对照

- [X] 自动测试 + lint 在 PR 时跑
- [ ] 200 LOC 目标 / 400 LOC 上限的 PR size cap
- [ ] reviewer session ≤ 1 小时(讲规矩)
- [-] 分层 review(浅层 AI 自审 + 人审架构);test-structural.el 已是初步
- [ ] 衡量减负而非 "AI 评论数";没 cycle time / incident rate 仪表盘

** Action Items

- [ ] 在 .github/workflows/ci.yml 加规则:`if PR.diff > 400 lines: fail with hint to split`
- [ ] CLAUDE.md 加 *PR size policy*:目标 200 LOC、硬上限 400 LOC、超出需说明理由
- [ ] 月度抽样统计:cycle time + 拒绝率 + reviewer 工时(手记 tasks/ 即可,先粗)

* Lens #4 — Test coverage discipline for AI-touched code
:PROPERTIES:
:CUSTOM_ID: a-lens-4
:END:

** 现状

ert + 多 tag 分层(unit / stable / mock / integration);Python pytest;但**没有 mutation testing**。
Test 思路偏 unit;integration test 部分覆盖。

** Best practices 对照

- [-] 测试金字塔:unit ✓,integration ◐,end-to-end ✓(tests/e2e/),mutation ✗
- [ ] mutation testing(Python: mutmut;Elisp 没成熟工具,可手作 sample)
- [X] integration test 优先(test-cmux-e2e-simulated.el 等)
- [-] TDD-with-AI:你做过(reproduce-before-fix 规则),但不是项目默认
- [ ] AI 把测试反向翻成 spec 验证(用 LLM-as-judge)

** Action Items

- [ ] Python 加 `mutmut run` 进 CI(先 `python/claude_agent/workspace_bridge.py` 一个文件试点)
- [ ] CLAUDE.md 加 *test contract first* 规则:"非微小改动:先人写测试、再让 AI 填实现"
- [ ] 把 reproduce-before-fix.md 升格为顶层规则(已在 .claude/rules/ 下,但要强调)

* ★ Lens #5 — Documentation as agent context(头号风险)
:PROPERTIES:
:CUSTOM_ID: a-lens-5
:PRIORITY: critical
:END:

** 现状

- CLAUDE.md = 12K(~280 行,可接受)
- *AGENTS.md = 30K(736 行)— **超 ≤60 行 cap 12 倍***
- 两者并存,内容大概率重复 → 双倍 context rot
- ETH 研究:auto-generated 长 doc 让任务成功率 -0.5~2%、推理成本 +20%
- 你 strict-LP 研究里讲的 *NL Outline / litprog-skill reverse-sync* 还没落地

** Best practices 对照

- [X] CLAUDE.md 用了 emphasis(IMPORTANT/ALWAYS/NEVER)
- [-] @import 引外部 doc(CLAUDE.md 用了一些,但 AGENTS.md 没)
- [ ] **≤60 行 cap**(单 doc)
- [ ] CLAUDE.md vs AGENTS.md 二选一(行业共识 AGENTS.md;Claude Code 当前不读它,所以你保留 CLAUDE.md 合理 — 但删 AGENTS.md 或缩到 60 行)
- [ ] NL Outline 在 >40 行函数体内(strict-LP 研究的 cross-ref)
- [ ] secrets / 路径 / 用户名 全清(检查一下两份 doc)

** Action Items(本周必做)

- [ ] *AUDIT*:跑 `diff CLAUDE.md AGENTS.md`,识别两份重叠的部分
- [ ] *DECIDE*:只留 CLAUDE.md;AGENTS.md 内容拆到 docs/ 下用 @import 引(或直接删除)
- [ ] *TRIM*:CLAUDE.md 也压到 ≤60 行核心规则;长内容拆到 docs/(已经做了一部分)
- [ ] 加 nested CLAUDE.md 在子模块(如 python/、tests/)分散 context
- [ ] 给主要 .org module 大函数(>40 行)加 NL Outline 注释段
- [ ] 在 ~/projects/literate-org 上原型 litprog-skill PostToolUse reverse-sync hook

* Lens #6 — Refactoring AI-generated speculation / dead branches
:PROPERTIES:
:CUSTOM_ID: a-lens-6
:END:

** 现状

CLAUDE.md 已有 *Simplicity First* 段("No features beyond what was asked")— 这是对的;
但**没跑 dead-code analyser**,也没 YAGNI lint。

** Best practices 对照

- [X] Simplicity-First 写进 CLAUDE.md
- [X] 禁 bare except(CLAUDE.md *Design Principles* 已写)
- [ ] dead-code analyser(Python: vulture;Elisp: 静态扫 unused defun)
- [ ] YAGNI lint(抽象基类 ≥2 子类、kwarg ≥2 caller 用过非默认值)
- [-] Inline Method / Collapse Hierarchy 类批量重构,做过但非 protocol

** Action Items

- [ ] 把 vulture 加到 `make check` 或 lint 流程,先扫 `python/claude_agent/`
- [ ] 写一个 simple Elisp checker:扫 `*.org` 找未被引用的 defun(可放 test-structural.el)
- [ ] 季度跑一次 "speculative kwargs" 扫描(grep `:keyword` 出现次数 < 2 的)

* Lens #7 — Long-context vs RAG for codebase navigation
:PROPERTIES:
:CUSTOM_ID: a-lens-7
:END:

** 现状

你用 Claude Code(agentic search,grep + Glob + Read);**正确选择**(<50k 文件)。
但 CLAUDE.md 没明文 *agent search 步骤指引*。

** Best practices 对照

- [X] 走 agentic tool-calling 路线(Claude Code default)
- [ ] 在 CLAUDE.md 给 agent search 步骤指引("先 Glob 缩范围、再 Grep、只 Read 必要文件")
- [-] doc 走 RAG 路径(部分 .claude/rules/ 在,但没 vector 索引)
- [N/A] tree-sitter repo map(项目规模没到那个门槛)

** Action Items

- [ ] CLAUDE.md 加一段 *Search efficiency* 指引(降 token 消耗)
- [ ] 评估:rules/ 下的 .md 是否要做轻量 RAG(Anthropic 的 prompt cache 已经覆盖大部分)

* Lens #8 — Prompt patterns for "don't break existing behavior"
:PROPERTIES:
:CUSTOM_ID: a-lens-8
:END:

** 现状

*reproduce-before-fix.md* 已在 .claude/rules/(强行规则);但**没有 characterization /
golden master test infrastructure**。Branch 隔离靠 git-worktree skill,有了。

** Best practices 对照

- [-] characterization tests / golden master(没系统;reproduce-before-fix 接近)
- [-] constraint-based prompting(规则有,但没正式 do-not 名单 + hard/soft 分级)
- [ ] diff-only output 强制(没 prompt template;靠 agent 自决)
- [X] git-worktree skill 隔离
- [X] hard "NEVER touch" 在 CLAUDE.md(NEVER commit .env、NEVER use git checkout for analysis 等)

** Action Items

- [ ] 引入 *approval test*(Python: approvaltests;Elisp: 自家 capture-and-compare 用 ert)给关键模块用
- [ ] 写一个 prompt template 库 `prompts/preserve-behavior.md`,带 hard/soft 分级模板,team 共用
- [ ] 给 agent 一个 "diff-only" mode 的 instruction snippet(可以放到 CLAUDE.md 的工作流段)

* Lens #9 — Telemetry / trace for AI-emitted code paths
:PROPERTIES:
:CUSTOM_ID: a-lens-9
:END:

** 现状

**这条已经走得很前面**:Phoenix on :6006 + OTel bridge :7331 + 每个 ai block 自动 trace
(execute-ai-block / cmux-execute / send-text spans)。

** Best practices 对照

- [X] agent observability 部署(Phoenix)
- [-] commit footer 加 Generated-by + prompt hash(*Co-Authored-By* 有,但 prompt hash 没)
- [-] OTel span attributes(已有,但 `agent.model` / `agent.session_id` 等是否齐全要查)
- [-] agent run trace ↔ application runtime trace 关联
- [X] 不存 raw prompt(目前没存)

** Action Items

- [ ] git commit-msg hook:**禁** Co-Authored-By(用户全局规则已要求);改加 `Generated-by: claude-sonnet-4.6` + `Prompt-Ref: <session-uuid>:<block-id>`
- [ ] 验证 Phoenix span attributes 包含 `gen_ai.system`、`gen_ai.request.model`、`gen_ai.usage.*`
- [ ] 在 application runtime(workspace_bridge.py)插 span attribute `agent.session_id` 关联到 ai-block trace

* Lens #10 — Security review: supply-chain, secrets, prompt injection
:PROPERTIES:
:CUSTOM_ID: a-lens-10
:END:

** 现状

**最弱的一条**:CI 没 secret scan、没 OWASP LLM Top 10 review、MCP server 权限没集中审。

** Best practices 对照

- [ ] gitleaks / TruffleHog 扫 git history(从未跑)
- [-] External secrets only(.gitignore 有 .env,但没明文规则禁硬编码 + secret scan in CI)
- [ ] indirect prompt injection 防御(README/ISSUES/PR description 当作 untrusted)
- [-] Tool / Skill / MCP server 加 path-scope(部分 MCP 用了,但没集中审)
- [ ] OWASP LLM Top 10 专项 review(SAST 不够)
- [ ] Injection drill(假设 indirect injection 成功,流程能撑住吗?)

** Action Items(高优)

- [ ] 一次性扫 `gitleaks detect` 完整 history;命中处理:rotate + 记到 tasks/lessons.md
- [ ] CI 加 gitleaks pre-commit + CI 双层(`.pre-commit-config.yaml` + `.github/workflows/ci.yml`)
- [ ] CLAUDE.md 加 **NEVER hardcode secrets** + 列举:API key、DB credential、私有 token、用户名、绝对路径
- [ ] MCP server 审计:列出当前所有 MCP servers,每个写它要的 tool + path scope(类似 AGENTS.md frontmatter `allowed-tools`)
- [ ] 每个 .org module 加一行 *Trust boundary*:此 module 接收的 input 是 trusted / untrusted

* Lens #11 — Memory / lessons-learned discipline
:PROPERTIES:
:CUSTOM_ID: a-lens-11
:END:

** 现状

全局 CLAUDE.md 已要求 "After any user correction, update `tasks/lessons.md`";
**但项目里没这个文件**(只 tasks/ 目录在)。规则形同虚设。

** Best practices 对照

- [ ] tasks/lessons.md 实际存在 + 持续累积
- [-] 双层结构 lessons → CLAUDE.md(规则有,流程没)
- [ ] 每条 rule 标日期 + 触发上下文
- [ ] 规则冲突主动检测(LLM-as-judge periodic)
- [X] 团队级集中(单仓 git 跟踪;CLAUDE.md 进 git)
- [-] prompt self-optimization 工具(没用 DSPy/TextGrad/GEPA)

** Action Items(立即)

- [ ] 创建 `tasks/lessons.md` 骨架(date / mistake / context / rule)+ 把过去几次踩过的坑 backfill
- [ ] CLAUDE.md 加流程规则:"≥3 次同类 lesson 才 promote 到 CLAUDE.md;否则留在 lessons.md"
- [ ] 季度跑一次:把 CLAUDE.md 喂给 LLM 问 "找 contradiction / stale rule";老的删

* Lens #12 — Pair-programming protocol
:PROPERTIES:
:CUSTOM_ID: a-lens-12
:END:

** 现状

你现在是 *L4 Autonomous*(Claude Code)+ 偶尔切到 *L3 Supervised*(plan mode)。
但**项目级 risk → autonomy level 映射表没写**;agent 默认按全局自决,不按 task 类型分。

** Best practices 对照

- [ ] 项目级 risk → autonomy level 映射表写进 CLAUDE.md
- [X] hard approval gate 在 destructive 边界(Claude Code 默认 "Bash command requires permission" 在用)
- [-] verification-first 协议(make check 是 baseline,但不是每条改动都 mandatory truth threshold)
- [-] Conductor 模式(git-worktree skill 在,但平时单 agent 用)
- [-] interrupt 协议(你用 plan mode = 自然 checkpoint)

** Action Items

- [ ] CLAUDE.md 加 *Risk → Autonomy Level* 表:auth/payments/migration → L3 强制 approve;refactor 样板 → L4;探索 → L1-L2
- [ ] 关键模块(code-agent-backend.org / 协议/认证)在 module 顶部 :PROPERTIES: 标 :AUTONOMY_LEVEL: 3
- [ ] 试用 Claude Code subagent + git-worktree 跑 "我同时改 3 个独立 branch" 工作流

* Lens #13 — Token / cost budget management for agents
:PROPERTIES:
:CUSTOM_ID: a-lens-13
:END:

** 现状

Claude Code 自带 budget system 起作用(用过)。**但没有 per-task hard cap、没有团队级 dashboard**。
Phoenix 已采 trace,扩成 cost dashboard 是顺手活。

** Best practices 对照

- [X] Claude Code 的 hard context limit + auto compaction 在用
- [ ] 每 task hard $ cap + token cap(超 route to human)
- [-] Prompt caching(Anthropic 默认开了,但你没显式 sanity-check)
- [ ] 按任务选 model(simple → Haiku;reasoning → Sonnet/Opus;现在默认 Sonnet)
- [-] Reflexion loop max + early-stop(不常用 reflexion,但要时设上限)
- [ ] FinOps 仪表盘(per-task / per-prompt 切片)

** Action Items

- [ ] Phoenix 扩 "cost view":按 trace.session_id group by token spend(workspace_bridge 已采 OTel,只补 dashboard query)
- [ ] CLAUDE.md 加规则:简单分类 / 格式化任务用 Haiku,显式说出来
- [ ] 评估当前是否 prompt cache hit(看 Phoenix 的 input_tokens 是否真在 cache 路径)

* Lens #14 — Multi-agent orchestration & supervisor patterns
:PROPERTIES:
:CUSTOM_ID: a-lens-14
:END:

** 现状

你用 Claude Code 单 agent。**没用 subagent / supervisor pattern**;但 git-worktree skill
已经具备 multi-branch 并行的基础。

** Best practices 对照

- [-] supervisor + workers default(没用 subagent)
- [N/A] swarm 不用(对的)
- [N/A] Reflexion loop max(没用 reflexion)
- [N/A] subagent 不嵌套(没用 subagent)
- [N/A] subagent 最小权限(没 subagent 配置)

** Action Items(可选,看是否需要)

- [ ] 试一个 *researcher subagent*(读多 .org 模块综述出报告)+ *implementer subagent*(改实现)+ supervisor 协调 — 类似 wshobson/agents pattern
- [ ] 如果不上 subagent,这条 lens 跳过 — 你单 agent 工作流已经够用

* Lens #15 — AI-induced concurrency & async correctness
:PROPERTIES:
:CUSTOM_ID: a-lens-15
:END:

** 现状

Elisp 主体单线程(timer + sentinel,你 CLAUDE.md 已强调 lexical-let 闭包捕获);Python 端
workspace_bridge.py 用了 threading + async。**没 race detector / property-based test**。

** Best practices 对照

- [X] 并发代码 review 严肃(literate-programming-document-first.md 强调 process filter 不能 signal error 等)
- [-] 语言原生原语(Python 用 asyncio.Lock / Queue 但需复审 workspace_bridge 全局)
- [ ] 静态分析:`no-floating-promises`(JS 没用)/ Python `pylint --enable=concurrency-check`
- [ ] property-based testing(Hypothesis 没用)
- [-] 跨文件 lock ordering(ARCHITECTURE.org 部分写,没专项)
- [ ] event-loop-aware checklist 在 PR review

** Action Items

- [ ] `python/claude_agent/` 加 `pylint` 的 concurrency 规则,先扫 workspace_bridge.py
- [ ] workspace_bridge 的核心函数加 Hypothesis 测试一组(比如 prompt-routing 那块)
- [ ] CLAUDE.md 加 *Concurrency-aware review* checklist:async 内 sync I/O / unhandled await

* Lens #16 — Framework migration / legacy modernization with AI
:PROPERTIES:
:CUSTOM_ID: a-lens-16
:END:

** 现状

Claude Code 项目本身**正在做** tri-protocol refactor(2026-04-24 完成 Phase 0-4)— 这条
lens 的实践你已经在亲身做。但**没 characterization tests**,主要靠 ert 单测覆盖。

** Best practices 对照

- [X] Strangler fig pattern(tri-protocol 重构就是渐进切,不 big-bang)
- [-] Test-driven modernization(没专门 capture baseline,但 reproduce-before-fix 接近)
- [-] 三阶段 Deep Analysis → Dependency → Conversion(你做过分析,但不是显式 framework 化)
- [ ] Performance/SLA 当 first-class constraint(没硬指标)
- [N/A] Azure Legacy-Modernization-Agents 工具(项目规模没到那)

** Action Items

- [ ] 下次大重构前,先用 approvaltests / 自家 ert capture-and-compare 框架建 baseline
- [ ] 关键模块(code-agent-backend.org)加 :PERF_BUDGET: drawer(P99 / 内存),让重构必过
- [ ] docs/design-docs/ 已经记录设计;补一份 "重构通用 playbook"(参 Anthropic Code Modernization Playbook)

* Lens #17 — Architectural drift detection across files
:PROPERTIES:
:CUSTOM_ID: a-lens-17
:END:

** 现状

*test-structural.el* 已有,是 fitness-function-equivalent(catches mistakes,FIX hint),
覆盖了 "public API 命名"、"NEVER use 某 fn" 等;但**没扫 mutant duplicates / pattern fragmentation**。

** Best practices 对照

- [X] fitness functions 进 CI(test-structural.el)
- [ ] AI-aware drift 工具(sauremilk/drift / fallow-rs/fallow 没用)
- [-] reference patterns + canonical example(CLAUDE.md 部分,没系统化)
- [ ] Drift dashboard(weekly metric)
- [ ] PR-time architectural-impact 自检 prompt

** Action Items

- [ ] 扩 test-structural.el:加一条 "semantic duplicate" 检测(比如:>= 2 个函数体高度相似 → fail)
- [ ] 评估 sauremilk/drift(Python 静态分析器);跑一次 baseline 看产出多少 finding
- [ ] CLAUDE.md 加 reference patterns 段:列 "error 处理 → 看 X.org 的 Y";agent 看到具体路径

* Lens #18 — Measuring real AI productivity / ROI
:PROPERTIES:
:CUSTOM_ID: a-lens-18
:END:

** 现状

**没有 ROI 测量**。Phoenix 有 trace 数据,但没建 productivity dashboard。

** Best practices 对照

- [ ] 自家 mini-RCT(METR 方法)
- [ ] DORA 4 + AI-specific 6 进 dashboard
- [ ] PR size hard cap(参 lens #3)
- [-] Baseline 已有(make check 强制),但 *AI 上来前后对比* 没做
- [ ] AI rejection-reason taxonomy
- [ ] Quarterly retro

** Action Items

- [ ] 季度 retro 模板:cycle time / bug rate / unreviewed merge / AI PR reject reason 五个数,记 tasks/retro-YYYY-Q.md
- [ ] (可选)做一次 mini-RCT:5 个真实 issue 给 AI / 5 个不给,记完成时间
- [ ] PR rejection-reason 分类:"AI 错误地 X" 出现 N 次后 → 升 CLAUDE.md(跟 lens #11 联动)

* Lens #19 — Tribal knowledge mining for agent context
:PROPERTIES:
:CUSTOM_ID: a-lens-19
:END:

** 现状

团队规模小(主要是你)— tribal knowledge 量级跟 Meta 不可比。但 *agent 不知道为什么* 的
现象仍存在(为什么 cmux 用 socket / 为什么 cmuxOnly 默认 / 为什么某 hook 写 lexical-let)。

** Best practices 对照

- [N/A] 复制 Meta 50-agent 蓝图(规模不需要)
- [-] Karpathy LLM Wiki 模式(轻量替代):docs/ 已经接近,但不是 LLM 自动维护
- [ ] Senior interview as service(自我对话录音 + LLM 提炼)— 你就是 senior,值得做
- [ ] Gap-driven mining(让 agent 跑 task 时记录 "我不知道为什么" 的瞬间)
- [-] 自动化挖矿(Slack/会议挖)— 你没团队 channel,但有 git commit message + design docs

** Action Items

- [ ] 启动 "design rationale" backfill:挑 5 个最重要的设计决定(tri-protocol、cmuxOnly、literate-elisp 选型、Phoenix 集成、git-worktree skill 设计),录一段口述,LLM 转 docs/design-docs/ 下
- [ ] 在 CLAUDE.md 加 prompt:"如果你不知道 X 为什么这样,*记录下来* 到 tasks/lessons.md 的 *unknowns* 段而不是猜"
- [ ] 季度看 unknowns 段,选高频项写 design-doc

* 优先级总结
:PROPERTIES:
:CUSTOM_ID: priority-summary
:END:

** 第 1 周 — 高 ROI 低成本(防出血)

- [X] *AGENTS.md 减肥*(736 → ≤60 行 或直接删,内容拆 docs/)— **头号**(lens #5)
- [X] *gitleaks 扫完整 git history* — 命中 → rotate(lens #10)
- [X] *gitleaks pre-commit + CI hook* 加进去(lens #10)
- [X] *PR size cap*:.github/workflows/ci.yml 拒 > 400 LOC PR(lens #3)
- [X] *创建 tasks/lessons.md 骨架* + 把 4-5 条已踩过的坑 backfill(lens #11)
- [X] *CLAUDE.md 加 NEVER hardcode secrets 段* 列具体禁项(lens #10)

** 第 1 月 — 装机制

- [X] *Risk → Autonomy Level 表* 写进 CLAUDE.md(lens #12)
- [-] *vulture* + Python `mutmut` 试点扫一个文件(lens #4、#6)
- [ ] *Phoenix cost view* 扩展 — 按 session_id group token spend(lens #13)
- [X] *CI supply-chain scanner*:`safety check` + `pip-audit`(lens #2)
- [X] *MCP server 审计*:列出当前 MCP servers 各自权限(lens #10)
- [X] *commit-msg hook*:加 `Generated-by:` + `Prompt-Ref:` 替代 Co-Authored-By(lens #9)
- [X] *扩 test-structural.el*:semantic duplicate 检测(F44 已加,当前 codebase 0 命中)(lens #17)

** 第 1 季度 — 战略

- [X] *NL Outline* 在主要 .org 模块大函数体内加(strict-LP cross-ref)
- [X] *characterization / approval test* 框架引入,关键模块覆盖(lens #8、#16)
- [X] *property-based testing*(Hypothesis)给 workspace_bridge 加(lens #15)
- [X] *design rationale backfill*(口述 → docs/design-docs)5 篇(lens #19)
- [X] *quarterly retro 模板*(lens #18)
- [X] *litprog-skill PostToolUse hook* 在 ~/projects/literate-org 原型(strict-LP cross-ref)

** 跳过 / 不适用

- 50-agent Meta swarm(规模不到)
- big-bang framework migration(不在做)
- swarm orchestration(supervisor 默认就够)
- vector RAG for code(grep + Glob 已最优)

* 元信息

- 来源 19 lens 文件:[[file:~/projects/dummy/notes/ai-codebase-mastery.org][ai-codebase-mastery.org]]
- 本文件位置:`~/projects/emacs-agent/tasks/ai-codebase-mastery-action-plan.org`
- 生成日期:2026-05-01
- 后续:逐项 check 完一项就 [X] 一下;季度回顾这份计划。
