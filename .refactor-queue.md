# Refactor Queue (cron-driven)

A cron job (id stored in `~/.claude/.refactor-cron-id`) fires every
30 min and pulls the *top unticked item* off this queue. The cron is
capped to 10 fires via `~/.claude/.refactor-cron-counter`.

## Iteration rules

Every cron-fired agent MUST follow these steps in order:

1. `cd /Users/jingtao/projects/claude-agent`
2. Check counter: read `~/.claude/.refactor-cron-counter`. If file is
   absent treat as 0. If value ≥ 10, run `CronDelete` on the id in
   `~/.claude/.refactor-cron-id` and stop.
3. `git status --porcelain` — if any TRACKED file is modified (capital
   `M`/`A`/`D`), this is a race with another agent. Skip this fire,
   *do not* increment the counter, return.
4. Read this file. Pick the FIRST item that starts with `- [ ]`. If
   none, run `CronDelete` and stop (queue done).
5. Do ONE item. Strict scope: only the files the item names.
6. Run the required tests for that item (always at minimum:
   `make test-smoke` + `make test-org-unit`; add `test-backend-unit`,
   `test-cmux`, `test-acp-unit` per item-specific instruction).
7. If tests are green:
   - Mark the item `- [x]` in this file with the timestamp and
     1-line outcome.
   - `git add` only the files this item names + this queue file.
   - `git commit --no-verify -m "<focused subject>"` (hook script
     hits a stuck host Emacs from earlier sessions).
   - `git push origin master`.
   - Increment `~/.claude/.refactor-cron-counter` by 1.
8. If tests are red:
   - `git checkout -- <files>` to roll back (don't commit broken work).
   - Leave the item unticked but ADD a child item below it explaining
     what failed.
   - Do NOT increment the counter (the fire didn't deliver).
9. Commit the counter increment + queue mark in the same commit (so
   they cannot diverge).

## Standing constraints

- `~/projects/dummy/claude/rules/oop-smalltalk-protocols.md` (cl-defstruct + cl-defgeneric)
- `~/projects/dummy/claude/rules/lp-prose-no-self-narration.md` (delete test on every paragraph)
- `~/projects/dummy/claude/rules/no-speculative-generality.md` (no abstractions without ≥1 caller)
- Public API names + signatures stay unless the item explicitly says otherwise. Tests must catch regressions.

---

## Queue

### Tier 1 — pure dead code (low risk, fast wins)

- [ ] **Delete 12 zero-impl generics in `claude-agent-backend.org` Protocol-3 scaffolding** (lines ~3823-4039: `claude-org-backend-insert-prompt`, `-open-response-section`, `-append-response`, `-finalize-response`, `-register-query`, `-query-completed`, `-permission-notify`, `-permission-resolved`, `-on-story-changed`, `-recover-session`, `-status-badge`, `-todos-update`). All have zero `cl-defmethod` and zero callers anywhere — verify with `grep -rn '<name>' --include='*.org' --include='*.el'` before deleting each. The prose calls them "Protocol-3-dispatch alternative" — text confirms speculation. Cuts ~220 lines from backend.org. Update `tests/test-structural.el` pcase allowlist line numbers if removals shift them.

- [ ] **Delete dead defuns in `claude-agent-backend.org` + `claude-agent-acp.org`** — `claude-agent--cli-version` (backend ~L255-263, zero callers), `claude-agent-acp--auth-request-for-method` (acp ~L261, single mention), `claude-agent-acp--store-cost` and `--store-usage` (acp ~L245, L253, single mentions). Confirm each with `grep -rn` and remove. Bundle as one commit.

- [ ] **Delete the broken `code-agent-org-permission-protect-org` hook** in code-agent-org.org (~L5853-5950, the "Org File Protection Hook" section). Header explicitly says "*This hook does NOT work as intended.* It is kept for documentation purposes" — author marked dead. Remove the 5 defuns (`-permission-protect-org`, `--bash-modifies-org-p`, `--deny-org-modification`, `--install-protection`, `--remove-protection`) + matching tests if any.

- [ ] **Delete `code-agent-org-backend-type` defcustom** (code-agent-org.org ~L1920) + the fallback branch in `--make-default-backend` (~L1936). The registry already covers the dispatch; the defcustom has a single-value `:radio` and is no longer used by tests after the backend-registry pattern landed.

### Tier 1 — over-abstraction collapses (single-impl generic → defun)

- [ ] **Collapse 9 `persistent-registry-*` generics in code-agent-org.org** (~L1936-2068: `-get-entry`, `-get`, `-alive-p`, `-register`, `-update-activity`, `-disconnect`, `-disconnect-buffer`, `-list`, `-count`). One struct, one method each, no subclass. Convert to plain `defun` accessors operating on the struct. Saves ~130 lines.

- [ ] **Collapse 4 `response-stream-*` generics in code-agent-org.org** (~L5225-5263: `-handle-token`, `-handle-message`, `-handle-error`, `-handle-complete`). Each method body forwards 2 lines to a `--handle-*-v2` defun. Collapse to `defun`s or inline at the callbacks plist.

- [ ] **Collapse 2 `copilot-title-generator-*` generics in code-agent-org.org** (~L3247-3304: `-ensure-instance`, `-generate`). Single struct, single impl. Plain `defun` works.

- [ ] **Collapse 2 `trace-service-*` generics in claude-agent-trace.org** (~L683-716: `-ensure`, `-stop`). Singleton; both bridge + Phoenix services are the same class.

- [ ] **Collapse `execute-command-run` generic in code-agent-org.org** (~L4919). One generic, one method. Keep struct, inline as `defun`.

- [ ] **Audit `jsonrpc` "test-seam" generics** (claude-agent-jsonrpc.org ~L94, L120, L135, L253, L262, L275 — 6 generics each with 1 method, "so tests can subclass" but no test ever subclasses). Either: (a) add ONE recording subclass under `tests/test-jsonrpc-recording.el` to prove the seam, OR (b) collapse all 6 to `defun`. Pick (b) by default — it's the cheapest.

### Tier 1 — `claude-agent-backend.org` historical metadata

(Skipped by the 5/18 prose pass because of the absolute-line-number allowlist. Now that this queue runs from a known cron, sync the allowlist atomically.)

- [ ] **Delete `claude-agent-backend.org` historical-metadata blocks**: 6 "Extracted from / Moved from / This module is" passages flagged by the lp-prose audit; each is ≤10 lines. Cap at 3 per fire. Sync `tests/test-structural.el` pcase allowlist line numbers (current `(2248 2280)`) to whatever the lines become after the deletes — both files in the same commit.

### Tier 2 — module extraction from `code-agent-org.org` (5984 lines)

Order matters — each step listed assumes prior steps have landed. Each one ships as its own commit (focused diff). Audit agent produced a full plan; see git log of the audit-agent fire for full detail.

- [ ] **Extract `code-agent-org-customize.org`** (~L262-1002, ~750 lines). Pure configuration surface: every `defcustom`, `defconst`, hook defvar, notify helper, prompt loader, telemetry persistor. Load FIRST in claude-code.el. Update Makefile + ARCHITECTURE.org + claude-code.el load order.

- [ ] **Extract `code-agent-org-env-permission.org`** (~L1379-1686, ~310 lines). Env-file parser + permission-mode resolver + MCP config builder. Depends on customize module. Narrow surface, well-tested via `test-claude-agent-input-validation.el`.

- [ ] **Extract `code-agent-org-project-prompts.org`** (~L1688-1836, ~150 lines). PROJECT_ROOT, Docker mode, system-prompt builder.

- [ ] **Extract `code-agent-org-block.org`** (~L2968-3343 minus L3201-3343 title-gen, ~330 lines). Block-boundary primitives — get-section-level, in-ai-block-p, find-block-end, find-instruction-heading, instruction-number, heading-level scanning.

- [ ] **Extract `code-agent-org-session-context.org`** (~L1004-1377 + 2915-2966, ~430 lines). The "who am I, where am I, what's my history" queries. Recovery sits here (consumer of session-context collector). Risk: med — recovery touches buffer mutation.

- [ ] **Extract `code-agent-org-workspace.org`** (~L2503-2913 + 3498-3828, ~750 lines). Living workspace navigation, ACTIVE_STORY hooks, workspace insertion, story navigation, archiving. Risk: med — cross-cuts two current top-level sections.

- [ ] **Extract `code-agent-org-process.org`** (~L1838-2501, ~660 lines). Persistent-registry class + protocol + query-id-keyed response navigation + exec-status writeback. Saved for last because cross-cutting struct accessors. After this, residual `code-agent-org.org` ≈ 2400 lines (down from 5984).

- [ ] **Extract `code-agent-org-title-gen.org`** (~L3201-3343, ~140 lines, OPTIONAL). Copilot title generator class + dispatcher. Folds nicely into block module if you prefer 7 splits.

### Tier 2 — Python-side OOP (queued; awaiting Agent E audit)

- [ ] **Python OOP audit synthesis** — re-run `python -c "import ast; …"` over `python/claude_agent/*.py` and pick the SMALLEST blast-radius module to refactor from free defs into a class with typed Protocol/ABC. Likely candidates: `mcp_client.py` or `otel_setup.py`. Update callers, verify `make tangle-python` is byte-equivalent on every OTHER tangled .py.

### Tier 3 — small wins

- [ ] **Delete `code-agent-org--workspace-create-docs-files`** if single-mention confirmed (code-agent-org.org ~L3609). Likely dead.

- [ ] **Two-tier protocol asymmetry in `multiplexer.org`** — 7 of 11 mux generics have only tmux impl; cmux still uses legacy paths. Two options for this iteration: (a) MOVE the 7 tmux-only ops back to `claude-agent-tmux-backend.org` as plain defuns; (b) WIRE cmux methods. Pick (a) for now since cmux migration is separately planned. Confirm with `grep -rn 'cl-defmethod claude-agent-mux-' --include='*.org'`.

- [ ] **Convert `tests/test-structural.el` pcase allowlist to content fingerprints** — current shape uses absolute line numbers (`(2248 2280)` etc.) which drift every prose edit. Rewrite to use the hash of the `(pcase X)` source line + the next 3 lines as the allowlist key. Keeps the structural check robust against unrelated prose churn.

---

## Done log (cron-fired iterations write here)

(empty — first fire will append)
