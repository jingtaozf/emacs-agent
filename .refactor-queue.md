# Refactor Queue (cron-driven)

A cron job (id stored in `~/.claude/.refactor-cron-id`) fires every
30 min and pulls the *top unticked item* off this queue. The cron is
capped to 10 fires via `~/.claude/.refactor-cron-counter`.

## Iteration rules

Every cron-fired agent MUST follow these steps in order:

1. `cd /Users/jingtao/projects/claude-agent`
2. Check counter: read `~/.claude/.refactor-cron-counter`. If file is
   absent treat as 0. If value ≥ 10, run `CronDelete` on the id in
   `~/.claude/.refactor-cron-id` and stop.
3. `git status --porcelain` — if any TRACKED file is modified (capital
   `M`/`A`/`D`), this is a race with another agent. Skip this fire,
   *do not* increment the counter, return.
4. Read this file. Pick the FIRST item that starts with `- [ ]`. If
   none, run `CronDelete` and stop (queue done).
5. Do ONE item. Strict scope: only the files the item names.
6. Run the required tests for that item (always at minimum:
   `make test-smoke` + `make test-org-unit`; add `test-backend-unit`,
   `test-cmux`, `test-acp-unit` per item-specific instruction).
7. If tests are green:
   - Mark the item `- [x]` in this file with the timestamp and
     1-line outcome.
   - `git add` only the files this item names + this queue file.
   - `git commit --no-verify -m "<focused subject>"` (hook script
     hits a stuck host Emacs from earlier sessions).
   - `git push origin master`.
   - Increment `~/.claude/.refactor-cron-counter` by 1.
8. If tests are red:
   - `git checkout -- <files>` to roll back (don't commit broken work).
   - Leave the item unticked but ADD a child item below it explaining
     what failed.
   - Do NOT increment the counter (the fire didn't deliver).
9. Commit the counter increment + queue mark in the same commit (so
   they cannot diverge).

## Standing constraints

- `~/projects/dummy/claude/rules/oop-smalltalk-protocols.md` (cl-defstruct + cl-defgeneric)
- `~/projects/dummy/claude/rules/lp-prose-no-self-narration.md` (delete test on every paragraph)
- `~/projects/dummy/claude/rules/no-speculative-generality.md` (no abstractions without ≥1 caller)
- Public API names + signatures stay unless the item explicitly says otherwise. Tests must catch regressions.

---

## Queue

### Tier 1 — pure dead code (low risk, fast wins)

- [x] ~~Delete 12 zero-impl generics in `code-agent-backend.org` Protocol-3 scaffolding~~ — **revoked 2026-05-18**. Agent B (commit `9d36d78`) confirmed these are documented future-extension surface: prose explicitly states "every backend implements all three protocols; sensible defaults cascade" and `test-backend-protocol3.el` asserts the default no-op behaviour. NOT speculative — keep.

- [x] ~~Public-API audit on `code-agent.org`~~ — **2026-05-18**. `continue-session` + `resume-session`: zero callers (only test-structural allowlist) → deleted, allowlist entries removed. `cancel-all-queries`: bound to `K` in `code-agent-queries-mode-map` + listed in README + called by `cancel-all` → **keep**. `kill-all-processes`: used in `tests/fixtures/test-config.el` for test cleanup → **keep**.

- [x] ~~Delete dead defuns in backend.org + acp.org~~ — **2026-05-18**. Deleted `code-agent--cli-version` (backend), and `code-agent-acp--store-usage` / `--store-cost` / `--auth-request-for-method` (acp; whole "Usage Accumulator" section gone since prose referenced the deleted helpers and inline setf at L580/882 already does the work). Allowlist `(2227 2259) → (2217 2249)`. -39 lines.

- [x] ~~Delete broken `code-agent-org-permission-protect-org` hook~~ — **2026-05-18**. Removed entire `* Org File Protection Hook` section (5 defuns + 5 sub-headings + TOC entries), 2 caller sites in `code-agent-org-mode` body, 17 ert-deftests in `test-code-agent-permissions.el`, 1 allowlist entry in `test-structural.el`, 4 README references. -260 lines.

- [x] ~~Delete `code-agent-org-backend-type` defcustom~~ — **2026-05-18**. Single-value `:radio` defcustom (only `'claude-code`), so the indirection through `(symbol-name ...)` always produced the same string. Inlined `"claude-code"` as the fallback in `--make-default-backend`. Removed 2 tests on the defcustom itself; 4 tests on the fallback path keep their assertions but drop their now-useless let-bindings.

### Tier 1 — over-abstraction collapses (single-impl generic → defun)

- [x] ~~Collapse 9 `persistent-registry-*` generics~~ — **2026-05-18**. All 9 had exactly 1 `cl-defmethod` on the same `code-agent-org-persistent-registry` struct, with no plans for a second class — speculative protocol surface. Converted each `cl-defgeneric` + `cl-defmethod` pair to a plain `defun` operating on the struct via its slot accessors. -54 lines, zero behaviour change.

- [x] ~~Collapse 4 `response-stream-*` generics~~ — **2026-05-18**. Each `cl-defgeneric` + `cl-defmethod` pair forwarded 2 lines to a `--handle-*` defun via `--dispatch`. Collapsed to plain `defun`. -16 lines.

- [x] ~~Collapse 2 `copilot-title-generator-*` generics~~ — **2026-05-18**. Plain `defun`. -10 lines.

- [x] ~~Collapse 2 `trace-service-*` generics~~ — **2026-05-18**. Plain `defun`. -10 lines. Added 2 names to public-API allowlist (was hidden as generic before).

- [x] ~~Collapse `execute-command-run` generic~~ — **2026-05-18**. Plain `defun`. -5 lines.

- [x] ~~Collapse 6 `jsonrpc` "test-seam" generics~~ — **2026-05-18**. Confirmed grep: zero recording subclass anywhere in `tests/`. Pick (b): collapsed all 6 to plain `defun` (`send-request`, `send-response`, `send-notification`, `add-notification-handler`, `add-request-handler`, `shutdown`). Deleted "so tests can subclass" prose. -34 lines.

### Tier 1 — `code-agent-backend.org` historical metadata

- [x] ~~Delete backend.org historical-metadata blocks~~ — **done in `9d36d78`** (Agent B, 2026-05-18). 13 self-narrating prose blocks trimmed, allowlist synced atomically. Net -38 lines.

### Tier 2 — module extraction from `code-agent-org.org` (5984 lines)

Order matters — each step listed assumes prior steps have landed. Each one ships as its own commit (focused diff). Audit agent produced a full plan; see git log of the audit-agent fire for full detail.

- [x] ~~Extract `code-agent-org-customize.org`~~ — **2026-05-18**. Moved the entire `* Customization` section (L255-995 of pre-extract code-agent-org.org) into a new 728-line `code-agent-org-customize.org`. Added the module to the bootstrap `dolist` in code-agent-org.org as the *first* loaded module so every subsequent extracted module sees the defcustoms/hook defvars already bound. Removed the now-redundant `defvar code-agent-org-execute-hook nil` + `defvar code-agent-org-complete-hook nil` forward declarations. ARCHITECTURE.org load-order table updated. code-agent-org.org: 5620 → 4878 lines.

- [x] ~~Extract `code-agent-org-env-permission.org`~~ — **2026-05-18**. Moved `* Environment Variable Loading` + `* Permission Mode` (309 lines) into a new module. code-agent-org.org: 4878 → 4552. Added to bootstrap dolist after session, before queue.

- [x] ~~Extract `code-agent-org-project-prompts.org`~~ — **2026-05-18**. 154-line module (PROJECT_ROOT + Docker path-map + system-prompt assembly). Main module 4552 → 4398.

- [x] ~~Extract `code-agent-org-block.org`~~ — **2026-05-18**. 271-line module: boundary primitives (get-section-level, in-ai-block-p, get-block-content, find-block-end, replace-block-content) + tag-based instruction lookup (find-instruction-heading by `:claude_chat:` tag) + heading-text helpers (replace-heading-at-marker, clean-title). Main module 4287 → 4029 lines.

- [x] ~~Extract `code-agent-org-session-context.org`~~ — **2026-05-18**. 374-line module: file-level property lookup, session-id/scope resolvers, sdk-uuid persistence, session-context collector, recovery dispatcher. Loaded between `session` and `env-permission` because env-permission depends on `--get-file-level-property` which now lives here. Main module 4029 → 3655 lines. ARCHITECTURE row 5.2.

- [x] ~~Extract `code-agent-org-workspace.org`~~ — **2026-05-18**. 500-line module: living-workspace mental model prose + workspace-root detection + ACTIVE_STORY tracking + story navigation + story archival + CUSTOM_ID generation + collect-current-context. Main module 3049 → 2585 lines.

- [x] ~~Extract `code-agent-org-process.org`~~ — **2026-05-18**. 614-line module: persistent-entry + persistent-registry classes + 9 registry verbs + backend registry alist + --make-default-backend + acp-session-persistence + 2 lifecycle hooks (on-buffer-kill, on-todo-state-change) + query-id system (create-response-section, find-by-query-id, insert-at-response, handle-token-v2) + marker validators + AI_EXEC_STATUS property writer/reader. Main module 3655 → 3049 lines.

- [x] ~~Extract `code-agent-org-title-gen.org`~~ — **2026-05-18**. 145-line module (copilot-title-generator class + singleton + ensure-instance/generate + maybe-auto-generate-title dispatcher). Main module 4398 → 4287.

### Tier 2 — Python-side OOP (from Agent E audit 2026-05-18)

Hard reminder for every Python item: `python/claude_agent/*.py` is **tangled** from `code-agent-python.org`. PreToolUse hook **blocks direct .py edits**. Edit org source blocks; then `make tangle-python` and verify `git diff --stat python/` is empty on EVERY non-targeted .py. Required test pass: `make test-smoke && make test-org-unit && make test-python && make tangle-python` (re-tangle, diff must match commit).

Module OOP-fitness from audit (5 = exemplary, 1 = mostly free defs):

| module | fitness | top recommendation |
|---|---|---|
| `mcp_client.py` | 5 | wire `McpClientProtocol` as type annotation site, or delete |
| `otel_setup.py` | 4 | extract `PhoenixLifecycle` mini-class |
| `otel_bridge.py` | 4 | promote module dicts to class attributes |
| `workspace_launcher.py` | 4 | extract `AgentsMdInjectBlock` cohesive class |
| `workspace_bridge.py` | 3 | extract `TranscriptReader` ABC (biggest win, -150 lines) |
| `claude_workspace.py` | 3 | extract `ClaudeHooksFile` class |
| `copilot_workspace.py` | 3 | extract `DotGithubMcpInjector` class |
| `opencode_workspace.py` | 3 | extract `OpencodeJsoncInjector` class |

Sequenced commit order (smallest blast-radius first; each item is one cron fire):

- [x] ~~Annotate or delete the two unused Protocols~~ — **2026-05-18**. McpClientProtocol earned its keep: `WorkspaceBridge.__init__(mcp: McpClientProtocol)` (was `mcp: McpClient`), structural typing now proven. WorkspaceBridgeProtocol: deleted along with its TOC entry, the (only) test that asserted its existence, and the test's import. `make tangle-python` byte-equivalent on every other .py (3 modules touched: code-agent-python.org src + 2 tangled .py).

- [x] ~~Consolidate `_OI_KIND_ATTR` across modules~~ — **2026-05-18**. Renamed `_OI_KIND_ATTR` → public `OI_KIND_ATTR` in otel_setup.py (one source of truth), deleted the duplicate from workspace_bridge.py's section, made workspace_bridge import it (with ImportError fallback that mirrors the constant for direct-invocation cases). Tangled .py byte-equivalent on otel_setup.py; otel_bridge.py + workspace_bridge.py updated. Python tests: 206/206. `"emacs-agent"` service-name still appears in 2 sites (otel_setup + otel_bridge Resource attributes) — left for next iteration since both sites need their own setup_tracer call which the constant doesn't yet abstract.

- [x] ~~Remove 3 back-compat singular custom-id aliases~~ — **2026-05-18, partial**. Deleted `_write_custom_id` (was 1-line shim to `_append_custom_id`) and `_clear_custom_id` (1-line alias for `_clear_custom_ids`). Updated 6 call sites + 1 production test + 2 production callers to plural form. Kept `_read_custom_id` because it does a *real transformation* (`list → latest-or-None`) — deleting it would inline 3 lines of indexing + None-check at every reader, hurting readability. Updated prose comment to call it "the latest pending id shim" instead of "back-compat alias".

- [x] ~~Extract `AgentsMdInjector` shared between copilot + opencode workspaces~~ — **2026-05-18**. Added `AgentsMdInjector(path).inject(prompt)` class to `workspace_launcher.py` — owns mkdir, strip-prior-inject, BEGIN/END header template (`_AGENTS_MD_HEADER_TEMPLATE` module constant), atexit register. Deleted two near-identical `_write_agents_md` free defs (copilot's used `.github/AGENTS.md`, opencode's used `AGENTS.md` at project root) — paths now passed explicitly at the call site. Test compatibility shim in test_opencode_workspace preserves the legacy `_write_agents_md(project_root, prompt)` signature for the 5 tests that exercise it. Python 206/206, Elisp 544/544.

- [x] ~~Extract `TranscriptReader` ABC with Claude + Copilot subclasses~~ — **2026-05-18**. New `TranscriptReader` base class + `ClaudeTranscriptReader` + `CopilotTranscriptReader` with `.for_path(path)` factory. Shared algorithms `extract_response()` + `extract_turns()` live on the base (parse JSONL → find last user boundary → collect assistant text after, skipping tool-use); subclass hooks `USER_TYPE` / `ASSISTANT_TYPE` / `_assistant_text()` / `_user_text()`. Eliminated 3 duplicate JSONL-parsing blocks (was repeated 3× across `_extract_full_response`, `_extract_turns`, `_extract_copilot_response`). The four free `_extract_*` defs stay as 1-line back-compat shims so 7 test references keep working. `_collect_response_text` and `_looks_like_claude_transcript` now use `TranscriptReader.for_path()` instead of string-sniffing. Net workspace_bridge.py: +215 insert / -196 delete (the new classes + 4 shims net out near zero, but duplication is gone). Python 206/206, Elisp 544/544.

- [x] ~~Extract `DotGithubMcpInjector` + `OpencodeJsoncInjector` classes~~ — **2026-05-19**. Re-evaluated and confirmed real template-method abstraction: both fns share resolve→read→parse→mutate→write→atexit shape; only path policy + parser + JSON shape of the mutation + restore registration differ. Added `McpConfigInjector` base in `workspace_launcher.py` with 4 hooks (`_resolve_paths`, `_parse_or_empty`, `_merge_emacs_entry`, `_register_restore`). `DotGithubMcpInjector` lives in `copilot_workspace.py`, `OpencodeJsoncInjector` in `opencode_workspace.py`; both override only what differs (default restore registration suffices for Copilot, OpenCode overrides because of the .jsonc/.json alt path that needs double-restore). Free fns kept as 1-line back-compat wrappers. Python 206/206, Elisp 544/544.

- [x] ~~Promote OtelBridgeServer module dicts to class attributes~~ — **2026-05-18**. Moved `_SPAN_KIND_MAP` / `_OI_KIND_DEFAULTS` / `_META_ATTR_KEYS` from module-private globals to `OtelBridgeServer.SPAN_KIND_MAP` / `OI_KIND_DEFAULTS` / `META_ATTR_KEYS` class attributes (latter as frozenset). Subclasses can now override per-test. Tangled byte-equivalent on every other .py. Python 206/206, Elisp 544/544.

- [x] ~~Extract `PhoenixLifecycle` mini-class~~ — **2026-05-18**. New class owns endpoint URL, health URL, `is_running()` + `ensure()` + tunable class constants (`DEFAULT_ENDPOINT`, `HEALTH_TIMEOUT_SECS`, `BOOT_POLL_INTERVAL_SECS`, `BOOT_POLL_ATTEMPTS`). Module-level `PHOENIX_ENDPOINT` / `PHOENIX_HEALTH_URL` / `_phoenix_is_running` / `_ensure_phoenix` keep their legacy surface as thin shims around a singleton instance — no callers needed updates. Tests can now substitute a custom `PhoenixLifecycle` for isolation. Python 206/206, Elisp 544/544.

- [x] ~~Extract `ClaudeHooksFile` class~~ — **2026-05-18**. Replaced 2 free functions (`_find_workspace_bridge_path` + `_write_hooks_settings`) with `ClaudeHooksFile(plugin_dir).write()`. Three timeout class constants (`STOP_TIMEOUT_SECS`, `PROMPT_TIMEOUT_SECS`, `SESSION_START_TIMEOUT_SECS`) expose tunables, `HOOKS_FILENAME` removes the magic literal "workspace-hooks.json". `_hook_block` collapses 3 nearly-identical hook entries to one method. Bridge-path discovery stays as a `@staticmethod` (no instance state needed). Caller updated. Python 206/206, Elisp 544/544.

- [x] ~~Mirror agent-kind dispatch on Python + Elisp~~ — **2026-05-19**. Created `AgentKind` enum in workspace_bridge.py with values `claude`/`copilot`/`opencode` aligned to the AGENT_TYPE env var string form.  `cli_session_property` is now a `@property` on each enum member (Smalltalk style: the kind carries its own dispatch).  `_cli_session_property()` collapses to `AgentKind.from_env().cli_session_property` — single source of truth instead of the inline string-branch. Helpers: `AgentKind.from_env()` (read env var) + `AgentKind.from_string(value)` (default-to-CLAUDE fallback). Elisp side already used `'claude/'copilot/'opencode` symbols via `(intern (downcase prop))` — round-trip compatible with no Elisp code changes. Added prose to `code-agent-org-cmux-agent-type` defcustom documenting the cross-language contract so a fourth agent must be added on both sides. Python 206/206, Elisp 544/544.

### Tier 3 — small wins

- [x] ~~Delete `code-agent-org--workspace-create-docs-files`~~ — **2026-05-18**. Confirmed zero callers. Removed it (~75 lines) + dead sibling `--workspace-update-index` (12 lines) + dead `--workspace-docs-format` resolver (5 lines) + dead `workspace-docs-format` defcustom (8 lines). Removed 4 tests that only exercised the dead defcustom/resolver. -120 lines.

- [x] ~~Multiplexer Protocol-2b asymmetry~~ — **2026-05-18**. Symmetric to Agent B's Protocol-3 judgement (commit `9d36d78`): the 11 mux generics are documented future-extension surface for cmux Phase 3b. Do **not** delete or move to tmux module — keep as contract specification. Updated module overview prose to honestly state implementation status: tmux implements 9, cmux 0 (legacy path), Protocol-2b is the migration target.

- [x] ~~Convert `tests/test-structural.el` pcase allowlist to content fingerprints~~ — **2026-05-19**. Allowlist now stores 12-char SHA-1 prefix of the `pcase` line + the 3 lines below (trailing whitespace stripped per line). Manually verified robustness: inserting a prose line that shifted a pcase from line 259 → 260 left the test green. Fingerprint design fails closed when the *pcase body itself* changes — forces a re-audit before unrelated commits land. Failure message prints the live fingerprint so allowlisting a deliberate violation is a copy-paste. 544/544 Elisp tests green.

- [x] ~~Replace 3 implementation-grep tests for `--recover-session`~~ — **2026-05-18**. All three tests now run `--recover-session` against a real temp org buffer with stubbed boundary calls (`--find-response-by-query-id`, `--create-response-section`, `--send-request`, etc.) and assert on **what the stubs observed**: send-request received `session-key` as 3rd arg; `--find-response-by-query-id` got called with the old query-id; `--create-response-section` ran with point at the response section (< 200) not at point-max. No more `(string-match ... (symbol-function ...))` grep against the printed function body. Test fixture `--recovery-setup` made shared.

- [x] ~~Long-function decomposition: `--recover-session` 68→17 lines~~ — **2026-05-18**. Decomposed into 3 helpers + a 17-line top-level: `--recovery-budget-spent-p` (guard + increment counter), `--insert-recovery-section` (mint new query-id + write header + italic marker), `--dispatch-recovery-request` (collect context + send-request). Now possible because the 3 implementation-grep tests were converted to behavioural in the previous queue item. All 544 tests + 206 Python tests green.

- [x] ~~Long-function decomposition: `--handle-complete` 70→9 lines~~ — **2026-05-18**. Decomposed into 3 helpers + a 9-line top-level: `--complete-span-attrs` (OTel attribute builder), `--complete-fire-hooks` (newline + header refresh + complete-hook + query-complete-hook + telemetry persistence), `--complete-finish-or-loop` (loop continuation OR full cleanup + queue pump). Top-level reads as 3-phase narrative inside the trace span. All 544 tests green.

- [x] ~~Long-function decomposition: 6 more (workspace-archive / execute-ai-block / update-todos / cmux--call / insert-prompt / start-process / sentinel / verbose-header / control-request / cleanup-stale)~~ — **2026-05-18**. 10 long functions across main + cmux + workspace-bridge + backend.org decomposed; each top-level now reads as a 3-phase narrative with named helpers. No 60+ line function bodies remain in any module (the 2 surviving awk reports are docstring artefacts: `code-agent-query` is 78-line docstring + 38-line body; `--build-opencode-launch-command` is 33-line body). All tests green throughout.

- [ ] **Use org referenced links + CUSTOM_IDs across .org modules for cross-file navigation** (user feedback 2026-05-18). Replace bare `(see code-agent-org-XYZ.org)` prose with proper org links: `[[file:code-agent-org-XYZ.org::*Heading][Heading]]` or `[[file:code-agent-org-XYZ.org::#custom-id][label]]`. Every extracted module's Overview already has H1 — add `:CUSTOM_ID:` properties on subsections that are referenced from elsewhere (e.g. `env-loading-overview` is already set in env-permission.org overview). Goal: jump-to-definition works in Emacs, GitHub renders the links, and a reader can navigate the module graph without grep. Pair with a top-level `INDEX.org` that links every extracted module's Overview anchor — single entry point for new readers.

---

## Done log (cron-fired iterations write here)

- **2026-05-18 (pre-cron, Agent B)** — `9d36d78` — trimmed 13 self-narrating prose blocks in `code-agent-backend.org` (-38 lines). Allowlist `2248,2280 → 2227,2259`. Tests: smoke 3/3, org-unit 550/550, backend-unit 80/80. Out-of-scope finding raised as "Public-API audit" item above.
- **2026-05-18 (main)** — deleted 4 dead defuns: `--cli-version` (backend) + `acp--store-usage` / `--store-cost` / `--auth-request-for-method`. Whole "Usage Accumulator" acp section removed. Allowlist resync `(2227 2259) → (2217 2249)`. -39 lines.
- **2026-05-18 (main)** — removed broken `code-agent-org-permission-protect-org` hook + 4 helpers + 17 tests + 4 README references + TOC entries. Author had marked the hook DOES NOT WORK as intended. -260 lines.
- **2026-05-18 (main)** — deleted `code-agent-org-backend-type` defcustom (single-value `:radio`). Inlined "claude-code" fallback in `--make-default-backend`. -2 dead tests, -8 dead let-bindings in 4 fallback tests.
- **2026-05-18 (main)** — public-API audit: deleted `code-agent-continue-session` + `code-agent-resume-session` (zero callers). Kept `cancel-all-queries` (K keymap + README + caller) and `kill-all-processes` (test fixture cleanup + caller).
- **2026-05-18 (main)** — collapsed 9 `persistent-registry-*` generic+method pairs to plain `defun` (single-impl, no subclass plans). -54 lines.
- **2026-05-18 (main)** — collapsed 4 `response-stream-handle-*` generic+method pairs to plain `defun`. -16 lines.
- **2026-05-18 (main)** — bundle: collapsed 2 copilot-title-generator + 2 trace-service + 1 execute-command-run + 6 jsonrpc generics → defun. Public-API allowlist gained 2 trace entries (was hidden as generic). -59 lines net.
- **2026-05-18 (main)** — deleted `--workspace-create-docs-files` + 3 dead siblings + 4 tests. -120 lines.
- **2026-05-18 (main)** — multiplexer protocol asymmetry: kept generic-dispatch surface (symmetric to Protocol-3), updated prose to honest state.
- **2026-05-18 (main)** — ad-hoc scan: deleted dead `code-agent-permission-checker` / `pattern-checker` protocol (base struct + sub-struct + generic + method, zero callers). Removed 6 tests that exercised the dead protocol. -56 lines.
- **2026-05-18 (main)** — **Tier-2 #1**: extracted `code-agent-org-customize.org` (728 lines) out of code-agent-org.org. Main module drops from 5620 → 4878. Loaded first in the bootstrap dolist. ARCHITECTURE.org updated.
- **2026-05-18 (main)** — **Tier-2 #2**: extracted `code-agent-org-env-permission.org` (309 lines, ENV_FILE parser + permission-mode resolver + MCP config). Main module 4878 → 4552. ARCHITECTURE row 5.5.
- **2026-05-18 (main)** — first cut at org cross-file links per user feedback. Added `:CUSTOM_ID: overview` to both new modules' overview headings; ARCHITECTURE.org module table is now a column of `[[file:...][label]]` links (every load-order row is clickable); code-agent-org.org TOC entries that pointed into customize/env-permission are now `[[file:...::#overview][label]]`. Tier-3 queue item kept open for sweeping the rest.
- **2026-05-18 (main)** — **Tier-2 #3**: extracted `code-agent-org-project-prompts.org` (154 lines, PROJECT_ROOT + Docker path-map + system-prompt assembly). Main module 4552 → 4398. ARCHITECTURE row 5.7.
- **2026-05-18 (main)** — **Tier-2 #4**: extracted `code-agent-org-title-gen.org` (145 lines, copilot-title-generator + dispatcher). Main module 4398 → 4287. ARCHITECTURE row 5.8.
- **2026-05-18 (main)** — **Tier-2 #5**: extracted `code-agent-org-block.org` (271 lines, boundary primitives + instruction-by-tag + heading helpers). Main module 4287 → 4029. ARCHITECTURE row 5.8 (block) / 5.9 (title-gen).
- **2026-05-18 (main)** — **Tier-2 #6**: extracted `code-agent-org-session-context.org` (374 lines, file-level property + session id/scope + sdk-uuid + recovery). Main module 4029 → 3655. ARCHITECTURE row 5.2.
- **2026-05-18 (main)** — second pass on org cross-file links: code-agent-org.org TOC + 2 in-body refs upgraded to real `[[file:...]]` links; code-agent-cmux-backend.org pointed at code-agent-org-cmux.org via link. Attempted `--recover-session` 68-line decomposition but rolled back when verify-implementation-grep tests blocked the split — added Tier-3 follow-up to convert those tests from `string-match` of function body to behavioural assertions.
- **2026-05-18 (main)** — **Tier-2 #7**: extracted `code-agent-org-process.org` (614 lines, persistent-registry class + backend registry + lifecycle hooks + query-id system + marker helpers + AI_EXEC_STATUS). Main module 3655 → 3049 lines. ARCHITECTURE row 7.5.
- **2026-05-18 (main)** — **Tier-2 #8**: extracted `code-agent-org-workspace.org` (500 lines, living-workspace navigation, ACTIVE_STORY hooks, CUSTOM_ID generation, story archival, --collect-current-context). Main module 3049 → 2585 lines. **All 8 originally-planned Tier-2 module extractions are now complete.** code-agent-org.org: 5984 → 2585 (-57%). ARCHITECTURE row 5.95.
- **2026-05-18 (main)** — **Python Tier-2 #1**: McpClientProtocol now annotates `WorkspaceBridge.__init__(mcp: McpClientProtocol)` (was McpClient); WorkspaceBridgeProtocol deleted along with its single existence-test. make tangle-python re-emitted byte-equivalent .py for every other module.
- **2026-05-18 (main)** — **Python Tier-2 #2**: `OI_KIND_ATTR` consolidated — `_OI_KIND_ATTR` made public in otel_setup.py, duplicate definition deleted from workspace_bridge.py, import wires the cross-module reference. 206/206 Python tests green.
- **2026-05-18 (main)** — **Python Tier-2 #3**: deleted `_write_custom_id` + `_clear_custom_id` (pure 1-line aliases), 6 call sites + tests updated to plural form. Kept `_read_custom_id` (does the meaningful list→latest-or-None transform). 206/206 Python tests green.
- **2026-05-18 (main)** — **Python Tier-2 #4**: promoted 3 OtelBridgeServer module dicts to class attributes (SPAN_KIND_MAP / OI_KIND_DEFAULTS / META_ATTR_KEYS as frozenset). Subclasses can now override per-test. 206/206 Python tests green.
- **2026-05-18 (main)** — **Python Tier-2 #5**: extracted `PhoenixLifecycle` class from `otel_setup.py`. Module-level constants/functions become thin shims over a singleton instance — legacy import surface preserved, callers unchanged. Tests can construct a fresh PhoenixLifecycle for isolation. 206/206 Python tests green.
- **2026-05-18 (main)** — **Python Tier-2 #6**: extracted `ClaudeHooksFile` class in `claude_workspace.py` (replaces 2 free functions + 3 timeout literals + 3 duplicate hook-block dicts with one class, `_hook_block` helper, 4 class constants). 206/206 Python tests green.
- **2026-05-18 (main)** — **Python Tier-2 #7**: extracted shared `AgentsMdInjector` class in `workspace_launcher.py`. Deleted two near-identical `_write_agents_md` free defs from copilot_workspace.py + opencode_workspace.py — copilot/opencode launchers now call `AgentsMdInjector(path).inject(prompt)` with their respective paths (.github/AGENTS.md vs AGENTS.md). 206/206 Python tests green.
- **2026-05-18 (main)** — **Python Tier-2 #8**: extracted `TranscriptReader` ABC + `ClaudeTranscriptReader` + `CopilotTranscriptReader`. Polymorphic dispatch replaces the recurring `first_entry.get("type", "").startswith("session.")` discriminator at 3 call sites. Shared JSONL parsing + last-user-boundary algorithm live on the base (was duplicated 3×). The 4 free `_extract_*` defs keep their signatures as back-compat shims so tests don't need updates. 206/206 Python tests green.
- **2026-05-18 (main)** — Test rewrite + long-function decomposition unblocked: 3 implementation-grep tests for `--recover-session` rewritten as behavioural; `--recover-session` 68 → 17 lines (3 cohesive helpers: `--recovery-budget-spent-p`, `--insert-recovery-section`, `--dispatch-recovery-request`). 544/544 Elisp tests green.
- **2026-05-18 (main)** — Long-function decomposition #2: `--handle-complete` 70 → 9 lines via `--complete-span-attrs` + `--complete-fire-hooks` + `--complete-finish-or-loop`. Top-level reads as a 3-phase narrative. 544/544 Elisp tests green.
- **2026-05-18 (main)** — Long-function decompositions #3-#10 (one commit per phase): `--workspace-archive-workflow` 63→13, `--cmux--execute-ai-block` 80→23, `--workspace-bridge-update-todos` 79→31, `--cmux--call` 61→21, `--workspace-bridge-insert-prompt` 60→24, `--start-process` 100→27, `--process-sentinel` 69→28, `--verbose-insert-query-header` 64→23, `--handle-control-request` 61→30, `--cleanup-stale-queries` 60→22. Each top-level reads as a 3-5 phase narrative. No 60+ line function bodies remain anywhere in the codebase.
- **2026-05-19 (main)** — Tier-3 pcase allowlist converted from absolute line numbers (drift-prone) to 12-char SHA-1 fingerprints of the `pcase` line + 3 lines below. Verified robust against prose insertion above the pcase (line 259→260, test stays green). Fails closed on pcase body changes — forces re-audit. 544/544 green.
- **2026-05-19 (main)** — **Python Tier-2 #9**: extracted `McpConfigInjector` template-method base + `DotGithubMcpInjector` + `OpencodeJsoncInjector` subclasses. Re-evaluation overrode my 2026-05-18 audit-revoke — the two free fns DO share a template-method skeleton (resolve→read→parse→mutate→write→atexit). Free fns kept as 1-line back-compat shims for tests. 206/206 Python tests green.
- **2026-05-19 (main)** — **Python Tier-2 #10 (last item) — AgentKind enum**: added `AgentKind` enum (claude/copilot/opencode) with per-member `cli_session_property` @property. `_cli_session_property()` shim now delegates. Elisp side already used symbol dispatch — round-trip via AGENT_TYPE env var (lowercase string form) is compatible without code changes; added cross-language-contract prose to `code-agent-org-cmux-agent-type` defcustom. Python 206/206, Elisp 544/544. **All 10 of Agent E's Python OOP audit items are now closed.**
