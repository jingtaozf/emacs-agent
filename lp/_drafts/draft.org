# -*- mode: org; -*-
#+TITLE: Design Drafts — code-agent
#+OPTIONS: toc:nil

Proposal queue.  Each proposal is a top-level =* <year>-<month>-<slug>=
section.  Approved designs migrate from here into the relevant code
.org module's prose preamble (per the project's =design-stays-in-org=
rule); rejected proposals stay here as historical record.

* 2026-05-26-pi-tui-to-emacs-migration
:PROPERTIES:
:CUSTOM_ID: pi-tui-migration
:STATUS: phase-1-shipping
:OWNER: jt
:END:

** Motivation
Pi.dev (=pi --mode rpc=) is embedded as the =:CLAUDE_BACKEND: pi=
backend.  Today, several configuration / discovery / interaction
features (model picker, OAuth login, session tree, =pi install=,
=pi config=) still require dropping into Pi's terminal UI in a separate
window.

Goal: a user who lives in Emacs should never need to leave it to do
*anything* Pi can do.

** Comprehensive feature matrix (Phase 1-4 plan)

10 categories cataloged from =reference/pi/packages/coding-agent/src/modes/interactive/=
(14 609 LOC, 24 components):
- Conversation rendering (assistant / user / thinking / tool / diff / bash / skill / compaction / branch / custom / error)
- Input handling (editor, autocomplete, external editor, bash mode, follow-up queue, image paste, extension editors)
- Selectors (model, session resume, session tree, fork, theme, thinking, settings, OAuth, extension select/confirm/input)
- Status / footer / header (cwd+git+tokens, working spinner, retry countdown, extension status, terminal title)
- Keybindings (~20 standard + extension shortcuts)
- Slash commands (=/settings=, =/model=, =/export=, =/import=, =/share=, =/name=, =/fork=, =/clone=, =/tree=, =/login=, =/logout=, =/new=, =/compact=, =/reload=, =/debug=, =/resume=, =/skill:*=)
- Extension UI (=ctx.ui.select/confirm/input/editor/notify/setStatus/setWidget/setTitle=)
- Streaming / progress
- Image / multimodal
- Help / discovery

Difficulty taxonomy: T (trivial, RPC already received) / E (easy, small Emacs handler) / M (medium, new Emacs UI) / H (hard, no RPC surface — Pi PR needed).

*** RPC commands available (30 total)

prompt, steer, follow_up, abort, new_session, get_state, set_model,
cycle_model, get_available_models, set_thinking_level, cycle_thinking_level,
set_steering_mode, set_follow_up_mode, compact, set_auto_compaction,
set_auto_retry, abort_retry, bash, abort_bash, get_session_stats,
export_html, switch_session, fork, clone, get_fork_messages,
get_last_assistant_text, set_session_name, get_messages, get_commands,
extension_ui_response.

*** Pi features WITHOUT an RPC surface (the hard cases)
- =registerMessageRenderer= (silent no-op in RPC mode)
- =setWorkingMessage / setWorkingIndicator= (TUI-only)
- =ctx.ui.custom(factory)= (returns undefined in RPC)
- =setFooter / setHeader= (no-op)
- =setEditorComponent / addAutocompleteProvider= (no-op)
- =setTheme / getAllThemes= (returns failure / empty)
- =registerShortcut= (no keyboard source in RPC)
- =onTerminalInput= (no-op)
- session tree (internal data structure; needs Pi PR or Emacs reads JSONL directly)
- OAuth login flow (TUI-internal; =async-shell-command "pi login"= is the workaround)

** Phase 1 — RPC controls + extension UI request handlers
:PROPERTIES:
:CUSTOM_ID: pi-mig-phase-1
:STATUS: shipping
:END:

Status: *implementing*.  All commands bound under =C-c C-/ p=
(submenu =code-agent-pi-ui-menu= defined in [[file:../backend/code-agent-pi-ui.org][code-agent-pi-ui.org]]).

Commands wired:
- Session: new, set-name, export-html
- Model: cycle, pick (=completing-read=)
- Thinking: cycle, set (=completing-read=)
- Send: steer, follow-up, abort, compact
- Info / Setup: stats, login (=async-shell-command "pi login"=)

Extension UI request handler (replaces the auto-cancel stub):
=select= → completing-read; =confirm= → yes-or-no-p; =input= → read-string;
=editor= → read-string (multi-line later); =notify= → message/display-warning;
=setStatus/setWidget/setTitle/set_editor_text= → log + ack (Phase 4 will
render properly).

E2E: =test-code-agent-pi-ui.el= covers smoke (symbols + menu prefix +
extension handler swap + error path) and live (cycle-thinking actually
mutates state via Pi RPC; steer/follow_up accepted).

** Phase 2 — Multimodal + tool-call inline rendering
:PROPERTIES:
:CUSTOM_ID: pi-mig-phase-2
:STATUS: queued
:END:

| Item | Approach |
|------+----------|
| =#+ATTR_AI: :image <path>= header parser | In [[file:../org/code-agent-org-block.org][code-agent-org-block.org]]; reads file, base64s, packs into Pi's =prompt.images= field |
| Per-tool inline rendering | =tool_execution_*= events → foldable =*** Tool: name= subheading inside response section |
| Diff overlay | unified-diff text → =#+begin_src diff= block (=diff-mode= fontification) |

** Phase 3 — Session tree + resume picker
:PROPERTIES:
:CUSTOM_ID: pi-mig-phase-3
:STATUS: queued
:END:

| Item | Approach |
|------+----------|
| Resume picker | Scan =~/.pi/agent/sessions/<cwd-hash>/=, =completing-read= over entries, dispatch =switch_session= |
| Session tree | Read JSONL parentId chain, render with =hierarchy.el= in =*Pi Sessions*= buffer |

** Phase 4 — Widget overlay + custom message rendering
:PROPERTIES:
:CUSTOM_ID: pi-mig-phase-4
:END:

| Item | Approach |
|------+----------|
| =setWidget= overlay | Buffer-local overlay zone above/below ai block |
| Custom message renderer | Fallback: pretty-print =message_start role=custom= JSON; pending Pi PR for native dispatch |

** Pi-side change set (potential upstream PRs)
1. =registerMessageRenderer= result over RPC (=custom_message_render= event)
2. =setWorkingMessage/setWorkingIndicator= passthrough event
3. =ctx.ui.custom()= degrade to =notify= + serializable description
4. =get_session_tree= RPC (currently we read JSONL directly as workaround)
5. =onTerminalInput= / =registerShortcut= — document as TUI-only

** References
- =reference/pi/packages/coding-agent/src/modes/interactive/= — Pi TUI source.
- =reference/pi/packages/coding-agent/src/modes/rpc/rpc-types.ts= — RPC contract.
- =reference/pi/packages/coding-agent/src/core/extensions/types.ts= — extension API.
- [[file:../backend/code-agent-pi-backend.org][code-agent-pi-backend.org]] — backend integration.
- [[file:../backend/code-agent-pi-ui.org][code-agent-pi-ui.org]] — Phase 1 control panel.
- =tests/test-code-agent-pi-ui.el= — Phase 1 tests.

* 2026-06-05-human-onboarding-interface
:PROPERTIES:
:CUSTOM_ID: human-onboarding-interface
:STATUS: research-done; proposal pending approval
:OWNER: jt
:END:

** Motivation
The codebase is increasingly written/driven by a code agent (Claude
Code) but co-owned by a human who did not author most of it.  The
human gets lost: doesn't know how to run features from the CLI, and
lacks a mental model of the main modules.  Goal: guide the agent to
produce + continuously keep fresh a *human-friendly onboarding
interface*, especially in our org-mode literate-programming way.

Four human pain points (all in scope):
1. *How to run* — accurate, copy-pasteable command cheatsheet / runbook.
2. *Architecture map* — main modules, data flow, entry points, "to change X edit Y".
3. *Freshness* — keep onboarding docs up to date as the agent edits; avoid doc rot.
4. *Getting started* — explicit read order + how to make a safe trial change + verify.

** Mechanism comparison (research, 2026-06-05)
| Mechanism | Pain | Auto-fresh? | LP/org fit | Precedent |
|-----------+------+-------------+------------+-----------|
| Self-documenting Makefile (=##= + =make help= grep/awk) | ① | Yes — targets are the source of truth | High | marmelab; prwhite gist |
| AGENTS.md / CLAUDE.md (root agent instructions) | ②④ | Manual | Med | codersera AGENTS-vs-CLAUDE 2026 |
| llms.txt (curated root index) | ②④ | Manual/gen | Med (web-leaning) | llmstxt.org |
| Aider-style repo map (tree-sitter + PageRank) | ② | Yes — generated from source | Med (elisp grammar) | aider.chat repomap |
| INDEX/README generation from .org metadata | ②④ | Yes — skill rebuild | High (have =rebuild-lp-readme=) | this repo |
| ADR + diagrams-as-code (Mermaid/C4 embedded) | ② | Semi | High (org inlines mermaid) | C4 / ADR practice |
| docs-as-tests / tangled runnable examples | ①④ | Yes — CI runs them | High (LP already tangles) | =tests-embedded-in-narrative= rule |
| Getting-started golden path (read + safe-try + verify) | ④ | Manual (short ⇒ low rot) | High | golden-path / executable onboarding |

** Current emacs-agent coverage (gap analysis, 2026-06-05)
| Pain | Current state | Verdict |
|------+---------------+---------|
| ① run | =make help= exists but *hand-written =@echo=*; 144 targets, ~30 documented | Will drift; already incomplete — highest-leverage fix |
| ② arch | =ARCHITECTURE.org= (191 ln, boundary diagram) + =lp/INDEX.org= group table + per-group READMEs | Strong but structural/agent-flavoured; missing human "change X → edit Y" table |
| ③ fresh | =rebuild-lp-readme= auto-regens READMEs/INDEX | Index layer auto-fresh; =make help= + ARCHITECTURE prose hand-maintained |
| ④ start | =lp/INDEX.org= has "Read order for first-time readers" | Read order present; missing "safe trial change + verify" path |
| misc | =README.org= is 2231 lines; =AGENTS.md= (64) + =CLAUDE.md= (300) both present | README too big for quickstart; AGENTS/CLAUDE dual-write drift risk |

** Recommended artifact mix (5)
1. *Self-document =make help=* (① ③).  Annotate 144 targets with =##=,
   render via grep/awk, =.DEFAULT_GOAL := help=.  Auto-fresh by
   construction; this is the live "how to run X" cheatsheet.
2. *=QUICKSTART.org=, ≤1 screen, human-written* (④ + entry to ②).  Not
   the 2231-line README: what this is in 3 sentences → the 5 things
   you actually run (point at =make help=) → read order (point at
   =lp/INDEX.org=) → safe first-change + verify golden path
   (=make test-smoke= → reload → run an AI block) → where to ask.
3. *"Where things live / change recipes" table in =ARCHITECTURE.org=* (②).
   10–15 rows: "change X → edit =lp/<group>/Y.org § Z=".  Human-curated.
4. *(advanced) generated elisp repo map* (② ③) as a machine-fresh
   cross-check that the hand-written change table hasn't drifted.
5. *Freshness enforcement* (③): post-edit/pre-commit regenerates
   make-help-derived + INDEX/READMEs (reuse =rebuild-lp-readme=); CI
   fails on staleness; scheduled =lp-research-audit= catches narrative drift.

** Anti-patterns (research-grounded)
- Hand-maintained command list that drifts — current =make help= 30/144 is the live example.
- 2231-line README as the onboarding doc — split a 1-screen quickstart out.
- *Agent free-writing onboarding prose unreviewed* — ETH Zürich AGENTbench
  (2026-02, 138 instances / 12 repos): LLM-generated context files lowered
  task success ~3% and raised cost 20%+; human-written improved ~4%.  Rule:
  agent generates *derived* artifacts (command table / INDEX / repo map);
  human writes the *narrative* (why / tour / change recipes); agent at most
  proposes narrative for human review.
- Architecture doc drifting from code — keep ARCHITECTURE a thin meta-map
  (it already is) + back with a generated repo map + drift check.
- AGENTS.md vs CLAUDE.md dual-write drift — one imports/derives the other.

** Landing plan (propose-before-edit; Risk: med)
1. (quick, no narrative risk) Self-document =make help= → verify output covers all targets.
2. (highest human value) Draft =QUICKSTART.org= (1 screen) + add "change X → edit Y" table to ARCHITECTURE; human finalises the narrative.
3. (freshness) pre-commit/CI doc-drift check (make-help ↔ targets; INDEX current); reuse =rebuild-lp-readme=.
4. (optional/advanced) elisp repo-map generator + hook.

** Protecting human-authored regions from the agent
:PROPERTIES:
:CUSTOM_ID: human-onboarding-protect-thread
:STATUS: v1 landed (Claude + Pi) 2026-06-05; live E2E + hardening pending
:END:

Companion question (2026-06-05): stop the agent editing human-owned
doc/code, or force confirm-before-change.  Decision after research:
*defense in depth* (hard block + soft confirm), protect at *region +
org-property* level, and *cross-agent* (not just Claude Code).

*** Review correction (2026-06-05) — the human/agent line is at edit-time, not commit
Two critiques (jt) reshaped this proposal:
1. *pre-commit cannot tell human from agent* — at =git commit= git only sees
   staged diffs; it cannot enforce "agent must not, human may".  Correct.
2. The goal is *not* freeze.  The agent MAY edit human-owned sections, but
   *every such edit must be surfaced to the human to review + confirm before
   it lands*.

Consequence: the enforcement point is the agent's *edit-time* gate, not
commit-time.  Claude Code's =PreToolUse= hook *only fires for the agent's own
Edit/Write/MultiEdit* — a human editing in Emacs never triggers it — so it is
exactly the place that distinguishes agent from human, and it can show the
proposed diff for approval BEFORE writing.  Mechanism = =PreToolUse= returning
=ask= (or =permissions.ask=) scoped to human-owned targets; the prompt surfaces
in this repo's Emacs permission UI, where the human sees the old→new diff and
accepts/rejects.

=pre-commit= is *demoted*: it fits only a different requirement ("frozen for
everyone", or a backstop against non-Claude agents), and even then blocks
wholesale with no per-edit approval — so it is NOT the mechanism for
"agent-edit-with-my-approval".

Stability holes found on review (must close in v1):
| Hole | Why unstable | Close it |
|------+--------------+----------|
| Bash-write escape | agent writes via =sed -i= / =>= / heredoc, skipping Edit/Write | also =ask=/=deny= Bash that writes protected paths |
| Region detection fragile | org section line-range × Edit old_string overlap is error-prone | ship *file-level* v1; section-range is v2 |
| Bypass silently allows | under headless / =--dangerously-skip-permissions= an =ask= cannot prompt | hook *fails closed* (=deny=) when no confirm channel |
| Guard gets disabled | agent edits the =:HUMAN_OWNED:= drawer / hook / settings | put those three in the protected set too |
| Pi out of jurisdiction | =PreToolUse= is Claude-Code-only | give Pi its own gate, or scope Pi out explicitly |

*** Headline finding — what works vs what doesn't
| Mechanism | Layer | Reliable? | Bypass |
|-----------+-------+-----------+--------|
| Rules in CLAUDE.md ("don't edit X") | advisory | *No* — agent acknowledges then violates | n/a |
| In-file "DO NOT EDIT" marker (alone) | advisory | *No* — agents don't reliably respect | n/a |
| =PreToolUse= hook =deny= / =ask= | Claude edit-time | *Yes* — blocks even under =--dangerously-skip-permissions= | edit the hook |
| =permissions.deny= glob (settings.json) | Claude edit-time | *Yes* — deny>ask>allow precedence | settings change |
| git pre-commit guard (path or region diff) | *cross-agent* commit-time | *Yes* — blocks the commit whoever made it | =--no-verify= |
| CODEOWNERS + branch protection | merge-time (team/remote) | Yes at merge, *not* local edit | admin override |

The load-bearing result: *advisory rules + markers do NOT work* — CLAUDE.md
rules are advisory and lost to context compaction (issues #7777 / #32193 /
#15443); "DO NOT EDIT" comments are not reliably respected.  Only
*code-enforced* layers (hooks / permissions / pre-commit) actually block.
So "always confirm" must be a =PreToolUse= =ask= gate, not a polite request.

*** Recommended defense-in-depth (matches: both + region/ownership + cross-agent)
1. *Claude edit-time, hard* — =PreToolUse= hook + =permissions.deny=:
   =deny= for "never touch"; =ask= for "confirm first" (surfaces the Emacs
   permission UI).  Reliable for Claude Code, but Claude-only.
2. *Region / org-ownership, hard* — =PreToolUse= hook reads the Edit's
   target range and the section's org property; denies/asks when the edit
   overlaps a protected section.  Org property is a cleaner structural
   anchor than free-text markers.
3. *Cross-agent, commit-time, hard* — custom git pre-commit guard (added to
   =.pre-commit-config.yaml=) that fails if a commit modifies a protected
   path or the content of a protected org section.  Catches Pi, other
   agents, and human mistakes — agent-agnostic.  Close the =--no-verify=
   escape for the agent via =permissions.deny= on =Bash(git commit:*--no-verify*)=.
4. *Soft, supporting (not enforcement)* — the marker + a CLAUDE.md rule
   document intent so the agent usually self-avoids and a human reading
   sees ownership.  Treated as a signal, never the wall.
- *(optional, team/remote)* CODEOWNERS + branch protection "require code
  owner review" — a merge gate, irrelevant to solo-local editing.

*** Org-native ownership (the LP-fit mechanism)
Mark a section with a property drawer — =:HUMAN_OWNED: yes= (or reuse the
existing lp-resync =:LITERATE_ORG_PIN: yes=).  Both the =PreToolUse= hook
(region overlap → deny/ask) and the pre-commit guard (section content
changed → fail) parse the property drawer, so protection maps onto the
org structure rather than fragile line ranges or free-text comments.

*** Guard the guard
Protection is only as strong as its own protection: add the hook scripts,
=settings.json= deny rules, =CODEOWNERS=, and =git commit --no-verify= to the
protected/denied set — otherwise the agent can simply disable them.

*** Current emacs-agent state (2026-06-05)
- =.claude/hooks/= holds only =_env.sh= — *no local block hooks*; tangled-file
  protection comes from the =literate-agent= plugin hooks (=block-tangled-edit.sh=).
- =.pre-commit-config.yaml= is active (gitleaks, check-merge-conflict,
  =lp-protocol-verify=, …) — the natural home for a protected-region guard.
- Precedent markers already present: =code-agent.org= tangled-output note
  "DO NOT EDIT DIRECTLY"; lp-resync =:LITERATE_ORG_PIN:=.  No =CODEOWNERS=,
  no =:HUMAN_OWNED:= yet.

*** Proposed minimal landing (revised; propose-before-edit; Risk: med)
Centred on the edit-time =ask= gate per the Review-correction above:
1. Mark human-owned at *file level* first — a =PROTECTED_PATHS= list and/or
   file-level =#+PROPERTY: HUMAN_OWNED yes=.
2. =PreToolUse= hook returns =ask= for Edit/Write/MultiEdit on protected paths
   → human reviews the diff in Emacs and confirms/rejects.  Fail closed
   (=deny=) when there is no prompt channel.
3. Also =ask=/=deny= Bash commands that write protected paths (close the
   =sed=/redirect escape).
4. Treat edits to the =HUMAN_OWNED= marker, the hook script, and
   =settings.json= as protected too (guard the guard).
5. (v2) refine to org section-range overlap, so only the protected *section*
   asks, not the whole file.
6. (separate requirement, optional) a pre-commit "frozen region" guard, only
   if a no-edit-by-anyone class is ever wanted.

*** v1 landed (2026-06-05)
Section-level =:HUMAN_OWNED:= gate, both agents, one shared detector.
| Artifact | Role |
|----------+------|
| =human-owned-guard.org= → =scripts/human_owned_guard.py= | shared detector (parses =:HUMAN_OWNED:= sections, decides =ask=/=deny=) |
| =.claude/settings.json= =PreToolUse= (=Edit\vert Write\vert MultiEdit=) | Claude adapter → execs the guard |
| =lp/backend/code-agent-pi-extensions.org= =attachHumanOwnedGate= → =emacs-mcp.ts= | Pi adapter: =tool_call= on =edit=/=write= → guard → =ctx.ui.confirm= / =!hasUI= ⇒ =block= |
| =tests/fixtures/human-owned-test.org= | one =:HUMAN_OWNED:= section for validation |

Validated: guard 7/7 detection cases (protected/normal/after/marker-touch/Write/MultiEdit/non-org); Pi ext bun-transpiles clean; both adapters wired.  Each takes effect at the *next* Claude / Pi session (configs load at start) — no live cross-agent E2E yet.

Remaining (v1.1):
- *Bash-write escape* still open — gate =Bash= (=sed -i= / =>=) on protected paths (plugin already does this for tangled files via =block-bash-tangle-write.sh=; mirror it).
- *Guard the guard* — =settings.json= hook, =scripts/human_owned_guard.py= / =human-owned-guard.org=, and the Pi extension are not yet self-protected.
- *Live E2E* — confirm a real Claude edit prompts, and a Pi =edit= raises =yes-or-no-p=, in fresh sessions.
- Nothing committed.

*** Protection-topic references
- Claude Code permissions + hook precedence: [[https://code.claude.com/docs/en/permissions][permissions docs]]
- Rules are advisory / hooks enforce: [[https://dev.to/minatoplanb/i-wrote-200-lines-of-rules-for-claude-code-it-ignored-them-all-4639]["200 lines of rules, ignored"]]; CC issues [[https://github.com/anthropics/claude-code/issues/32193][#32193]], [[https://github.com/anthropics/claude-code/issues/7777][#7777]]
- Markers not reliably respected / use config: [[https://www.agentrulegen.com/guides/how-to-protect-files-from-ai-agents][protecting files from AI agents]]
- CODEOWNERS is review-routing (needs branch protection to gate): [[https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners][GitHub about-code-owners]]
- git pre-commit protected-file/region guard: [[https://blacksheepcode.com/posts/quick_tip_protect_committing_file_modifications][blacksheepcode]]

** References
- llms.txt: [[https://llmstxt.org][llmstxt.org]]; [[https://codersera.com/blog/llms-txt-complete-guide-2026/][2026 guide]]
- AGENTS.md vs CLAUDE.md + ETH AGENTbench: [[https://codersera.com/blog/agents-md-vs-claude-md-vs-cursor-rules-comparison-2026/][codersera]]; [[https://bestagent.dev/claude-md-vs-agents-md-2026/][Claude Code reads CLAUDE.md]]
- Aider repo map: [[https://aider.chat/2023/10/22/repomap.html][repomap blog]]; [[https://aider.chat/docs/repomap.html][docs]]
- Self-documenting Makefile: [[https://marmelab.com/blog/2016/02/29/auto-documented-makefile.html][marmelab]]; [[https://www.thapaliya.com/en/writings/well-documented-makefiles/][thapaliya]]

* 2026-06-19-pi-vs-claude-code-stability
:PROPERTIES:
:CUSTOM_ID: pi-vs-claude-code-stability
:STATUS: Tier-1 shipped + E2E-verified 2026-06-19; Tier-2/3 pending
:OWNER: jt
:END:

** Motivation
Pi (=:CLAUDE_BACKEND: pi=) is observably less stable and gets less
done than Claude Code *even when both drive the same model*.  The gap
is therefore not the model — it is the *agent harness around* the
model.  Source dive (2026-06-19) across =reference/pi/= and
=~/projects/claude-code/src= located the cause in three compounding
layers, each fixable at a different blast radius:

1. *Core engineering resilience* — pi's core loop lacks two guardrails
   Claude Code has (per-tool-call timeout, in-stream reconnect) and a
   third (turn/loop cap) that pi only enforces via an *optional
   extension*, not the core.  Any single transient fault can topple
   the whole session.  Lives in pi source ⇒ upstream ask (Tier 3).
2. *Configuration* — =~/.pi/agent/= ships its compaction feature
   *switched off*, blinds observability (otel =metadata_only=), and
   defaults to a weaker model (DeepSeek) — while coding discipline
   lives only in the optional presets.  Ours to fix today (Tier 1).
3. *Thin default prompt* — pi's *default* system prompt carries only a
   few generic guidelines; Claude Code injects a rich conditional
   prompt + per-tool-result =<system-reminder>= guardrails.  pi *does*
   expose injection points (=appendSystemPrompt=, =promptGuidelines=,
   preset =instructions=), so the lever is partly ours via
   =APPEND_SYSTEM.md= (Tier 1), partly upstream (Tier 3).

** Evidence note (independently verified 2026-06-19)
Every file:line below was read first-hand (not taken from the research
subagents).  Four subagent claims were *corrected* on verification and
are NOT carried into the proposal:

- =elispString= "multiline injection bug" — *not a bug*.  =emacs-mcp.ts:76=
  escapes =\= and ="=; a literal newline inside an Elisp =\"…\"= string
  literal is *legal* Elisp, so values with newlines do not break.  Dropped
  from Tier 2.
- =execFileSync= "no try/catch, crashes the chain" — *wrong*.
  =runHumanOwnedGuard= already wraps it in =try/catch= (=emacs-mcp.ts:334=,
  comment: "a broken guard must not freeze every Pi edit").  The real
  (lesser) issue is synchronous *blocking*, not a crash.  Downgraded.
- "pi has no turn cap" — true of *core* (=agent-loop.ts:170= =while(true)=)
  but the *deployed* =settings.json loopGuard= IS enabled
  (=maxTurnsPerWindow:50 / windowMinutes:10= + content-loop detection),
  so the runaway is already mitigated in config.  Re-scoped.
- "overflow recovery only once per session / 2nd overflow kills it" —
  =_overflowRecoveryAttempted= resets every turn (=agent-session.ts:491,543=).
  Accurate statement: *one* compact-and-retry *per overflow episode*; a
  back-to-back overflow fails that turn, then resets.  Re-worded.

** Root-cause comparison (research, 2026-06-19)
Each row: the resilience dimension, Claude Code's mechanism (file:line
in =~/projects/claude-code/src=), pi's state (file:line in
=reference/pi/packages=), and the failure mode pi exhibits.

| Dimension | Claude Code | pi | pi failure mode |
|-----------+-------------+----+-----------------|
| Default system prompt | =constants/prompts.ts:131= conditional modules + =<system-reminder>= per tool-result | =coding-agent/.../system-prompt.ts:130= — a few generic guidelines ("Be concise" :125, "Show file paths" :126, bash file-ops :114); injection points exist (=appendSystemPrompt= :48, =promptGuidelines=, preset =instructions=) | default / non-preset path has no confirm-before-delete / read-before-write discipline |
| Retry / backoff | =services/api/withRetry.ts:52= — =MAX_RETRIES=10=, exp backoff + jitter (cap 32s), respects =Retry-After=, 3×529→cheap-model fallback, persistent 30s heartbeat | =agent-session.ts= retry exists (=settings.maxRetries= gate :562, =_prepareRetry= :965); SDK retry *off* =anthropic.ts:525= (=maxRetries: 0=) | mid-stream 429 / HTTP-2 RST throws "stream ended before message_stop" (=anthropic.ts:449=) ⇒ whole turn dies, no in-stream reconnect |
| Context compaction | =query.ts:396= three-layer: snip → Haiku microcompact → autocompact (=autoCompact.ts:75=); 413 triggers reactive in-turn compaction | =_checkCompaction= =agent-session.ts:1812= exists *but* =settings.json compaction.enabled:false=; one compact-retry per overflow episode (=:1849=, resets per turn =:491,543=) | built-in compaction OFF ⇒ window fills → overflow; a back-to-back overflow fails that turn |
| Large tool results | =utils/toolResultStorage.ts= — spill to disk, model gets =<persisted-output>= pointer | no disk-spill equivalent found — results stay inline | big Bash/Read output inflates the context window |
| Tool-call timeout | AbortController cascades to every parallel tool =query.ts:664= | =AbortSignal= threaded through the loop (=agent-loop.ts:159=) but *nothing fires it on a deadline* | a hung =bash= never auto-aborts; session stalls |
| Turn / loop cap | explicit exit reasons =query.ts:307= (=blocking_limit= etc.) | core =while(true)= no cap =agent-loop.ts:170=; deployed =loopGuard= ON (=maxTurnsPerWindow:50/10min= + content-loop detection) | core unbounded — runaway prevented *only* by the loopGuard extension config |
| Abort handling | synthesizes =tool_result= for orphan =tool_use= =query.ts:1013= | — | orphan =tool_use= block corrupts message history |
| Extension error boundary | n/a | =emitToolCall= =runner.ts:862-883= has *no* =try/catch= (cf =emitUserBash:893=) | a throwing =tool_call= handler pierces the chain — every handler must self-guard |

** Why "same model, worse results"
The model is the engine; pi is a car whose airbag is unplugged at the
factory (compaction off) and whose seatbelts are an *optional* dealer
add-on (discipline only in presets).  Claude Code spends thousands of
lines on the *harness* — ten-deep retry with capacity fallback,
three-layer compaction, disk-spilled tool results, abort cascades, and
a prompt that re-teaches coding discipline on every turn.  pi *can* do
much of this (preset =instructions= carry real discipline; loopGuard is
on; an extension API exists), but its *default* path ships the thin
prompt and the disabled compaction.  On a clean short task the two look
similar; the gap opens exactly when a real coding session gets long,
hits a 429, or runs a slow command — the cases the harness exists to
survive.

** Proposal — Tier 1: =~/.pi/agent/= config (zero-code, ship today)
Highest leverage-per-effort: every item is a config edit, no pi source
change.  Ranked by stability impact.

1. *Enable compaction* — =settings.json= → =compaction.enabled: true=
   (=keepRecentTokens: 30000= already set but inert with the flag
   off).  Removes the single largest session-death cause (window-full
   → overflow).  =auto-continue-compaction.ts= is *also* installed —
   verify whether it is a separate post-compaction continuation
   helper or gated by the same flag, so the two don't double-fire.
2. *Un-blind observability* — =settings.json otel.captureContent=
   =metadata_only= → =full= (or =content=).  Today Phoenix spans carry
   no input/output, so diagnosing a pi failure is blind.  We *have* a
   Phoenix workflow; let it see pi.
3. *Thicken =APPEND_SYSTEM.md== (≈10 lines → real discipline) — this
   is the *global* lever for the thin-default-prompt problem: presets
   carry discipline in their =instructions=, but the =default= /
   =cheap= / =research= presets and any no-preset session do not.
   =appendSystemPrompt= injects into *every* session.  Add: confirm
   before destructive ops, read-before-write, risk-tier self-disclose,
   preset-switch hint (=/preset plan|implement|research=).  (The 中文
   language rule already rides in via the CLAUDE.md symlink + the
   =research= preset; no need to duplicate it here.)
4. *Trust =literate-agent== — =trust.json= add
   =/Users/jingtao/projects/literate-agent: true=.  Currently only 3
   paths are trusted (=emacs-agent=, =edo-literate=, =edo-literate/repos/mega-cli=);
   =literate-agent= absent ⇒ tool permissions may degrade when its LP
   rules load.
5. *Pin dynamic-agent model* — =agents/worker.md= frontmatter has only
   =name= + =description=, no =model:=, so it inherits the DeepSeek
   default, violating the global =use-sonnet-for-dynamic-agents= rule.
   Set =model: claude-sonnet-4-5= (or deepseek-v4-flash for cheap
   mechanical work).  Check =scout/planner/reviewer.md= for the same
   gap.
6. *Raise =maxTokens== for cloud reasoning models — =models.json=
   =mimo-v2.5-pro= 16384 → 32768 (=contextWindow= is 1 M, so headroom
   is ample); the low cap truncates long-codebase output (looks like
   "pi gave up early").
7. *Audit =presets.json default== — points at =openai-codex/gpt-5.5=.
   Verify that ID resolves through the =openai-codex= OAuth provider
   in a real session; a stale alias would silently fail every default
   session.
8. *Reconsider =defaultModel== — =deepseek-v4-pro= is weaker on this
   repo's Elisp/LP/中文 surface than the Claude models already in the
   =models= whitelist.  Keep DeepSeek for cheap turns, but make the
   stability-critical path default to Claude (the =implement= / =plan=
   presets already do).

** Proposal — Tier 2: pi extensions (code we own in =~/.pi/agent/extensions/=)
The extension API (=loader.ts:520=, 28 events + =registerTool=) lets us
*compensate for core gaps without forking pi*.  No "real bug" in
=emacs-mcp.ts= survived verification (the two the research flagged were
a non-bug and an already-guarded blocking call — see Evidence note), so
this tier is compensating guards + ergonomics, ranked by value.

1. *Add a tool-timeout guard extension* — the highest-value item.  Wrap
   tool execution (via the =tool_call= hook / =registerTool=) with a
   deadline + =AbortSignal= fire, compensating for the missing core
   timeout (Tier 3 #2).  This is the extension-layer fix for "a hung
   =bash= stalls the session".
2. *Self-guard every =tool_call= handler* — because =emitToolCall=
   (=runner.ts:862=) has *no* error boundary, a throwing handler pierces
   the chain.  The emacs human-owned gate already self-guards
   (=emacs-mcp.ts:334=); audit the other installed extensions
   (=pi-permissions=, =git-ai=, =cost-tracker=…) for unguarded
   =tool_call= handlers and wrap each.  (Systemic fix is Tier 3 #1.)
3. *Make the human-owned gate async* (=emacs-mcp.ts:335=): =execFileSync=
   synchronously blocks pi's event loop for the duration of the Python
   guard call on *every* =edit= / =write=.  Switch to async =execFile= +
   Promise.  Already =try/catch=-guarded, so this is latency, not a
   crash risk.
4. *Gate tool registration on MCP health* (=emacs-mcp.ts:268=): the
   health probe currently only emits a "warning" notification (:281)
   while the =emacs_*= tools register unconditionally, so a dead MCP
   server surfaces as per-call failures.  Disable/withhold the tools
   on a failed probe.
5. *Confirm =loopGuard= covers the hang case* — =loopGuard= is already
   on (=settings.json=, =maxTurnsPerWindow:50=, content-loop detection
   via =pi-loop-guard.ts=).  Verify it also trips on a *single* stuck
   turn (no progress), not only on repetition — that overlaps with #1.
6. *Optional: dedicated Emacs write tools* — =emacs_eval= already allows
   arbitrary writes (gated by =ctx.ui.confirm= :301), so this is
   ergonomics, not a capability gap: structured =emacs_org_set_property=
   / =emacs_buffer_insert= would be safer + clearer than free-form eval.
7. *Register a =session_shutdown= cleanup hook* — reset =nextRpcId=,
   log MCP state on reload for debuggability.

** Proposal — Tier 3: pi core (upstream asks / known limitations)
Lives in =reference/pi/= source; can't patch locally without forking.
File as upstream issues / track as known limits.  Several are
*mitigated* by Tier 1/2 (noted), so Tier 3 is the "proper fix" backlog.

1. =emitToolCall= needs a =try/catch= error boundary (=runner.ts:871=,
   inside the :862 method) — it is the one emit path without one
   (=emitUserBash:893= shows the correct shape).  *Partly mitigated* by
   Tier 2 #2 (per-handler self-guard).
2. Per-tool-call timeout in the loop (=agent-loop.ts:159+= threads the
   =AbortSignal= but nothing fires it on a deadline).  *Mitigated* by
   Tier 2 #1.
3. Turn / loop iteration cap in core =while(true)= (=agent-loop.ts:170=).
   *Mitigated* by the =loopGuard= extension (Tier 2 #5); core has no
   built-in cap.
4. In-stream reconnect + raise SDK =maxRetries= above 0
   (=anthropic.ts:525=); make retry stream-level, not post-=agent_end=
   (=agent-session.ts:965=).
5. Allow more than one compact-and-retry within a single overflow
   episode (=agent-session.ts:1849=) before giving up the turn.
6. Thicken the default system prompt (=system-prompt.ts:130=).
   *Mitigated* by Tier 1 #3 (=APPEND_SYSTEM.md=) for our own setup.

** Verification / rollout
- Tier 1: edit config, start a *fresh* pi session (configs load at
  start), run a long AI block that previously died on window-fill;
  confirm a compaction event fires and the session survives.  Check
  Phoenix now shows span input/output.  Confirm =/preset implement=
  resolves Claude and =default= resolves a live =gpt-5.5=.
- Tier 2: drive a deliberately slow tool (e.g. =sleep 600=) and confirm
  the timeout guard aborts it instead of hanging; throw inside a test
  =tool_call= handler and confirm the self-guard contains it.
- Tier 3: open upstream issues with the file:line evidence above;
  re-test the mitigated items once Tier 1/2 land.

** E2E verification — Tier 1 shipped (2026-06-19)
All 7 Tier-1 edits landed in =~/.pi/agent/= (backups =*.bak-20260619=):
=compaction.enabled:true=, =otel.captureContent:full=, mimo
=maxTokens:32768=, =trust.json= + =literate-agent=, four subagents
pinned to =anthropic/claude-sonnet-4-5=, =APPEND_SYSTEM.md= thickened
with destructive-op / read-before-write / risk-declaration discipline.
(=defaultModel=, =presets.default gpt-5.5= left untouched — cost /
verify-first.)

*Auto-compaction mechanism (verified)*: =compaction.enabled= IS the
auto-compaction gate — =setAutoCompactionEnabled= / RPC
=set_auto_compaction= just call =setCompactionEnabled=
(=agent-session.ts:2073=), and =_checkCompaction= early-returns on
=!settings.enabled= (=:1814=).  Two post-turn triggers: *overflow*
(compact + auto-retry) and *threshold* (=shouldCompact= =compaction.ts:220=:
=contextTokens > contextWindow − reserveTokens=) which compacts but does
*not* auto-retry — it stops.  The installed =auto-continue-compaction.ts=
closes exactly that stop: on =session_compact=, if the agent is idle
(threshold, not overflow) it injects a "continue" turn (circuit-breaker
=MAX=5=, 800 ms defer, skips =/compact= + extension-driven).  It was
*dormant* while compaction was off (no =session_compact= events fire);
flipping the one flag activated built-in + extension together.
=reserveTokens= left at the default 16384: a bump to 32768 (to cover
mimo's =maxTokens=) was reverted because =reserveTokens= is *global* and
mimo's 1 M =contextWindow= makes its mid-turn-overflow zone (~983 K)
practically unreachable + caught by the overflow path — so the bump only
penalised the small-window models (deepseek ~128 K, claude ~200 K) that
actually compact, shaving usable window for no real gain.  Residual gap
vs Claude Code (Tier 3): pi still compacts *after* a turn + nudges, not
*before* each model call.

E2E ran =pi -p= in cmux =surface:53= (=~/projects/emacs-agent=, a
trusted path), prompt exercising read + discipline recall.  Evidence:

| Check | Result | Proof |
|-------+--------+-------|
| pi starts on new config | pass | =PI_EXIT=0=; all 3 JSON re-parse clean |
| =APPEND_SYSTEM= discipline live | pass | reply recited "在执行 =rm -rf=、=git push --force=… 前必须先确认"; Phoenix =llm.input_messages=, =token_count.prompt=13070= |
| =otel.captureContent:full= | pass | Phoenix =default= project spans @14:39 now carry =llm.input_messages=, =llm.output_messages=, =pi.tool.input/output=, =output.value= — absent under the old =metadata_only= |
| 中文 language rule | pass | reply in 简体中文 (CLAUDE.md symlink) |
| trust (no prompt) | pass | emacs-agent ran with no trust gate |
| compaction fires | not stress-tested | enabled + valid; needs a window-filling session to trigger |

*Side-finding (orthogonal to config)*: pi answered "README.org has no
=#+TITLE=" though line 8 has one.  The =pi.tool.rg= span shows
=matchCount:0= for pattern =^#\+TITLE:=; system =rg= matches it
(=exit 0=) but =rg -F= (fixed-string) does not (=exit 1=) — pi's rg
tool treats the regex as a literal.  A pi tool-layer bug, not a model
or config issue; log as a Tier-2/3 follow-up.

** Anti-patterns (what NOT to do)
- *Don't fork pi to fix Tier 3 inline* — the extension API + config
  cover most of the real-world pain; a fork forfeits upstream updates
  (=lp-cowork-boundary-object-evolution= reasoning: pi is a boundary
  object, adapt via the override surface, not a fork).
- *Don't "fix" instability by only swapping to a stronger model* — the
  harness gaps bite every model; a better model just delays the cliff.
- *Don't enable compaction without the otel fix* — if compaction
  misbehaves you need the trace content to see it.
- *Don't blanket-raise =maxTokens== on local Gemma4* (4096/6144) — that
  starves *VRAM*, a different failure; only raise the cloud reasoning
  models.

* 2026-06-19-claude-code-to-pi-migration
:PROPERTIES:
:CUSTOM_ID: claude-code-to-pi-migration
:STATUS: Phase-1 + Phase-2-web shipped + E2E-verified 2026-06-19; Phase-2 MCP pending
:OWNER: jt
:END:

** Motivation
The sibling proposal ([[#pi-vs-claude-code-stability][§ pi-vs-claude-code-stability]])
closed the *stability* gap.  This one asks the harder question: can pi
*replace* Claude Code as the daily driver?  That is a *capability-parity*
question, not a resilience one.  Source dive (2026-06-19) inventoried
both agents' user-facing surfaces (=~/projects/claude-code/src= vs
=reference/pi/= + =~/.pi/agent/extensions/=).  Verdict: pi's *in-session*
layer (hooks, steering, permissions, subagents, multimodal, context) is
already at or near parity; the *external-integration* layer (MCP, web)
is the real wall.

** Capability parity matrix (research, 2026-06-19)
Grouped by migration difficulty.  CC mechanism (file:line in
=claude-code/src=) vs pi state (file:line in =reference/pi= or
=~/.pi/agent/=).

*** Native parity — works today (config-only)
| Capability | Claude Code | pi | Note |
|-----------+-------------+----+------|
| CLAUDE.md hierarchy | =context.ts:170= | =system-prompt.ts:60= loads CLAUDE.md+AGENTS.md | symlink already wired |
| Skills + =/skill:= | =loadSkillsDir.ts:431= (~/.claude/skills) | =settings-manager.ts:973= reads =settings.skills=; user has =skills:["~/.claude/skills"]= | *verified working for this user* — NOT a gap |
| Permissions | =types/permissions.ts= | =pi-permissions.ts= reads =.claude/settings.json= | full |
| Mid-turn steering | =query.ts:1547= | =agent-session.ts:1218= =steer()=/=followUp()= | full |
| Image input | =attachments.ts:1062= | =cli/initial-message.ts:7= =pi @img.png= | full |
| Subagent model pin | =AgentTool.tsx:86= | =subagent/agents.ts:67= (pinned to sonnet in Tier-1) | full, *no worktree isolation* |
| Output modes | — | =modes/index.ts:5= tui/print/rpc/json | full |

*** Portable — pi equivalent exists, needs porting work
| Capability | Claude Code | pi | Migration action |
|-----------+-------------+----+------------------|
| Hooks | 27 events + =exit 2= gate + JSON inject (=coreTypes.ts:26=, =hooks.ts:236=) | =tool_call= (block+modify), =tool_result=, =input= (transform), =session_start=, =session_shutdown= (=types.ts:985=) | rewrite =~/.claude/hooks= shell → TS extension handlers; =pi.exec()= runs the shell |
| Plan mode | =EnterPlanModeTool.ts:36= (platform-native) | =examples/extensions/plan-mode/= (not default) | install + adapt the example extension |
| TodoWrite | =TodoWriteTool.ts:31= (native) | =examples/extensions/todo.ts= | install the example extension |
| Output styles | =loadOutputStylesDir.ts:25= | none | extension: read =~/.claude/output-styles/*.md=, inject into =systemPrompt= at =before_agent_start= |
| Auto-memory | memdir =paths.ts:92= (MEMORY.md) | none | extension: load =MEMORY.md= into context + a save command |
| Checkpoint/rewind | per-turn =fileHistory.ts:40= snapshots | =git-checkpoint.ts:54= (git-stash style) | *semantics differ* — git-stash ≠ per-turn rewind; accept or extend |
| Slash commands | =.claude/commands/*.md= | =registerCommand()= | port one by one |

*** Blockers — no pi equivalent, real engineering
| Capability | Claude Code | pi | Why it blocks |
|-----------+-------------+----+---------------|
| *MCP client* | stdio/sse/http/ws (=services/mcp/utils.ts:313=), =mcpServers= config | no transport in core, *but a community MCP-wrapper extension exists* (per jt) — usable directly | downgraded from blocker: Phase 2 = install + configure the wrapper, not build from scratch |
| Web search / fetch | =WebSearchTool.ts=, =WebFetchTool.ts= | *SHIPPED* =web-tools.ts= (=web_fetch= + =web_search=) | resolved Phase-2 — no key, no deps; see Phase 2 below |
| Subagent worktree isolation | =isolation:"worktree"= (=AgentTool.tsx:99=) | subagent has no worktree mode | parallel agents share the tree (contention risk) |

** Why the blockers are the whole story
Everything *inside* a turn — gating tool calls, injecting context,
steering, permissions, multimodal, subagents — pi already does (the
=tool_call= hook alone covers most CC =PreToolUse= automation).  What pi
*cannot* do is reach *outward*: it has no MCP client and no web tools, so
the moment a workflow depends on an =mcp__*= server or a web lookup, pi
hits a wall a config can't fix.  For this user specifically, skills are
already wired (=settings.skills=), so the migration reduces to: *port the
hooks, install three example extensions, and build one MCP bridge.*

** Phased migration plan
- *Phase 0 — stability align* (DONE 2026-06-19): Tier-1 config from the
  sibling proposal; E2E-verified.
- *Phase 1 — wire the existing equivalents* (DONE 2026-06-19):
  1. *Output style + auto-memory* → shipped as one extension
     =~/.pi/agent/extensions/claude-bridge.ts=: on =before_agent_start=
     it appends the active =~/.claude= output style (=settings.outputStyle=
     → =output-styles/<name>.md=) and the project =MEMORY.md= to the
     system prompt.
     - *Memory — hard-proven*: =pi -p "what number + colour should you
       remember?"= → "42, 蓝色" (the MEMORY.md values, unreachable to a
       default pi); the marker also appears in the Phoenix span content.
     - *Output style — load-proven, effect inconclusive*: an A/B on a
       3-way comparison prompt (=CLAUDE_BRIDGE=0= control) gave ON 983
       chars / OFF 1411, *both* with a table + verdict.  CLAUDE.md is
       loaded natively in *both* arms (symlink, independent of the
       bridge) and already carries the concise discipline, so the
       style injection cannot be isolated behaviourally — it is
       *largely redundant* with CLAUDE.md for this user.  Clean signals:
       ON ~30 % shorter + more answer-first.  Net: the *memory* half is
       the real win; the *style* half only adds the precise form rules
       CLAUDE.md defers to the style (table thresholds, 120-word cap).
  2. *plan-mode* → installed (=examples/extensions/plan-mode/= copied).
     *E2E A/B*: =pi --plan -p "write /tmp/pm-plan.txt"= refused ("plan
     mode … =write= 工具不可用", naming the read-only toolset) and the
     file was *not* created; the same prompt without =--plan= wrote
     =/tmp/pm-normal.txt=.  Tool-swap (=setActiveTools=) proven active.
  3. *todo* → *already present* via =agent-stuff/todos.ts= (62 KB); the
     example duplicate was removed (tool-name conflict).
  4. *Hooks* → no port needed: the only global hook
     (=~/.claude/hooks/emacs-agent-hook.sh=) is *Claude-backend-specific*
     (emacsclient SessionStart/Stop notify); pi drives Emacs over RPC, and
     the edit safety-gate is already covered by =pi-permissions.ts= +
     emacs-mcp's human-owned gate.
- *Phase 2 — break the wall*:
  1. *Web* → *SHIPPED* =~/.pi/agent/extensions/web-tools.ts=: registers
     =web_fetch= (fetch + dependency-free HTML→text) and =web_search=
     (keyless DuckDuckGo HTML scrape; best-effort).  No API key, no new
     deps.  *E2E*: =web_fetch https://example.com= → "Example Domain";
     =web_search "Anthropic Claude"= → 3 real results with =uddg=-decoded
     URLs (claude.com / anthropic.com).  Upgrade path: drop in a Brave/
     Tavily key for a sturdier =web_search= than the DDG scrape.
  2. *MCP* — install + configure the existing community MCP-wrapper
     extension (per jt) instead of building =mcp-bridge.ts= from scratch.
     Verify it reads an =mcpServers=-style config and registers external
     servers' tools.  (Deferred at jt's request.)
- *Phase 3 — polish*: subagent worktree isolation, checkpoint/rewind
  semantics, the CC-only hook events with no pi equivalent
  (=PreCompact=, =PermissionRequest=, =SubagentStop=, =PostToolUseFailure=)
  — decide per-event whether they carry weight.

*Decision gate*: pi becomes a viable daily driver for this user after
*Phase 1 + the MCP bridge*.  Web and auto-memory are the next-most-missed;
everything else is parity polish.

** Anti-patterns (what NOT to do)
- *Don't cut over before the MCP bridge* — losing every =mcp__*= server
  is a silent capability cliff, not a degraded mode.
- *Don't rewrite all hooks at once* — port the safety gates first and
  verify each =tool_call= block fires before moving on.
- *Don't treat =git-checkpoint= as rewind* — git-stash semantics differ
  from CC's per-turn =FileHistorySnapshot=; a "restore" won't behave the
  same.
- *Don't assume skills are broken on pi* — they load via =settings.skills=
  (verified =settings-manager.ts:973=); the only requirement is the
  explicit path entry, which is already present.
- *Don't fork pi for the blockers* — MCP/web belong in extensions
  (=registerTool= + =pi.exec=/=fetch=), keeping upstream updates intact
  (=lp-cowork-boundary-object-evolution=).

* 2026-06-29-remove-story-concept
:PROPERTIES:
:CUSTOM_ID: remove-story-concept
:STATUS: proposal
:OWNER: jt
:END:

** Motivation

The current org-mode structure has 4 layers:

: Workspace → Story → System Prompt + Workflow → Instruction + Response

Story was designed to let users run multiple tasks under one Claude Code session
via ACTIVE_STORY switching. In practice, the indirection adds complexity without
clear value:

1. ACTIVE_STORY is always set to the last-executed story — switching is automatic,
   not deliberate. Users rarely have multiple stories alive simultaneously.
2. Stories carry zero properties (everything inherits from workspace). The extra
   heading level is pure hierarchy ceremony.
3. The bridge, cmux, and session-context modules all need story-awareness code
   (find-story-heading, collect-stories, ACTIVE_STORY property, story-changed hook).
   Removing the layer eliminates ~15 functions and 2 hooks.
4. One workspace = one topic is the natural mental model. If you want a different
   topic, create a new workspace.

** Proposed structure (3 layers)

: Workspace → System Prompt + Workflow → Instruction + Response

#+begin_example
,* My Project
:PROPERTIES:
:CUSTOM_ID: xxx
:CMUX_WORKSPACE: my-project
:CLAUDE_SESSION_ID: sdd-20260629-120000
:CLAUDE_CLI_SESSION: <UUID>
:CMUX_SURFACE_ID: surface:N
:CLAUDE_BACKEND: cmux
:END:

,** System Prompt :system_prompt:
:PROPERTIES:
:CUSTOM_ID: xxx
:END:
Content...

,** Workflow :sdd:
:PROPERTIES:
:CUSTOM_ID: xxx
:END:

,*** Instruction 1 :claude_chat:
:PROPERTIES:
:CUSTOM_ID: xxx
:END:
#+begin_src ai
query...
#+end_src

,*** Response 1 (2026-06-29 12:00) :ai_output:
:PROPERTIES:
:QUERY_ID: xxx
:END:
#+end_example

*** What changes (complete inventory)

857 references to "story" across the codebase. No backward compatibility.
Every reference gets deleted or rewritten.

| Item | Before | After |
|------+--------+-------|
| Workspace child depth | Story (level+1) → System Prompt (level+2) | System Prompt (level+1) |
| ACTIVE_STORY property | on workspace heading | removed |
| code-agent-org-active-story-changed-hook | fires on story switch | removed |
| cmux tab naming | =workspace: story= | =workspace= only |
| insert-workspace | creates workspace + First Story + SP + WF | creates workspace + SP + WF |
| insert-story | creates story under workspace | removed |
| header-line badge | =workspace / story= | =workspace= only |
| Python story_name | workspace_launcher.story_name, _normalize_story_slug | removed |
| backend protocol | =on-story-changed= generic method | removed |
| System Prompt body | =The current workspace story is "X"= | removed |

*** What stays the same

- Workspace heading detection (code-agent-org--workspace-heading-p)
- PROJECT_ROOT resolution (inherits from file or workspace)
- Session management (CLAUDE_SESSION_ID, CLAUDE_CLI_SESSION)
- CUSTOM_ID generation (workspace prefix for terminal sections)
- Workflow archive + fresh workflow creation
- Instruction + Response structure
- Two channels: C-c C-c and direct terminal input
- :system_prompt: tag scanning (project-prompts.org) — unrelated to story

*** Complete removal list — Elisp functions + hooks + variables (workspace.org)

| Symbol | Type | Lines |
|--------|------|-------|
| code-agent-org--find-story-heading | defun | 178-202 |
| code-agent-org-active-story-changed-hook | defvar | 210 |
| add-hook org-property-changed-functions | hook registration | 260 |
| code-agent-org--set-active-story | defun | 214-228 |
| code-agent-org-get-active-story | defun | 230-237 |
| code-agent-org--on-org-property-changed | defun | 239-261 |
| code-agent-org--collect-stories | defun | 286-302 |
| code-agent-org--find-last-ai-block | defun | 304-315 |
| code-agent-org--goto-story-jump | defun | 317-323 |
| code-agent-org--goto-story-make-key | defun | 325 |
| code-agent-org--goto-story-make-action | defun | 333 |
| code-agent-org--goto-story-build-items | defun | 338 |
| code-agent-org-goto-story | defun | 349-366 |
| code-agent-org--goto-story-select | defun | 368-382 |
| code-agent-org--goto-story-show-menu | defun | 384-400 |

*** Complete removal list — code-agent-org.org

| Symbol | Type | Lines |
|--------|------|-------|
| code-agent-org-insert-story | defun | 358-403 |
| insert-story keybinding (C-c C-s) | keymap entry | 338 |

*** Complete removal list — cmux.org

| Symbol | Type | Lines |
|--------|------|-------|
| code-agent-org-cmux--on-story-changed | defun | 2510-2518 |
| add-hook active-story-changed-hook | hook registration | 2521 |
| ACTIVE_STORY auto-update in execute path | code block | 2073-2074 |
| rename-workspace "story name" comment | comment | 499 |
| "per-story" prose references | prose | 70, 78, 574 |

*** Complete removal list — workspace-bridge.org

| Symbol | Type | Lines |
|--------|------|-------|
| ACTIVE_STORY lookup in --find-workflow | code block | 228-242 |
| "active story" prose | prose | 84, 229, 237, 273 |

*** Complete removal list — header-line.org

| Symbol | Type | Lines |
|--------|------|-------|
| code-agent-org--header-story-name | defvar-local | 57-58 |
| story name update in --update-cached-names | code block | 149-155 |
| story badge in --location-badge | code block | 276-284 |
| "story" in prose | prose | 10, 20, 127, 143 |

*** Complete removal list — terminal-base.org

| Symbol | Type | Lines |
|--------|------|-------|
| ACTIVE_STORY read in tab-title function | code block | 127-132 |
| fallback: use workspace heading name instead | code change | — |

*** Complete removal list — backend protocol (code-agent-backend.org)

| Symbol | Type | Lines |
|--------|------|-------|
| code-agent-org-backend-on-story-changed | cl-defgeneric | 4247-4248 |
| on-story-changed row in protocol table | table row | 3999 |
| test-p3-on-story-changed-default-is-noop | test | 3999 |

*** Complete removal list — Python code

| Symbol | File | Action |
|--------|------|--------|
| _normalize_story_slug | claude_workspace.py:38 | delete function |
| story_name usage in build_args | claude_workspace.py:236-237 | delete slug logic |
| self.story_name | workspace_launcher.py:355 | delete attribute |
| story_name in _query_emacs_for_session_info | workspace_launcher.py:396-427 | remove from MCP query + parsing |
| story_name fallback in cmux workspace | workspace_launcher.py:460-467 | use cmux_ws or "" only |
| story_name in _set_terminal_tab_title | workspace_launcher.py:516-518 | use session_id or agent_name |
| story_name in print_status | workspace_launcher.py:533 | delete line |
| story_name in pi_workspace.py | pi_workspace.py:62 | delete line |
| story_name in opencode_workspace.py | opencode_workspace.py:222 | delete line |
| TestNormalizeStorySlug | test_claude_workspace.py:172-196 | delete class |
| story_name in _make_launcher | test_claude_workspace.py:31,39 | delete param |

*** Complete removal list — documentation

| File | Action |
|------|--------|
| README.org | Remove "Story section" keybinding row (line 597); remove SDD per-story docs section (lines 693-718); remove per-story override section (line 755); rewrite "story" references in system prompt examples |
| CLAUDE.md | Remove "story they're serving" line (181); remove "per-story" in research line (283) |
| .github/AGENTS.md | Remove story references (~5 lines) |
| lp/INDEX.org | Update workspace.org description (line 60): remove "ACTIVE_STORY lifecycle" |
| lp/org/README.org | Update workspace.org description |
| lp/org/code-agent-org.org | Remove story-related prose (~40 lines): insert-story section, story level alignment, story CUSTOM_ID prefix doc; remove transient menu entries: =("s" "Story section" ...)= and =("j" "Go to story" ...)= (lines 2538, 2541); remove =C-c C-s= keybinding (line 338) |
| lp/org/code-agent-org-workspace.org | Rewrite entirely: remove all story prose, story navigation section, ACTIVE_STORY section, per-story system prompt section; fix overview table (lines 22-23) — functions listed =--current-story-heading=, =--find-story-by-name=, =--story-system-prompt=, =--archive-active-story= don't exist as defuns |
| lp/org/code-agent-org-session-context.org | Remove "recovery story" prose (line 41); update conversation history docs |
| lp/backend/code-agent-multiplexer.org | Remove "focus story" prose (line 136) |
| lp/backend/code-agent-pi-extensions.org | Remove "inspect a story" prose (line 338) |
| lp/sdk/PROTOCOL-MAP.org | Remove =on-story-changed= row (line 95) |
| lp/trace/code-agent-trace.org | Update "SDD story" to "SDD session" (line 47) |
| lp/chat/code-agent.org | Update =*cmux: <story>*= verbose buffer name (line 453) |
| .claude/skills/e2e-trace/SKILL.md | Remove "Insert an SDD story" instruction (line 23) |
| .claude/rules/e2e-harness-dev-env.md | Remove "test workspace/story" (line 26) |
| tasks/docs-alignment.md | Remove story references (lines 60, 90) |
| .refactor-queue.md | Historical — leave as-is (records what happened) |
| CODEBASE-REVIEW.org | "extraction history" — English prose, leave as-is |
| research/*.org, research/*.md | Historical research docs — leave as-is |

*** Complete removal list — tests (~300+ lines across 25+ files)

**** Tests to delete entirely (story-only features)

| File | What to delete |
|------|---------------|
| test-code-agent-org-workspace.el | =test-workspace-insert-sets-active-story= (lines 106-116) |
| test-cmux-e2e-simulated.el | E14 story switch test (lines 2883-2943); ACTIVE_STORY property tests (lines 1522-1588); tab-title ACTIVE_STORY tests (lines 1640-1669) |
| test-backend-protocol3.el | =test-p3-on-story-changed-default-is-noop= (lines 78-81) |
| test-structural.el | =code-agent-org-get-active-story= symbol check (line 628) |
| test-cmux-env-injection.el | story name env injection tests (~16 lines) |
| test-e2e-pi-backend.el | story-related E2E tests (~41 lines) |

**** Tests to update — heading level adjustments

After removing the story layer, heading levels drop by 1:

| Pattern | Before (story layer present) | After (flat) |
|---------|------------------------------|--------------|
| System Prompt | =*** System Prompt :system_prompt:= (level 3) | =** System Prompt :system_prompt:= (level 2) |
| Workflow | =*** Workflow :sdd:= (level 3) | =** Workflow :sdd:= (level 2) |
| Instruction | =**** Instruction 1 :claude_chat:= (level 4) | =*** Instruction 1 :claude_chat:= (level 3) |
| Response | =**** Response= (level 4) | =*** Response= (level 3) |

Files with level-dependent assertions/fixtures to update:

| File | Lines | Change |
|------|-------|--------|
| test-code-agent-org-workspace.el:35 | =^\\*\\*\\* System Prompt= | → =^\\*\\* System Prompt= |
| test-code-agent-org-workspace.el:41 | =^\\*\\*\\* Workflow= | → =^\\*\\* Workflow= |
| test-code-agent-org-workspace.el:73 | =^\\*\\*\\* System Prompt= | → =^\\*\\* System Prompt= |
| test-code-agent-org-workspace.el:77 | =^\\*\\*\\* Workflow= | → =^\\*\\* Workflow= |
| test-code-agent-org-workspace.el:82 | =^\\*\\*\\*\\* Instruction= | → =^\\*\\*\\* Instruction= |
| test-code-agent-org-workspace.el:130 | =** First Story\n*** Workflow= | → =** Workflow= |
| test-code-agent-org-workspace.el:436 | level 3 CUSTOM_ID check | → level 2 |
| test-code-agent-org-workspace.el:441 | level 3 CUSTOM_ID check | → level 2 |
| test-cmux-e2e-simulated.el:1322 | =*** System Prompt= | → =** System Prompt= |
| test-cmux-e2e-simulated.el:1329 | =*** Workflow= | → =** Workflow= |
| test-cmux-e2e-simulated.el:1334 | =**** Instruction= | → =*** Instruction= |
| test-cmux-e2e-simulated.el:1526,1572,1654 | =*** Workflow= | → =** Workflow= |
| test-cmux-e2e-simulated.el:2900 | =*** Workflow= | → =** Workflow= |
| test-cmux-e2e-simulated.el:2905 | =**** Instruction= | → =*** Instruction= |

**** Tests to update — fixture structure flattening

| File | Before | After |
|------|--------|-------|
| test-code-agent-org-workspace.el:55-90 | =test-workspace-insert-creates-story-and-notebook-sections= — rename to =test-workspace-insert-creates-system-prompt-and-workflow=; remove "First Story" assertion; update level assertions |
| test-code-agent-org-refactor-phase1.el:81 | =* Development\n** Story\n*** Task= | =* Development\n** Task= |
| test-code-agent-org-refactor-phase1.el:187 | =* Top\n** Story\n*** Task= | =* Top\n** Task= |
| test-workspace-bridge-response.el:40 | =* Story= heading | =* Test Workspace= |
| test-cmux-e2e-agent-profiles.el | =* Test Story= headings + =story-ap-*= CUSTOM_IDs | =* Test Workspace= + =workspace-ap-*= |
| test-code-agent-org-content-loss-repro.el | Flatten story-level fixtures | Flatten to workspace → WF → instruction |
| test-code-agent-org-wrong-position-repro.el | Flatten story-level fixtures | Flatten to workspace → WF → instruction |
| test-code-agent-org-loop-detection.el | Flatten story fixtures | Flatten to workspace → WF → instruction |
| fixtures/pi-cmux-e2e.org:8 | =* Pi cmux test story= | =* Pi cmux Test Workspace= |
| test-workspace-bridge-e2e.sh:401 | =--title "my story"= | =--title "test-workspace"= |
| test-cmux-agent-name-lookup.el:12 | Comment referencing =_normalize_story_slug= | Remove or rewrite |
| support/pi-test-helpers.el:7 | "story tests" comment | "workspace tests" |

**** Tests to add — verify new flat structure works

| Test | File | What it verifies |
|------|------|-----------------|
| =test-workspace-insert-creates-flat-structure= | test-code-agent-org-workspace.el | insert-workspace produces =* WS → ** SP + ** WF → *** Instruction= (no story heading) |
| =test-workspace-no-active-story-property= | test-code-agent-org-workspace.el | workspace heading has no ACTIVE_STORY property |
| =test-workflow-found-directly-under-workspace= | test-code-agent-org-workspace.el | =--find-active-workflow-pos= finds WF as direct child of workspace |
| =test-bridge-finds-workflow-without-story= | test-workspace-bridge-response.el | bridge inserts prompt/response into workspace's WF without ACTIVE_STORY |
| =test-cmux-tab-title-workspace-only= | test-cmux-e2e-simulated.el | tab title is workspace name only, no story suffix |
| =test-header-line-workspace-badge-only= | test-code-agent-org-workspace.el | header line shows workspace badge, no story badge |
| =test-workspace-structure-levels= | test-code-agent-org-workspace.el | SP at level 2, WF at level 2, Instruction at level 3 |

**** E2E fixtures and instructions (code/prose changes)

| File | Action |
|------|--------|
| tests/e2e/org/workspace-test.org | Strip all ACTIVE_STORY properties (~15 occurrences); flatten multi-story workspaces to separate workspaces; adjust heading levels |
| tests/e2e/living-workspace-test.org | Delete T12/T13/T21/E14 story tests (~79 lines); update structure descriptions; adjust level assertions |
| tests/e2e/org/pi-backend-test.org | Flatten story references (~9 lines) |
| tests/e2e/org/pi-streaming-behavior-test.org | Rename "Story 1" / "Story 2" section headers to "Prompt 1" / "Prompt 2" (lines 46, 61) |
| tests/e2e/org/pi-tool-response-routing-test.org | Rename "Story — trigger several tool calls" heading (line 23) |
| tests/e2e/org/restart-extra-args-test.org | Rename "regression story" heading (line 38) |
| tests/e2e/org/supersede-response-test.org | "history of type systems" — English prose, leave as-is |
| tests/e2e/org/tmux-test.org | Strip ACTIVE_STORY property (line 24) |
| tests/e2e/E2E_TASKS.org | Remove story-related task entries (~8 lines) |
| tests/e2e/quality-loop-prompt.org | Remove E14 story switch entry (~7 lines) |
| tests/e2e/e2e-loop-prompt.org | Remove story switch prompt (~10 lines) |
| tests/e2e/e2e-quality-team-proposal.md | Remove E12/E31/E51 story test rows (lines 55, 86, 126); update "workspace/story" (line 152) |

**** Test prompts with "story" as English word (leave as-is)

| File | Line | Content | Action |
|------|------|---------|--------|
| test-code-agent-integration.el | 272 | "Write a very long story about numbers..." | leave |
| test-code-agent-mock.el | 169 | same | leave |
| test-code-agent-org-cancel.el | 197, 270 | "Tell me a story" | leave |
| test-code-agent-org-mock.el | 162, 238, 365, 991 | "Write a very long story..." | leave |
| test-code-agent-sentinel.el | 141 | "Write a very long story..." | leave |
| mock-claude-cli.sh | 81 | "long story" pattern | leave |
| mock-claude-cli-v2.py | 84 | "long story" pattern | leave |
| test-e2e-pi-multiblock.el | 186 | "The single ERT story" (English prose) | leave |
| test-code-agent-org-unit.el | 749, 783 | `:history` tag (not story) | leave |

*** Functions to simplify (not remove)

| Function | File | Change |
|----------+------+--------|
| code-agent-org-insert-workspace | code-agent-org.org | Remove "First Story" child; create workspace + SP + WF directly |
| code-agent-org--find-active-workflow-pos | code-agent-org.org | Walk from workspace heading, not from story |
| code-agent-org--workspace-heading-p | workspace.org | Remove story-related comments |
| workspace-bridge --find-workflow | workspace-bridge.org | Find WF directly under workspace (no ACTIVE_STORY) |
| workspace-bridge-insert-prompt | workspace-bridge.org | No story routing; always workspace's single WF |
| terminal-base tab-title fn | terminal-base.org | Use workspace heading name, not ACTIVE_STORY |
| header-line --update-cached-names | header-line.org | Remove story name caching |
| header-line --location-badge | header-line.org | Show workspace badge only |
| Python build_args | claude_workspace.py | Remove story_name slug logic |
| Python _query_emacs_for_session_info | workspace_launcher.py | Remove story from query + parsing |

*** Migration for existing org files

Existing org files with the old 4-layer structure need a one-time migration.
A =code-agent-org--migrate-remove-stories= function handles this interactively:

1. For each workspace with exactly one story: promote story children to workspace
   children (System Prompt and Workflow move up one level). Delete the story heading.
2. For each workspace with multiple stories: each story becomes a separate workspace.
   The new workspace inherits PROJECT_ROOT and CLAUDE_BACKEND from the parent.
   A new CLAUDE_SESSION_ID is generated for each.
3. Strip all ACTIVE_STORY properties.
4. Strip the =The current workspace story is "X"= line from System Prompt bodies.

No automated migration for Python — the story_name attribute simply disappears.
Callers that used it fall back to cmux_ws or session_id.

*** Design rationale

The story concept was an optimization for a workflow that rarely materializes:
running multiple concurrent tasks in one session. In practice:

1. Users create one workspace per project/topic
2. Each workspace has exactly one active workflow at a time
3. Archive + fresh workflow (already supported) handles task switching within a workspace
4. The extra indirection layer adds cognitive load and code complexity

Removing stories makes the org structure flatter, the code simpler, and the mental
model cleaner: one workspace = one persistent Claude Code session = one topic.

*** Verification

1. Create new workspace with the simplified structure
2. Execute ai blocks and verify responses insert correctly
3. Verify bridge routing works (direct terminal input → correct WF)
4. Verify cmux tab naming shows workspace name only (no story suffix)
5. Verify header line shows workspace badge only
6. Run migration on existing test org file and verify structure
7. Verify Python launcher no longer references story_name
8. Run full test suite: =make test-unit= + =make test-python=
9. Verify =grep -rn "story" --include="*.org" --include="*.el" --include="*.py"= returns only
   prose/history references (commit messages, research docs), zero code references
10. New tests pass: flat structure, no ACTIVE_STORY, direct WF lookup, workspace-only badges

*** Anti-patterns

- Don't keep ACTIVE_STORY as a "just in case" — dead code breeds bugs
- Don't create a compatibility shim for old story functions — break cleanly
- Don't change the Workflow/Instruction/Response structure — that stays
- Don't rename workspace concepts — CMUX_WORKSPACE, CLAUDE_SESSION_ID stay as-is
- Don't leave story references in prose that describes current behavior — rewrite or delete

*** References

- lp/org/code-agent-org-workspace.org — current workspace + story model (88 refs)
- lp/org/code-agent-org.org — insert-workspace, insert-story (40 refs)
- lp/org/code-agent-org-workspace-bridge.org — terminal-to-org bridge (9 refs)
- lp/org/code-agent-org-cmux.org — cmux backend (32 refs)
- lp/org/code-agent-org-header-line.org — header line badges (14 refs)
- lp/org/code-agent-org-terminal-base.org — tab title (2 refs)
- lp/sdk/code-agent-backend.org — backend protocol (7 refs)
- lp/sdk/code-agent-python.org — Python launcher (32 refs)
- python/code_agent/workspace_launcher.py — story_name attribute
- python/code_agent/claude_workspace.py — _normalize_story_slug
- tests/ — ~200+ lines across 12 test files


* 2026-07-25-orca-multiplexer-backend
:PROPERTIES:
:CUSTOM_ID: orca-multiplexer-backend
:STATUS: shipped
:OWNER: jt
:END:

** Motivation

The user now runs Claude Code inside *Orca* (=com.stablyai.orca=
v1.4.155) as well as cmux — both apps are live.  Every org
control-plane verb (launch, restart, cancel, status, permission focus)
was hard-wired to cmux, so inside Orca the whole control plane was
dead.

The goal was *one more multiplexer*, not a refactor of the multiplexer
story: cmux behaviour unchanged, no protocol re-litigated.

** What shipped

| Piece | Where |
|-------+-------|
| Orca backend — all 9 Protocol 2b generics, direct (not a facade) | [[file:../backend/code-agent-orca-backend.org][code-agent-orca-backend.org]] |
| Registry entry + =code-agent-org-default-backend= defcustom | [[file:../org/code-agent-org-process.org][code-agent-org-process.org]], [[file:../org/code-agent-org-customize.org][code-agent-org-customize.org]] |
| Six Protocol 3 generics for hook routing + terminal lifecycle | [[file:../sdk/code-agent-backend.org][code-agent-backend.org]] § Hook Routing, § Terminal Lifecycle From Org |
| Backend-neutral handler bodies any future multiplexer inherits | [[file:../org/code-agent-org-terminal-base.org][code-agent-org-terminal-base.org]] § Hook Routing |
| Frontend =fboundp= dispatchers replaced by Protocol 3 dispatch | [[file:../org/code-agent-org.org][code-agent-org.org]] § Terminal Dispatchers |
| Host-aware workspace labelling | [[file:../sdk/code-agent-python.org][code-agent-python.org]] =_host_multiplexer= |
| 14 unit tests + Journey 9 live E2E | =tests/test-orca-backend.el=, =tests/e2e/living-workspace-test.org= |

Gates at the end: =make check= green (static analysis, 73 elisp unit
tests, org-surface, 117 python tests), plus the live E2E table below.

** E2E evidence

Measured against the running Orca runtime on 2026-07-25, including
three real Claude Code sessions launched, driven, cancelled and closed.
Findings that *contradicted* an earlier draft of this proposal are
marked *CORRECTION*.

| # | What was tested | Result |
|---+-----------------+--------|
| T1 | CLI latency, 5-call mean | =orca= ≈ *230 ms/call*; =cmux= ≈ 103 ms.  =orca --help= alone costs 407 ms → the cost is *process startup* (a bash wrapper), not runtime RTT |
| T3 | Faster IPC available? | =daemon-v26.sock= + token exists under the app support dir.  Version-pinned name ⇒ private API; not used |
| T4 | Launch command shape | The repo's =unset VIRTUAL_ENV; set -a; . <envfile>; set +a; <cmd>= prefix runs verbatim.  cwd = worktree path.  =ORCA_WORKTREE_ID= inherited by the pane |
| T4 | =CLAUDECODE= inheritance | *Not* inherited — a spawned =claude= starts without the "inside another Claude Code session" refusal |
| T4b | =terminal read= output | Plain text, *no ANSI escapes*.  Claude's prompt line uses NBSP (=\xa0=) after =❯= |
| T5 | =wait --for tui-idle= at launch | =satisfied: true= at *3.5 s*, exactly when the prompt box finished rendering |
| T6 | Bracketed paste, 2-line prompt | Both lines land in the input box; *nothing submits early* |
| T7 | =wait --for tui-idle= during a query | *CORRECTION*: =satisfied: true= after *0.5 s* while the TUI still showed =· Schlepping…=.  Render quiescence, not turn-complete |
| T7c | Cursor stream under a TUI | *CORRECTION*: =nextCursor= froze for the whole session — alt-screen redraws never advance it.  =terminal read= is a screen snapshot, exactly like =capture-pane= |
| T8/T9 | ESC cancel while genuinely busy | *A single ESC does not cancel* — it only leaves vi INSERT mode.  A *second* ESC cancels and restores the prompt text |
| T10 | Unknown handle | Error =terminal_handle_stale= |
| T12 | =terminal rename= vs auto-titling | Rename returns =ok=, but =terminal list= keeps Orca's agent-derived title.  *Titles are not a usable identity key* |
| T11/T13 | =path:= selector strictness | Exact registered worktree root only.  A subdirectory → =selector_not_found=; an unregistered git repo → =selector_not_found= |
| T14 | Closed-tab handle | *Bug found in implementation*: =terminal show= still answers =ok: true= after =terminal close --tab=, with =connected: false=.  =ensure-session= now requires =connected= |
| T15 | Handle aliasing | =terminal create= and =terminal list= can report *different* handles for the same =tabId=; both resolve and both drive the pane |

Still untested, deliberately: behaviour across an Orca *app restart*.
Testing it means quitting Orca under the user's live session.  The
=adopt-identity= path is written for it (properties, not handles, are
the durable identity) but the claim is unverified.

** Consequences that shaped the code

1. *230 ms per CLI call.*  Emacs is single-threaded and the project
   already bans =call-process= inside timers.  Every non-user-initiated
   call uses =start-process= + sentinel, and identity is cached in
   slots + org properties instead of re-queried.
2. *=tui-idle= is not turn-complete.*  Used for launch readiness and
   nothing else; turn completion still arrives via the =Stop= hook.
3. *Titles are not identity.*  cmux's =--recover-session= resolves a
   workspace by heading title.  That cannot port, so Orca stores
   =ORCA_WORKTREE= + =ORCA_TERMINAL= as properties and reads them back
   in =adopt-identity=.
4. *A project must be registered with Orca first.*  The backend
   resolves the git top level, and on =selector_not_found= raises a
   =user-error= naming the exact =orca repo add --path …= command
   rather than registering on the user's behalf — the CLI has no
   =repo rm=.
5. *An envelope's =ok= is not liveness.*  T14; see
   =tasks/lessons.md= 2026-07-25.

** The obstacle, and what it actually was

[[file:../org/code-agent-org.org][code-agent-org.org]] routed three MCP hook events with a =cond= over
=(fboundp 'code-agent-org-cmux--…)= — the one place the *frontend
itself* knew a multiplexer by name, and the reason a second
multiplexer was unreachable regardless of how completely it
implemented Protocols 1 and 2b.

Reading the four cmux functions those arms reached showed almost none
of the body was cmux-specific:

| Function | cmux-specific content |
|----------+------------------------|
| =--query-completed= | none — session state, request-id file, loop scheduling.  Zero CLI calls |
| =--permission-resolved= | none — one =code-agent-remove-alert= |
| =--permission-needed= | two lines: focus-workspace and activate-app |
| =--recover-session= | the org-buffer scan is generic; only the title lookup and the surface/UUID =puthash= are cmux |

So the neutral bodies moved to =code-agent-org-terminal-base.org= and
cmux kept one-line =cl-defmethod= s onto its existing legacy
functions — its behaviour is byte-for-byte what it was, reached
through dispatch instead of =fboundp=.

** Generics added — six, not the one first proposed

The first draft claimed a single new generic would do.  That was wrong
in a specific way worth recording: it assumed the transient menu could
keep binding =code-agent-org-cmux-restart= directly, which would have
left the menu working for exactly one multiplexer — the same bug as
the =fboundp= dispatchers, one layer up.

| Generic | Why |
|---------+-----|
| =query-completed= / =permission-needed= / =permission-resolved= | route hook callbacks that arrive with only a workspace-session-id |
| =adopt-identity= | rebuild a backend's pane identity after an Emacs restart; cmux uses titles, Orca uses properties |
| =launch= / =cancel-from-org= | what the transient menu binds |

Two of these names were deleted in 2026-07 along with the JSON-stream
engine.  The resurrection is narrow: those carried *execution*, these
carry *routing*, and each has two implementors and a live caller from
the day it landed.

=code-agent-mux-focus= gained its first caller rather than being added
— it had shipped as a forward declaration with a no-op default and
zero callers.

** Resolved questions

*** Rename =CMUX_WORKSPACE= to a neutral =AGENT_WORKSPACE=?  No.

[[file:../org/code-agent-org-workspace.org][code-agent-org-workspace.org]] recognises a workspace root by its own
=CMUX_WORKSPACE= *or* =CLAUDE_SESSION_ID=, and that function's
docstring calls =CLAUDE_SESSION_ID= "the session marker every backend
keys on".  An Orca workspace omits =CMUX_WORKSPACE= entirely and
existing detection already works — verified live by the Journey-9
fixture, which carries no cmux property.

*** Sidebar status / progress parity?  No.

The cmux sidebar wrappers appear in *no protocol*, and a repo-wide
grep finds *zero* callers outside =code-agent-org-cmux.org=.  Orca
inherits the no-op defaults.

*** Follower / verbose buffer for orca?  None.

[[file:../sdk/PROTOCOL-MAP.org][PROTOCOL-MAP.org]] listed =mux-start-follower= / =-stop-follower= as
generics; neither had a =cl-defgeneric= or =cl-defmethod= anywhere —
the map was stale and those rows are now deleted.  T7c independently
kills rebuilding a follower on cursor reads: under a TUI the cursor
never advances.

*** Default backend when =CLAUDE_BACKEND= is unset?  Still cmux.

Runtime host detection (=TERM_PROGRAM=Orca= → orca) was considered and
rejected: both apps run simultaneously, and environment-derived
selection means the same org file behaves differently depending on
which terminal opened Emacs.  =code-agent-org-new-workspace= now stamps
=code-agent-org-default-backend= (default ="cmux"=) instead of a
hard-coded string, so a user who lives in Orca sets it once.

Note the same reasoning *did* apply to the Python launcher, where env
vars are the only trustworthy signal: from inside an Orca pane
=cmux identify= happily answers about whichever cmux workspace is
focused, so the launcher would have labelled the session with a
workspace it has nothing to do with.  =_host_multiplexer= reads the
per-pane env vars instead.

** Remaining debt

| Item | Note |
|------+------|
| =code-agent-org-cmux--build-launch-command= called from the orca backend | multiplexer-agnostic in substance, still cmux-named.  Promoting it (plus the agent-profile registry) into terminal-base is the next natural step; orca does not need it to work |
| =code-agent-org-cmux--ensure-ide-server= named in a Python MCP eval | the function is multiplexer-agnostic and works as-is; renaming touches four files for no behaviour change |
| Orca app-restart behaviour | untested — see above |
| No tangle-drift gate | the 2026-07-25 lesson wants =make check-tangle-drift=; needs a tangle-to-tempdir mode the Makefile lacks |
