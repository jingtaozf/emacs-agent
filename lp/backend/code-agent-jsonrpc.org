#+TITLE: Claude Agent JSON-RPC 2.0 Transport
#+PROPERTY: literate-load yes

* Overview

ACP, the JetBrains MCP shim, and any future protocol that pairs
newline-delimited JSON-RPC 2.0 with a stdio-based subprocess all
need the same transport plumbing: spawn, request/response
correlation by id, notification dispatch, stderr buffering, clean
shutdown.  Inlining that plumbing into each protocol implementation
produced four near-identical copies before this module was extracted.

Invariant: =code-agent-jsonrpc-client= is the *only* transport
class.  Higher-level protocols (ACP, future siblings) compose with
it through generics, never reach into =:process= or =:stderr-buffer=
directly.  Request ids are integers issued by this layer; callers
must NOT supply their own.

* Claude Agent JSON-RPC 2.0 Transport

Pure JSON-RPC 2.0 over stdio.  Agent-agnostic — wraps any existing
subprocess whose stdout emits newline-delimited JSON-RPC messages and
whose stdin accepts the same, and turns it into a Smalltalk-flavoured
object that other modules talk to through generics.

Used as the transport layer for the ACP (Agent Client Protocol)
backend and any future protocol that shares the wire format.

** Design shape

Class rather than plist: once ACP grew multiple callers reaching
into =:process= and =:stderr-buffer= directly, the
free-function-over-plist shape stopped carrying its weight.  Struct
accessors are exported so callers read state through them rather
than the raw fields.

** Dependencies

Loaded eagerly at file evaluation time — symbols below assume every required feature is already provided.

#+BEGIN_SRC elisp
(require 'cl-lib)
(require 'json)
(require 'map)
#+END_SRC

** The Client Class

=cl-defstruct= for the JSON-RPC client — bundles process handle, request-id counter, pending callbacks hash, stderr buffer.

#+BEGIN_SRC elisp
(cl-defstruct (code-agent-jsonrpc-client
               (:constructor code-agent-jsonrpc-client--create)
               (:copier nil))
  "JSON-RPC 2.0 client — one instance per subprocess."
  (process nil
           :documentation "Emacs process object for the subprocess.")
  (pending nil
           :documentation "Hash table: request-id (integer) → callbacks plist.")
  (notification-handlers nil
                         :documentation "List of (MSG) → void handlers invoked for every incoming notification.")
  (request-handlers nil
                    :documentation "List of (MSG) → void handlers invoked for server-to-client requests.")
  (stderr-buffer nil
                 :documentation "Buffer owned by the client; killed on shutdown.  Callers may `plist-put'-style attach a buffer after construction when the factory does not create one itself.")
  (partial-line ""
                :documentation "Accumulator for the trailing half-line not yet terminated by \\n."))

(defvar code-agent-jsonrpc--next-id 0
  "Monotonic JSON-RPC request id counter (process-global).
A global counter is sufficient because each client has its own
`pending' hash; routing never confuses ids across clients.")
#+END_SRC

** Factory

=lexical-let= is required because literate-elisp does not honour the
file-level =lexical-binding= directive — without it the lambda
closures would see =client= as a dynamic variable and fail at
runtime.

#+BEGIN_SRC elisp
(defun code-agent-jsonrpc-make-client (process)
  "Create a JSON-RPC client wrapping PROCESS.
Caller must ensure PROCESS is live and uses utf-8-unix with pipe I/O.
Returns a `code-agent-jsonrpc-client' struct with its filter and
sentinel wired; the returned value is the same object the caller
stores on whichever higher-level state it owns (typically a backend
struct)."
  (let ((client (code-agent-jsonrpc-client--create
                 :process process
                 :pending (make-hash-table :test 'equal)
                 :notification-handlers nil
                 :request-handlers nil
                 :stderr-buffer nil
                 :partial-line "")))
    (lexical-let ((client client))
      (set-process-filter process
                          (lambda (_proc output)
                            (code-agent-jsonrpc--filter client output)))
      (set-process-sentinel process
                            (lambda (_proc event)
                              (code-agent-jsonrpc--sentinel client event))))
    client))
#+END_SRC

** Sending

Outbound side — serialises a request struct, writes to the process stdin / socket.

#+BEGIN_SRC elisp
(defun code-agent-jsonrpc-send-request
    (client method params on-success on-failure)
  "Send a JSON-RPC request to the peer via CLIENT.
METHOD is a string.  PARAMS is an alist or nil.  ON-SUCCESS receives
the result; ON-FAILURE receives the error alist.  Returns the
assigned request id."
  (let* ((id (cl-incf code-agent-jsonrpc--next-id))
         (msg `((jsonrpc . "2.0")
                (id . ,id)
                (method . ,method)
                (params . ,params)))
         (json-str (json-encode msg))
         (proc (code-agent-jsonrpc-client-process client)))
    (puthash id
             (list :on-success on-success
                   :on-failure on-failure
                   :method method)
             (code-agent-jsonrpc-client-pending client))
    (message "code-agent-jsonrpc: -> %s (id=%d)" method id)
    (process-send-string proc (concat json-str "\n"))
    id))

(defun code-agent-jsonrpc-send-response
    (client id result &optional error-obj)
  "Send a JSON-RPC response for the inbound request ID.
Provide RESULT on success or ERROR-OBJ on failure."
  (let* ((msg (if error-obj
                  `((jsonrpc . "2.0") (id . ,id) (error . ,error-obj))
                `((jsonrpc . "2.0") (id . ,id) (result . ,result))))
         (json-str (json-encode msg))
         (proc (code-agent-jsonrpc-client-process client)))
    (process-send-string proc (concat json-str "\n"))))

(defun code-agent-jsonrpc-send-notification (client method params)
  "Send a JSON-RPC notification (no id, no response expected)."
  (let* ((msg `((jsonrpc . "2.0")
                (method . ,method)
                (params . ,params)))
         (json-str (json-encode msg))
         (proc (code-agent-jsonrpc-client-process client)))
    (process-send-string proc (concat json-str "\n"))))
#+END_SRC

** Receiving

The filter accumulates partial lines then parses each complete line.
An outer =condition-case= wraps the loop because a process filter
that signals an error terminates the subprocess in Emacs — we cannot
afford that at the transport layer.  Parse errors on a single line
are caught and logged separately so one malformed payload does not
halt the stream.

#+BEGIN_SRC elisp
(defun code-agent-jsonrpc--filter (client output)
  "Accumulate OUTPUT and dispatch complete JSON-RPC lines from CLIENT."
  (condition-case filter-err
      (let ((partial (concat (code-agent-jsonrpc-client-partial-line client)
                             output)))
        (while (string-match "\n" partial)
          (let ((line (substring partial 0 (match-beginning 0))))
            (setq partial (substring partial (match-end 0)))
            (when (> (length line) 0)
              (condition-case parse-err
                  (let ((msg (json-read-from-string line)))
                    (code-agent-jsonrpc--route client msg))
                (error
                 (code-agent-jsonrpc--handle-parse-error
                  client line parse-err))))))
        (setf (code-agent-jsonrpc-client-partial-line client) partial))
    (error
     (message "code-agent-jsonrpc: filter error (process preserved): %s"
              (error-message-string filter-err)))))

(defun code-agent-jsonrpc--handle-parse-error (_client line err)
  "Log LINE + ERR when JSON parsing fails.
The pending request id is unknown at parse-error time so callers
time out on their own clocks; the log line is enough to diagnose
most ACP wire-format skews."
  (message "code-agent-jsonrpc: parse error %s on line: %s"
           err
           (truncate-string-to-width line 200 nil nil "...")))

(defun code-agent-jsonrpc--route (client msg)
  "Dispatch an incoming JSON-RPC MSG to the correct handler branch.
Three shapes matter: a response (id but no method), a server-to-
client request (id AND method), and a notification (method, no id).
Anything else is logged and dropped."
  (let ((id (map-elt msg 'id))
        (method (map-elt msg 'method))
        (result (map-elt msg 'result))
        (err (map-elt msg 'error)))
    (cond
     ;; Response to our request (has id, no method).
     ((and id (not method))
      (let ((pending (gethash id (code-agent-jsonrpc-client-pending client))))
        (if pending
            (progn
              (remhash id (code-agent-jsonrpc-client-pending client))
              (if err
                  (progn
                    (message "code-agent-jsonrpc: <- error id=%s: %s"
                             id (map-elt err 'message))
                    (when (plist-get pending :on-failure)
                      (funcall (plist-get pending :on-failure) err)))
                (message "code-agent-jsonrpc: <- result id=%s" id)
                (when (plist-get pending :on-success)
                  (funcall (plist-get pending :on-success) result))))
          (message "code-agent-jsonrpc: response to unknown id=%s" id))))

     ;; Server-to-client request (has id AND method).
     ((and id method)
      (message "code-agent-jsonrpc: <- request %s (id=%s)" method id)
      (dolist (handler (code-agent-jsonrpc-client-request-handlers client))
        (funcall handler msg)))

     ;; Notification (has method, no id).
     (method
      (dolist (handler (code-agent-jsonrpc-client-notification-handlers client))
        (funcall handler msg)))

     (t
      (message "code-agent-jsonrpc: unrecognized message: %S"
               (truncate-string-to-width (format "%S" msg) 200 nil nil "..."))))))

(defun code-agent-jsonrpc--sentinel (client event)
  "Process sentinel for CLIENT's subprocess; rejects pending on exit.
Invokes every pending :on-failure callback with a synthesized error
so callers' completion paths fire deterministically when the peer
dies mid-stream."
  (condition-case err
      (let ((event-clean (string-trim event)))
        (message "code-agent-jsonrpc: process %s" event-clean)
        (maphash (lambda (_id pending)
                   (when (plist-get pending :on-failure)
                     (funcall (plist-get pending :on-failure)
                              `((code . -1)
                                (message . ,(format "Process %s"
                                                    event-clean))))))
                 (code-agent-jsonrpc-client-pending client))
        (clrhash (code-agent-jsonrpc-client-pending client)))
    (error (message "code-agent-jsonrpc: sentinel error: %S" err))))
#+END_SRC

** Handler Registration

Registration surface — maps the incoming identity to the local handler / state slot.

#+BEGIN_SRC elisp
(defun code-agent-jsonrpc-add-notification-handler (client handler)
  "Register HANDLER (function of one MSG) for server-side notifications."
  (push handler (code-agent-jsonrpc-client-notification-handlers client)))

(defun code-agent-jsonrpc-add-request-handler (client handler)
  "Register HANDLER (function of one MSG) for server-to-client requests."
  (push handler (code-agent-jsonrpc-client-request-handlers client)))
#+END_SRC

** Shutdown

Tear-down sequence — kills the subprocess, closes pipes, drops state.

#+BEGIN_SRC elisp
(defun code-agent-jsonrpc-shutdown (client)
  "Release CLIENT: kill its subprocess and stderr buffer, clear pending."
  (when client
    (let ((proc (code-agent-jsonrpc-client-process client))
          (stderr (code-agent-jsonrpc-client-stderr-buffer client)))
      (when (and proc (process-live-p proc))
        (delete-process proc))
      (when (and stderr (buffer-live-p stderr))
        (kill-buffer stderr)))
    (when-let* ((pending (code-agent-jsonrpc-client-pending client)))
      (clrhash pending))))
#+END_SRC

** Provide Feature

Single =provide= so the module can be =require=d.

#+BEGIN_SRC elisp
(provide 'code-agent-jsonrpc)
;;; code-agent-jsonrpc.el ends here
#+END_SRC
