#+TITLE: Design: Codebase Review — Architecture, Quality, and Harness Improvements
#+DATE: [2026-03-12]
#+STATUS: active (open-items log; DONE entries cleared 2026-05)


* Architecture Findings

** A2. Backend References MCP Server Variable (MEDIUM)

=code-agent-backend.org= declares and mutates =emacs-mcp-server-path-mappings= (lines 1603,
2244, 2352), violating the stated invariant that =emacs-mcp-server.org= has zero =claude-*=
dependencies. The backend couples to a peer module's global state.

*Proposal*: Move path-mapping ownership to the backend. MCP server reads from backend, not the
reverse.

** A3. Dual Load Paths (MEDIUM)

Both =code-agent.org= (lines 111–115) and =code-agent.el= (lines 58–61) load sub-modules
independently. =claude-org.org= (lines 219–231) has the same issue. Load order is enforced in two
places with no synchronization.

*Proposal*: Single authoritative load path in =code-agent.el=. Remove =dolist= self-loading from
=code-agent.org= and =claude-org.org=, or make it clearly conditional (only when loaded
standalone for development).

** A6. =claude-ide.org= Has No Dedicated Test Suite (MEDIUM)

2,200+-line module with WebSocket lifecycle, MCP protocol, deferred responses — only tested
incidentally via other E2E tests.

*Proposal*: Create =tests/test-claude-ide.el= with unit tests for session lifecycle, ping timer,
deferred response handling, and lockfile management.

** A11. Backend Capability Enum Undocumented (LOW)

=code-agent-backend-supports-p= accepts capability symbols, but valid values are scattered across
method implementations.

*Proposal*: Add a =defconst= listing all valid capability keywords. Add a structural test that
validates new backends declare capabilities.

* Code Quality Findings

** Q2. =McpClient.eval_elisp= Returns =None= for All Failures (MEDIUM)

=mcp_client.py:50–66= — network failure, malformed JSON, and Emacs-reported errors all return
=None=. Callers in =sdd_bridge.py= cannot distinguish "Emacs errored" from "connection refused".

If =insert-prompt= raises (org buffer read-only, session not found), Python continues silently,
writes no custom-id, and the response insertion later fails with "Instruction not found".

*Proposal*: Return structured result — ={"success": True, "result": ...}= vs ={success: False,
"error": ...}=. Raise distinct exceptions for connection failures vs Emacs errors.

** Q4. Status Set to "busy" With No Reset on MCP Failure (MEDIUM)

In =workspace_bridge.py= — =write_status(session_id, "busy")= runs before
the MCP call. If MCP fails, status stays "busy" forever. No cleanup path
exists.

*Proposal*: Wrap MCP call in try/except; reset status to "ready" on failure.


** Q8. =_escape_elisp_string= Missing Tab/Null Escaping (LOW-MEDIUM)

In =workspace_bridge.py='s =_escape_elisp_string= — tab characters and
null bytes are not escaped. Claude responses containing tab-indented
code will produce valid but potentially misaligned org properties.

*Proposal*: Add =\t= and =\x00= escaping.

** Q10. =save-cli-session= Not Persisted (LOW)

=code-agent-org-workspace-bridge-save-cli-session= writes a property but
doesn't call =save-buffer=. Python callers have no way to trigger save
afterwards. Property is lost on unexpected Emacs exit.

*Proposal*: Add =save-buffer= call (with =inhibit-message= t) inside the function.

** Q13. Loop Timer Fires After Buffer Kill (MEDIUM)

=code-agent-org--maybe-continue-loop= schedules a =run-at-time= timer.
If the buffer is killed between timer arm and fire, the marker check
prevents a crash but =code-agent-org--cleanup-session= may not cancel
an already-armed timer in time.

*Proposal*: Add a cancelled-session guard in =code-agent-org--execute-loop-iteration=.

** Q14. Stdin Close Timer Not Cancellable (LOW)

=code-agent--maybe-close-stdin= creates a 0.1 s timer with no stored
handle. If the process is killed while the timer is pending, it fires
uselessly.

*Proposal*: Store timer in process state; cancel in kill/interrupt functions.

** Q16. Missing =lexical-let= in =code-agent-org--send-request= (LOW)

The =mk= helper lambda is defined via plain =lambda= inside a =let*=.
Works today because it is called immediately, but fragile — any
refactor to async invocation would break under dynamic binding.

*Proposal*: Wrap =mk= in =lexical-let= for defensive correctness.


* Test Harness Findings

** T2. MCP Round-Trip Has Zero Tests (HIGH)

The Python → MCP → Emacs → org buffer pipeline has no automated tests. The only verification is
manual E2E testing. Bugs in =sdd_bridge.py= ↔ =emacs-mcp-server.org= interactions (escaping,
error handling, timeout) are discovered only in production.

*Proposal*: Create an integration test harness that:
1. Starts the MCP server in a subprocess
2. Sends structured MCP requests via HTTP
3. Verifies Emacs state changes (buffer content, properties)
4. Covers error cases (invalid session ID, read-only buffer, missing heading)

** T5. Regression Tag + Dedicated =make test-regression= (MEDIUM)

Bug-fix tests are scattered across test files with no easy way to run "just the regression tests".

*Proposal*: Add =:regression= tag to all bug-fix tests. Add =make test-regression= target.

** T6. E2E Test Infrastructure for CI (MEDIUM)

Real E2E tests (cmux/tmux + Claude Code + Emacs) can only run on
macOS-with-cmux for the multiplexer family; tmux pieces could in
principle run on Linux. No CI pipeline exercises the live multiplexer
stack today; tests/e2e/E2E_TASKS.org is human-driven via /loop.

*Proposal*:
1. macOS GitHub Actions runner for the cmux + Claude Code path
2. Linux runner for the tmux primitive tests (cmux unavailable)
3. Record session transcripts for debugging failures

Note: the mock-CLI subprocess scenario system (=MOCK_SCENARIO=,
=make test-integration=) this proposal originally relied on was
deleted 2026-07 along with the JSON-stream engine — a CI harness now
needs a real cmux/tmux + Claude Code path (or a new mocking strategy
at the multiplexer layer), not a subprocess-level mock.

** T10. =code-agent-chat= Has Zero Tests (MEDIUM)

The comint-based chat mode (=code-agent-chat=, =chat-send=, =chat-interrupt=, =chat-quit=,
=chat-clear=, =chat-new-session=) is listed in the public API with no test file.

*Proposal*: Create =tests/test-code-agent-chat.el= with mock CLI tests for the chat UI.

** T11. Three Expected-Failure Tests Represent Known Debt (LOW)

Tests marked =:expected-result :failed= in cancel, marker lifecycle, and integration suites.

*Proposal*: Track count in a structural test that fails if the count increases without
documentation. Periodically revisit and fix.

* Implementation Priority

| Priority | Item | Effort | Impact |
|----------|------|--------|--------|
| P1 | Q2: Structured MCP error returns | S | Prevents silent Python failures |
| P1 | Q4: Reset status on MCP failure | S | Prevents permanent "busy" |
| P2 | T2: MCP round-trip test harness | L | Catches bridge bugs |
| P2 | A3: Single load path | S | Prevents double-init bugs |
| P2 | A6: claude-ide test suite | M | Tests 2K-line module |
| P3 | T5: Regression tag | S | Fast regression runs |
| P3 | T10: Chat mode test suite | M | Tests zero-coverage module |
| P3 | A2: MCP server boundary fix | S | Clean layering |

Effort: S = hours, M = 1-2 days, L = 3-5 days.

* References

- =ARCHITECTURE.org= — module boundaries and invariants
- =ARCHITECTURE.org= → "Testing & Harness" — current harness shape (originally =docs/design-docs/2026-harness-engineering.org=)
- =code-agent-backend.org= → "Design Heritage" — backend extraction history (originally =docs/design-docs/2026-backend-extraction.org=)
- =ARCHITECTURE.org= → "Removed Backends" — what the iTerm2 mode-line / Query Manager integration looked like (deleted in commit 1038144)
