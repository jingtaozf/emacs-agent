#+TITLE: Architecture
#+DESCRIPTION: Meta-map for code-agent — navigational aid for what falls between literate documents

This file covers ONLY cross-module concerns. Each .org file documents
itself. *Tri-protocol refactor Phases 0–4 shipped* (2026-04-24); the
full design lives in =code-agent-backend.org= → "Tri-Protocol
Architecture"; this file's "Tri-Protocol Architecture" section below
is the meta-map summary.

* Module Boundary Diagram

#+begin_example
  Entry: code-agent.el (loads all modules in order below)
  ─────────────────────────────────────────────────────────

  ┌─────────────────────────────────────────────────────┐
  │ TOP: code-agent-org.org                                 │
  │   Org integration — AI blocks, streaming, execution │
  │   Depends on: code-agent, org sub-modules         │
  └──────────────────────┬──────────────────────────────┘
                         │ requires
  ┌──────────┬───────────┼───────────┬───────────┐
  │ session  │  queue    │ response  │ scheduled │
  │  .org    │  .org     │  .org     │  .org     │
  └──────────┴───────────┼───────────┴───────────┘
                         │ all depend on
  ┌──────────────────────┴──────────────────────────────┐
  │ code-agent.org  (thin UI/coordination layer)      │
  │   Mode-lines, Chat Mode, Utilities (translate/title)│
  │   Loads: backend, permission, ide                   │
  └──────────────────────┬──────────────────────────────┘
                         │ depends on
  ┌──────────────────────┴──────────────────────────────┐
  │ code-agent-backend.org  (bulk of SDK logic)       │
  │   Customization, data structures, process mgmt,     │
  │   session mgmt, JSON protocol, core API, client API,│
  │   backend protocol, JSON stream & CLI backends,     │
  │   verbose output, query cancellation                │
  └───┬────────────────────────────────────────────┬────┘
      │ cross-module dep                           │
  ┌───┴──────────┐                      ┌──────────┴──┐
  │ permission   │                      │     ide      │
  │  .org        │                      │     .org     │
  └──────────────┘                      └─────────────┘
  BOTTOM LAYER: no cross-dependencies

  ════════════════════════════════════════════════════════
   INDEPENDENT: emacs-mcp-server.org (zero claude-* deps)
   INDEPENDENT: code-agent-trace.org (OTel tracing, zero claude-* deps)
   UTILITY:     code-agent-chat.org (interactive comint-based chat REPL)
   UTILITY:     code-agent-translate.org (streaming translation commands)
   UTILITY:     code-agent-title.org (conversation titling helper)
   UTILITY:     code-agent-refine.org (AI-assisted prompt refinement helper)
   OPTIONAL:    claude-ide.org (WebSocket IDE server, needs websocket pkg)
   OPTIONAL:    code-agent-jsonrpc.org (JSON-RPC 2.0 over stdio, transport)
   OPTIONAL:    code-agent-acp.org (generic ACP backend, requires jsonrpc)
   OPTIONAL:    code-agent-acp-opencode.org (OpenCode ACP profile)
   OPTIONAL:    code-agent-acp-gemini.org (Gemini ACP profile)
   OPTIONAL:    code-agent-acp-codex.org (Codex ACP profile, needs codex-acp shim)
   DEFAULT:     code-agent-pi-backend.org + code-agent-pi-ui.org (pi.dev)
   OPTIONAL:    code-agent-pi-extension.org (tangles Pi-side TS, on demand)
   TRI-PROTO:   code-agent-multiplexer.org
                code-agent-cmux-backend.org code-agent-tmux-backend.org
#+end_example

Load order in code-agent.el:
| Order | Module                  | Why this position              |
|-------+-------------------------+--------------------------------|
|     0 | [[file:code-agent-trace.org][code-agent-trace]] | OTel tracing macros (no deps)  |
|     1 | [[file:code-agent-backend.org][code-agent-backend]] | SDK core: protocol, API, process mgmt |
|     2 | [[file:code-agent-permission.org][code-agent-permission]] | Tool permissions, no deps |
|     3 | [[file:claude-ide.org][code-agent-ide]]   | Buffer context, no deps        |
|     4 | [[file:code-agent.org][code-agent]]       | UI/coordination layer, needs 1-3 |
|   4.5 | [[file:code-agent-org-customize.org::#overview][code-agent-org-customize]] | defgroup/defcustom/defconst + hook defvars + notifier + telemetry persistence (zero deps, loaded first) |
|     5 | [[file:code-agent-org-session.org][code-agent-org-session]] | Session state, needs 4         |
|   5.2 | [[file:code-agent-org-session-context.org::#overview][code-agent-org-session-context]] | session id / scope / sdk-uuid / recovery (collect-context + recover-session), needs 4.5 + session |
|   5.5 | [[file:code-agent-org-env-permission.org::#overview][code-agent-org-env-permission]] | ENV_FILE parser + permission-mode resolver + MCP config builder, needs 4.5 + session-context (for --get-file-level-property) |
|   5.7 | [[file:code-agent-org-project-prompts.org::#overview][code-agent-org-project-prompts]] | PROJECT_ROOT + Docker path-map + system-prompt assembly, needs 4.5 + trace |
|   5.8 | [[file:code-agent-org-block.org::#overview][code-agent-org-block]] | Block/heading boundary primitives (in-ai-block-p, find-instruction-heading, replace-block-content, clean-title), needs 4.5 |
|   5.9 | [[file:code-agent-org-title-gen.org::#overview][code-agent-org-title-gen]] | Auto-title generator (copilot + claude backends), needs 4.5 + 5.8 + trace |
|   5.95 | [[file:code-agent-org-workspace.org::#overview][code-agent-org-workspace]] | Living-workspace navigation, ACTIVE_STORY lifecycle, CUSTOM_ID generation, story archival, needs 4.5 + 5.8 + session-context |
|     6 | [[file:code-agent-org-queue.org][code-agent-org-queue]] | Block queue, needs 5           |
|     7 | [[file:code-agent-org-response.org][code-agent-org-response]] | Response rendering, needs 5  |
|   7.5 | [[file:code-agent-org-process.org::#overview][code-agent-org-process]] | Persistent client class + backend registry + query-id tracking + marker lifecycle + AI_EXEC_STATUS, needs 5/5.2/5.5/5.7/5.8/7 |
|     8 | [[file:code-agent-org.org][code-agent-org]]     | Top-level org integration      |
|   8.5 | [[file:code-agent-org-header-line.org][code-agent-org-header-line]] | Header-line UI for code-agent-org buffers (loaded after the parent so its symbols are bound when the major mode is enabled) |
|     9 | [[file:code-agent-org-scheduled.org][code-agent-org-scheduled]] | Scheduled execution, needs 8 |
|  10.5 | [[file:code-agent-org-workspace-bridge.org][code-agent-org-workspace-bridge]] | Workspace bridge functions, needs 8 |
|  10.6 | [[file:code-agent-org-terminal-base.org][code-agent-org-terminal-base]] | Shared terminal utilities, needs 4 |
|    11 | [[file:code-agent-org-cmux.org][code-agent-org-cmux]] | cmux terminal backend, needs 10.5/10.6 |
|    14 | [[file:emacs-mcp-server.org][emacs-mcp-server]]   | Independent, loaded last       |
|   opt | [[file:claude-ide.org][claude-ide]]         | WebSocket IDE, needs websocket |
|   4.7 | [[file:code-agent-pi-backend.org][code-agent-pi-backend]] | pi.dev RPC backend (loaded by default; needs `pi' npm at runtime) |
|   opt | [[file:code-agent-acp.org][code-agent-acp]]   | OpenCode ACP backend, needs acp pkg |
|   opt | [[file:code-agent-pi-extension.org][code-agent-pi-extension]] | Tangle source for Pi-side TS extension; install via `make tangle-pi-extension' |

* Invariants

- Every public function has a docstring
- Every test file appears in a Makefile target
- Session state lives in hash tables, never global variables
- Process filters/sentinels wrapped in condition-case (F36 enforces)
- All code lives in =#+begin_src elisp= blocks (literate-elisp requirement)
- Timer callbacks check liveness before operating on process/buffer
- Test failures include =FIX:= remediation messages

* Absence Constraints

| Constraint                                      | Why                                    |
|-------------------------------------------------+----------------------------------------|
| code-agent.org has no org-mode dependency      | Core SDK must work without org         |
| emacs-mcp-server.org has no claude-* dependency  | Reusable as standalone MCP server      |
| No circular dependencies between modules         | Load order would break                 |
| No hardcoded paths in test files                 | Portable CI                            |
| literate-elisp ignores lexical-binding: t        | Use lexical-let for closures           |
| Tests never live inside .org files               | Always in tests/*.el                   |
| No global mutable state                          | Session state keyed by buffer-local    |
| No iTerm2 references in source .org files        | Backend removed; use generic terms     |
| No standalone =code-agent--active-states= var  | Registry is sole owner; prevents split-brain |
| No unguarded =(if parsed ...)= in JSON dispatch | Must check =(listp parsed)= first; bare JSON crashes |

* Extension Points

| To add...              | Go here                                                    |
|------------------------+------------------------------------------------------------|
| New agent backend      | Subclass =code-agent-backend= directly (or =code-agent-acp-backend= for ACP profiles); register in =code-agent-org-backend-registry= (see =code-agent-pi-backend.org=) |
| New Pi extension tool  | Add =*** tool= subsection to =code-agent-pi-extension.org=; =make tangle-pi-extension= |
| New multiplexer backend| Subclass =code-agent-multiplexer-backend=; register in =code-agent-org-backend-registry= |
| New MCP tool           | Call =emacs-mcp-server-register-tool= from any module      |
| New structural test    | Add =ert-deftest= to test-structural.el with =FIX:= msg   |
| New org sub-module     | Create code-agent-org-NAME.org, add to code-agent.el/Makefile |
| New test file          | Create tests/test-NAME.el, add to Makefile target          |
| New behavior prompt    | Add .org file to prompts/tags/                             |
| New agent profile      | Push onto =code-agent-multiplexer--agent-profiles= (post Phase 3); add launch-fn + defcustom + Python launcher + bridge hooks |

* Development Lifecycle

#+begin_example
  Edit .org  ──>  Reload  ──>  Smoke  ──>  Unit
     │               │           │           │
  any .org     M-x literate-   make        make
   file        elisp-load    test-smoke  test-unit
#+end_example

* Literate Programming

Elisp source in .org files loaded by literate-elisp. Key constraints:
=lexical-let= for closures, =cond=+equal= for string dispatch. The full
document-first protocol + mechanical org conventions (heading levels,
block naming, property inheritance) live in the imported literate-agent
=literate-programming-document-first.md= rule (see =CLAUDE.md= top).

* Python LP

=code-agent-python.org= (tangle to =python/claude_agent/*.py=). =make tangle-python=, =make check-python-structure=. Hook blocks direct .py edits.

* Tri-Protocol Architecture

Shipped 2026-04-24 (Phases 0–4). Full design in =code-agent-backend.org=
→ "Tri-Protocol Architecture". Three protocols — lifecycle
(=code-agent-backend-*=), mux-wire (=code-agent-mux-*=),
org-integration (=code-agent-org-backend-*=). The agent-wire layer retired
2026-05; agent backends inherit lifecycle base directly. Families:
agent (claude-code, opencode, gemini, codex, pi) and multiplexer (cmux,
tmux), symmetric siblings. Frontend dispatches via Protocol 3 generics.

* External Pattern Studies

Ralph (overnight loop) and Atomic (SDLC-style harness) — per-pattern
adoption notes in =code-agent-org.org= → "External Pattern Studies —
Ralph and Atomic".

* Removed Backends

Pre-Tri-Protocol approaches deleted in commit =1038144= once cmux + tmux
covered the same ground better:

| Removed | Tried | Why it lost |
|---------+-------+-------------|
| iTerm2 driver | =iterm2-ctl= + screen-scrape | macOS-only; brittle; cmux has native hooks |
| Native eat / vterm | Run Claude in-Emacs terminal | Emacs main thread blocks; can't survive process restart |
| In-Emacs harness (general) | Various wrappers | External multiplexer wins on every axis |

Survived: =--active-queries= registry / mode-line spinner /
=*Claude Queries*= buffer (now drive cmux + tmux); JSON-stream path —
see =code-agent-backend.org= → "JSON Stream Backend" → "Design
Heritage".

* Testing & Harness

Three real surfaces (no boundary mocks):

| Surface | Location | Driver |
|---------+----------+--------|
| Unit | =tests/test-*.el= =:unit= | =make test-unit= |
| Structural | =tests/test-structural.el= | with =FIX:= remediation msgs |
| E2E | =tests/e2e/E2E_TASKS.org= (32 tasks: cmux + tmux + bridge + cross-cutting) | =/loop= skill |

Long-lived E2E fixtures: =tests/e2e/org/{workspace,tmux,pi-backend}-test.org=.
Harness lessons distilled into =.claude/rules/= (LP / oop-smalltalk / bounded-resource / no-backward-compat / e2e-trace).
